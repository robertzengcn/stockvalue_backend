---
phase: 03-test-coverage
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-05"]
files_modified:
  - stockvaluefinder/tests/integration/test_repositories.py
autonomous: true
requirements: [TEST-06]

must_haves:
  truths:
    - "StockRepository.create persists a stock with ticker, name, market, industry and get_by_ticker retrieves it"
    - "FinancialReportRepository.create persists a report linked to a ticker and get_by_ticker returns it"
    - "RiskScoreRepository.create persists a risk score and get_by_score_id retrieves it by UUID"
    - "RiskScoreRepository.upsert_by_report_id inserts new score and updates existing score for same report_id without duplication"
    - "ValuationRepository.create persists a valuation result and get_by_valuation_id retrieves it"
    - "YieldGapRepository.create persists a yield gap analysis and get_by_analysis_id retrieves it"
    - "DividendRepository.create persists a dividend record and get_by_ticker returns it"
    - "RateRepository.create persists rate data and get_by_rate_date retrieves it"
    - "All repository tests use test database and rollback after each test"
  artifacts:
    - path: "stockvaluefinder/tests/integration/test_repositories.py"
      provides: "Repository CRUD integration tests for all 7 repositories"
      contains: "class TestStockRepository"
      min_lines: 300
  key_links:
    - from: "test_repositories.py"
      to: "stockvaluefinder/stockvaluefinder/repositories/"
      via: "Repository instances initialized with real async DB session from integration conftest.py"
      pattern: "Repository.*db_session"
---

<objective>
Create integration tests for database persistence layer (all 7 repositories).

Purpose: Per D-02 (full coverage including repositories) and D-04 (E2E with database). Repository tests verify that SQLAlchemy ORM models map correctly to database tables and CRUD operations work with real PostgreSQL. Uses test database infrastructure established in Plan 05 (integration/conftest.py with session-scoped test_engine and per-test db_session with rollback).

Output: test_repositories.py with CRUD tests for stock, financial_report, risk_score, valuation_result, yield_gap, dividend, and rate repositories -- a total of 38 test methods across 8 test classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-test-coverage/03-CONTEXT.md
@stockvaluefinder/stockvaluefinder/repositories/base.py
@stockvaluefinder/stockvaluefinder/repositories/stock_repo.py
@stockvaluefinder/stockvaluefinder/repositories/financial_repo.py
@stockvaluefinder/stockvaluefinder/repositories/risk_repo.py
@stockvaluefinder/stockvaluefinder/repositories/valuation_repo.py
@stockvaluefinder/stockvaluefinder/repositories/yield_repo.py
@stockvaluefinder/stockvaluefinder/repositories/dividend_repo.py
@stockvaluefinder/stockvaluefinder/repositories/rate_repo.py
@stockvaluefinder/stockvaluefinder/models/enums.py
@stockvaluefinder/stockvaluefinder/models/stock.py
@stockvaluefinder/stockvaluefinder/models/financial.py
@stockvaluefinder/stockvaluefinder/models/risk.py
@stockvaluefinder/stockvaluefinder/models/valuation.py
@stockvaluefinder/stockvaluefinder/models/yield_gap.py
@stockvaluefinder/stockvaluefinder/models/dividend.py
@stockvaluefinder/stockvaluefinder/models/rate.py
@stockvaluefinder/tests/integration/conftest.py

<interfaces>
<!-- Key types and contracts extracted from codebase for the executor -->

From stockvaluefinder/tests/integration/conftest.py:
- TEST_DB_URL = "postgresql+asyncpg://svf_admin:Fo41_2vhaOHKnBAyMUToMA@localhost:5433/stockvaluefinder_test"
- test_engine: session-scoped AsyncEngine fixture (creates/drops all tables)
- db_session: per-test AsyncSession fixture (yields session, then rollback)
- @pytest.mark.skip_if_no_db: custom marker registered via pytest_configure
- db_session fixture uses expire_on_commit=False

From stockvaluefinder/repositories/base.py:
- BaseRepository(Generic[ModelType, CreateSchemaType, UpdateSchemaType])
- __init__(self, model: type[ModelType], session: AsyncSession)
- async get_by_id(self, id: Any) -> ModelType | None
- async get_all(self, limit=100, offset=0) -> list[ModelType]
- async create(self, data: CreateSchemaType) -> ModelType
- async update(self, id: Any, data: UpdateSchemaType) -> ModelType | None
- async delete(self, id: Any) -> bool

From stockvaluefinder/models/enums.py:
- Market: A_SHARE, HK_SHARE (str enum)
- ReportType: ANNUAL, QUARTERLY (str enum)
- RiskLevel: LOW, MEDIUM, HIGH, CRITICAL (str enum)
- DividendFrequency: ANNUAL, SEMI_ANNUAL, QUARTERLY, SPECIAL (str enum)
- YieldRecommendation: ATTRACTIVE, NEUTRAL, UNATTRACTIVE (str enum)
- ValuationLevel: SIGNIFICANTLY_UNDERVALUED, UNDERVALUED, FAIR_VALUE, OVERVALUED, SIGNIFICANTLY_OVERVALUED (str enum)

From stockvaluefinder/models/stock.py:
- StockCreate(ticker: str, name: str, market: Market, industry: str, list_date: date)
- StockUpdate(name: str | None, industry: str | None, list_date: date | None)

From stockvaluefinder/models/financial.py:
- FinancialReportCreate(ticker, period: str "YYYY-MM-DD", report_type: ReportType, revenue, net_income, operating_cash_flow, gross_margin, assets_total, liabilities_total, equity_total, accounts_receivable, inventory, fixed_assets, goodwill, cash_and_equivalents, interest_bearing_debt, report_source: str, fiscal_year: int, fiscal_quarter: int | None)
- Note: fiscal_quarter must be None for ANNUAL, required for QUARTERLY

From stockvaluefinder/models/risk.py:
- RiskScoreCreate(score_id: UUID, ticker: str, report_id: UUID, risk_level: RiskLevel, m_score: float, mscore_data: MScoreData, f_score: int, fscore_data: FScoreData, 存贷双高: bool, cash_amount: Decimal, debt_amount: Decimal, cash_growth_rate: float, debt_growth_rate: float, goodwill_ratio: float, goodwill_excessive: bool, profit_cash_divergence: bool, profit_growth: float, ocf_growth: float, red_flags: list[str], narrative: str | None)
- MScoreData(dsri, gmi, aqi, sgi, depi, sgai, lvgi, tata, audit_trail: dict)
- FScoreData(positive_roa, positive_cfo, improving_roa, cfo_exceeds_roa, lower_leverage, higher_liquidity, no_new_shares, improving_margin, improving_turnover: bool)

From stockvaluefinder/models/valuation.py:
- ValuationResultCreate(valuation_id: UUID, ticker: str, current_price: Decimal, intrinsic_value: Decimal, wacc: float, margin_of_safety: float, valuation_level: ValuationLevel, calculated_at: datetime, dcf_params: DCFParams, audit_trail: dict, narrative: str | None)
- DCFParams(growth_rate_stage1, growth_rate_stage2, years_stage1, years_stage2, terminal_growth, risk_free_rate, beta, market_risk_premium: float)

From stockvaluefinder/models/yield_gap.py:
- YieldGapCreate(analysis_id: UUID, ticker: str, cost_basis: Decimal, current_price: Decimal, gross_dividend_yield: float, net_dividend_yield: float, risk_free_bond_rate: float, risk_free_deposit_rate: float, yield_gap: float, recommendation: YieldRecommendation, market: Market, calculated_at: datetime, narrative: str | None)

From stockvaluefinder/models/dividend.py:
- DividendCreate(ticker: str, ex_dividend_date: date, dividend_per_share: Decimal, dividend_frequency: DividendFrequency, fiscal_year: int | None)

From stockvaluefinder/models/rate.py:
- RateDataCreate(rate_date: date, ten_year_treasury: float, three_year_deposit: float, one_year_deposit: float, benchmark_rate: float, rate_source: str)

CRITICAL NOTES from 03-05-SUMMARY:
- Use @pytest.mark.skip_if_no_db decorator pattern (custom pytest marker), NOT "from conftest import skip_if_no_db"
- Use await session.commit() after writes since db_session fixture rolls back after each test
- The conftest.py registers the marker via pytest_configure and applies skip via pytest_collection_modifyitems hook
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repository integration tests for StockRepository, FinancialReportRepository, RiskScoreRepository, and ValuationRepository</name>
  <files>stockvaluefinder/tests/integration/test_repositories.py</files>
  <read_first>
    stockvaluefinder/stockvaluefinder/repositories/base.py
    stockvaluefinder/stockvaluefinder/repositories/stock_repo.py
    stockvaluefinder/stockvaluefinder/repositories/financial_repo.py
    stockvaluefinder/stockvaluefinder/repositories/risk_repo.py
    stockvaluefinder/stockvaluefinder/repositories/valuation_repo.py
    stockvaluefinder/stockvaluefinder/models/enums.py
    stockvaluefinder/stockvaluefinder/models/stock.py
    stockvaluefinder/stockvaluefinder/models/financial.py
    stockvaluefinder/stockvaluefinder/models/risk.py
    stockvaluefinder/stockvaluefinder/models/valuation.py
    stockvaluefinder/tests/integration/conftest.py
  </read_first>
  <action>
Per D-02 (full coverage including repositories), D-04 (E2E with database), and TEST-06 (database persistence integration tests):

Create stockvaluefinder/tests/integration/test_repositories.py with the first 4 repository test classes.

IMPORTANT: Use `@pytest.mark.skip_if_no_db` decorator on EVERY test function (NOT "from conftest import skip_if_no_db" -- the 03-05 execution established this pattern via custom pytest marker registered in pytest_configure). Each test must call `await db_session.commit()` after writes since the db_session fixture from integration conftest.py does rollback after each test.

Structure:

```python
"""Integration tests for repository CRUD operations with real database.

Per D-02: Full coverage including all 7 repositories.
Per D-04: E2E with database (route->service->repo->DB->response).
Per D-05: Uses stockvaluefinder_test database on same PostgreSQL instance.
"""

import pytest
from datetime import date, datetime, timezone
from decimal import Decimal
from uuid import uuid4

from sqlalchemy.ext.asyncio import AsyncSession

from stockvaluefinder.models.enums import (
    DividendFrequency,
    Market,
    ReportType,
    RiskLevel,
    ValuationLevel,
    YieldRecommendation,
)
from stockvaluefinder.models.financial import FinancialReportCreate
from stockvaluefinder.models.risk import (
    FScoreData,
    MScoreData,
    RiskScoreCreate,
)
from stockvaluefinder.models.stock import StockCreate
from stockvaluefinder.models.valuation import (
    DCFParams,
    ValuationResultCreate,
)
from stockvaluefinder.repositories.financial_repo import FinancialReportRepository
from stockvaluefinder.repositories.risk_repo import RiskScoreRepository
from stockvaluefinder.repositories.stock_repo import StockRepository
from stockvaluefinder.repositories.valuation_repo import ValuationRepository
```

Test classes to create:

1. **TestStockRepository** -- 6 tests:
   - `test_create_stock`: Create StockRepository(db_session), call repo.create(StockCreate(ticker="600519.SH", name="贵州茅台", market=Market.A_SHARE, industry="白酒", list_date=date(2001,8,27))). Call await db_session.commit(). Assert returned entity has ticker="600519.SH" and non-None id.
   - `test_get_by_ticker_found`: Create stock, commit, then call repo.get_by_ticker("600519.SH"). Assert returns entity with correct name.
   - `test_get_by_ticker_not_found`: Call repo.get_by_ticker("999999.SH"). Assert returns None.
   - `test_ticker_exists_true`: Create stock, commit, call repo.ticker_exists("600519.SH"). Assert True.
   - `test_ticker_exists_false`: Call repo.ticker_exists("999999.SH"). Assert False.
   - `test_get_all`: Create 2 stocks (600519.SH, 000858.SZ), commit, call repo.get_all(limit=10). Assert returned list length >= 2.

2. **TestFinancialReportRepository** -- 5 tests:
   - `test_create_financial_report`: First create and commit a stock for FK. Then create FinancialReportRepository(db_session), call repo.create(FinancialReportCreate(ticker="600519.SH", period="2023-12-31", report_type=ReportType.ANNUAL, revenue=Decimal("127554000000"), net_income=Decimal("74734000000"), operating_cash_flow=Decimal("58150000000"), gross_margin=87.6, assets_total=Decimal("255000000000"), liabilities_total=Decimal("75000000000"), equity_total=Decimal("180000000000"), accounts_receivable=Decimal("3500000000"), inventory=Decimal("40000000000"), fixed_assets=Decimal("25000000000"), goodwill=Decimal("500000000"), cash_and_equivalents=Decimal("150000000000"), interest_bearing_debt=Decimal("2000000000"), report_source="AKShare", fiscal_year=2023, fiscal_quarter=None)). Commit. Assert returned entity has report_id (UUID auto-generated) and ticker="600519.SH".
   - `test_get_by_ticker`: Create report, commit, call repo.get_by_ticker("600519.SH"). Assert list with at least 1 item.
   - `test_get_by_ticker_and_period`: Create report, commit, call repo.get_by_ticker_and_period("600519.SH", date(2023,12,31)). Assert returns the report.
   - `test_get_latest_annual`: Create report, commit, call repo.get_latest_annual("600519.SH"). Assert returns the report.
   - `test_exists_for_ticker_and_period`: Create report, commit, call repo.exists_for_ticker_and_period("600519.SH", date(2023,12,31)). Assert True.

3. **TestRiskScoreRepository** -- 5 tests:
   - `test_create_risk_score`: First create stock + financial report (commit both). Then create RiskScoreRepository(db_session). Build MScoreData(dsri=1.0, gmi=1.0, aqi=1.0, sgi=1.0, depi=1.0, sgai=1.0, lvgi=1.0, tata=0.01, audit_trail={}) and FScoreData(positive_roa=True, positive_cfo=True, improving_roa=True, cfo_exceeds_roa=True, lower_leverage=True, higher_liquidity=True, no_new_shares=True, improving_margin=True, improving_turnover=True). Create RiskScoreCreate with score_id=uuid4(), ticker="600519.SH", report_id=(the report's report_id), risk_level=RiskLevel.LOW, m_score=-3.5, mscore_data=(the MScoreData), f_score=9, fscore_data=(the FScoreData), 存贷双高=False, cash_amount=Decimal("150000000000"), debt_amount=Decimal("2000000000"), cash_growth_rate=0.05, debt_growth_rate=0.02, goodwill_ratio=0.003, goodwill_excessive=False, profit_cash_divergence=False, profit_growth=0.1, ocf_growth=0.08, red_flags=[], narrative=None. Call repo.create(data). Commit. Assert returned entity has score_id matching input.
   - `test_get_by_score_id`: Create risk score, commit, call repo.get_by_score_id(score_id). Assert returns entity.
   - `test_get_by_score_id_not_found`: Call repo.get_by_score_id(uuid4()). Assert returns None.
   - `test_upsert_by_report_id_insert`: Create risk score data, commit. Call repo.upsert_by_report_id with the same RiskScoreCreate data. Assert returns entity (insert path since no prior record for that report_id).
   - `test_upsert_by_report_id_update`: Create risk score (m_score=-3.5), commit. Build new RiskScoreCreate with same report_id but m_score=-2.0 and same score_id. Call repo.upsert_by_report_id(new_data). Commit. Assert the returned entity has m_score close to -2.0 (updated, not duplicated). Query get_by_report_id to verify only 1 record exists.

   Helper: Create a module-level async helper function `_create_stock_and_report(db_session)` that creates a Stock and FinancialReport, commits them, and returns (stock, report). Reuse in all risk and valuation tests to avoid repetition.

4. **TestValuationRepository** -- 4 tests:
   - `test_create_valuation_result`: Create stock (commit). Create ValuationRepository(db_session). Build DCFParams(growth_rate_stage1=0.05, growth_rate_stage2=0.03, years_stage1=5, years_stage2=5, terminal_growth=0.025, risk_free_rate=0.028, beta=0.8, market_risk_premium=0.07). Create ValuationResultCreate(valuation_id=uuid4(), ticker="600519.SH", current_price=Decimal("1800.00"), intrinsic_value=Decimal("2200.00"), wacc=0.084, margin_of_safety=0.222, valuation_level=ValuationLevel.UNDERVALUED, calculated_at=datetime.now(timezone.utc), dcf_params=(the DCFParams), audit_trail={"wacc_components": {}}, narrative=None). Call repo.create(data). Commit. Assert returned entity has valuation_id matching input.
   - `test_get_by_valuation_id`: Create valuation, commit. Call repo.get_by_valuation_id(valuation_id). Assert returns entity with ticker="600519.SH".
   - `test_get_by_valuation_id_not_found`: Call repo.get_by_valuation_id(uuid4()). Assert returns None.
   - `test_get_latest_for_ticker`: Create valuation, commit. Call repo.get_latest_for_ticker("600519.SH"). Assert returns entity.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_repositories.py --co -q 2>&1 | tail -30</automated>
  </verify>
  <done>
    - test_repositories.py exists with TestStockRepository (6 tests), TestFinancialReportRepository (5 tests), TestRiskScoreRepository (5 tests), TestValuationRepository (4 tests)
    - All tests decorated with @pytest.mark.skip_if_no_db
    - All tests use await db_session.commit() after writes
    - Tests can be collected by pytest: 20 test methods
    - Parent entities (stock, financial report) created before child entities (risk score, valuation) to satisfy FK constraints
  </done>
  <acceptance_criteria>
    - stockvaluefinder/tests/integration/test_repositories.py exists
    - File contains `class TestStockRepository` with methods test_create_stock, test_get_by_ticker_found, test_get_by_ticker_not_found
    - File contains `class TestFinancialReportRepository` with methods test_create_financial_report, test_get_by_ticker, test_get_latest_annual
    - File contains `class TestRiskScoreRepository` with methods test_create_risk_score, test_upsert_by_report_id_insert, test_upsert_by_report_id_update
    - File contains `class TestValuationRepository` with methods test_create_valuation_result, test_get_by_valuation_id
    - File contains `@pytest.mark.skip_if_no_db` on all test functions
    - File contains `await db_session.commit()` after create calls
    - `uv run pytest tests/integration/test_repositories.py --co -q` exits 0 and collects 20 tests
    - `uv run ruff check tests/integration/test_repositories.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add repository tests for YieldGapRepository, DividendRepository, RateRepository, and BaseRepository generic CRUD</name>
  <files>stockvaluefinder/tests/integration/test_repositories.py</files>
  <read_first>
    stockvaluefinder/stockvaluefinder/repositories/base.py
    stockvaluefinder/stockvaluefinder/repositories/yield_repo.py
    stockvaluefinder/stockvaluefinder/repositories/dividend_repo.py
    stockvaluefinder/stockvaluefinder/repositories/rate_repo.py
    stockvaluefinder/stockvaluefinder/models/enums.py
    stockvaluefinder/stockvaluefinder/models/yield_gap.py
    stockvaluefinder/stockvaluefinder/models/dividend.py
    stockvaluefinder/stockvaluefinder/models/rate.py
    stockvaluefinder/tests/integration/conftest.py
    stockvaluefinder/tests/integration/test_repositories.py
  </read_first>
  <action>
Per D-02 (full coverage) and TEST-06 (database persistence integration tests):

Append 3 more repository test classes and 1 base CRUD test class to the existing test_repositories.py file created in Task 1.

5. **TestYieldGapRepository** -- 4 tests:
   - `test_create_yield_gap`: First create and commit a stock. Create YieldGapRepository(db_session). Create YieldGapCreate(analysis_id=uuid4(), ticker="600519.SH", cost_basis=Decimal("1800.00"), current_price=Decimal("1750.00"), gross_dividend_yield=0.022, net_dividend_yield=0.0176, risk_free_bond_rate=0.028, risk_free_deposit_rate=0.025, yield_gap=-0.0104, recommendation=YieldRecommendation.UNATTRACTIVE, market=Market.A_SHARE, calculated_at=datetime.now(timezone.utc), narrative=None). Call repo.create(data). Commit. Assert returned entity has analysis_id and recommendation=="UNATTRACTIVE" (stored as string).
   - `test_get_by_analysis_id`: Create yield gap, commit. Call repo.get_by_analysis_id(analysis_id). Assert returns entity.
   - `test_get_by_analysis_id_not_found`: Call repo.get_by_analysis_id(uuid4()). Assert returns None.
   - `test_get_latest_for_ticker`: Create yield gap, commit. Call repo.get_latest_for_ticker("600519.SH"). Assert returns entity.

6. **TestDividendRepository** -- 4 tests:
   - `test_create_dividend`: Create DividendRepository(db_session). Create DividendCreate(ticker="600519.SH", ex_dividend_date=date(2024,6,30), dividend_per_share=Decimal("30.87"), dividend_frequency=DividendFrequency.ANNUAL, fiscal_year=2023). Call repo.create(data). Commit. Assert returned entity has auto-generated dividend_id (string UUID) and dividend_per_share==Decimal("30.87").
   - `test_get_by_ticker`: Create dividend, commit. Call repo.get_by_ticker("600519.SH"). Assert list with at least 1 item.
   - `test_get_latest_dividend`: Create dividend, commit. Call repo.get_latest_dividend("600519.SH"). Assert returns entity.
   - `test_get_by_ticker_and_year`: Create dividend with fiscal_year=2023, commit. Call repo.get_by_ticker_and_year("600519.SH", 2023). Assert list with at least 1 item.

7. **TestRateRepository** -- 5 tests:
   - `test_create_rate`: Create RateRepository(db_session). Create RateDataCreate(rate_date=date(2024,1,15), ten_year_treasury=0.028, three_year_deposit=0.025, one_year_deposit=0.015, benchmark_rate=0.035, rate_source="AKShare"). Call repo.create(data). Commit. Assert returned entity has rate_id (auto-generated string UUID) and ten_year_treasury==0.028.
   - `test_get_by_rate_date`: Create rate, commit. Call repo.get_by_rate_date(date(2024,1,15)). Assert returns entity.
   - `test_get_by_rate_date_not_found`: Call repo.get_by_rate_date(date(2020,1,1)). Assert returns None.
   - `test_get_latest_rate`: Create rate with date(2024,1,15), commit. Call repo.get_latest_rate(). Assert returns entity with rate_date >= date(2024,1,15).
   - `test_rate_date_exists`: Create rate, commit. Call repo.rate_date_exists(date(2024,1,15)). Assert True.

8. **TestBaseRepositoryCRUD** -- 5 tests using RateRepository as the concrete implementation (simplest schema):
   - `test_get_by_id_returns_entity`: Create rate, commit. Call repo.get_by_id(rate_id). Assert returns entity.
   - `test_get_by_id_returns_none_for_missing`: Call repo.get_by_id("nonexistent-uuid"). Assert returns None.
   - `test_get_all_returns_entities`: Create 2 rates with different dates, commit. Call repo.get_all(limit=10). Assert returned list length >= 2.
   - `test_delete_entity`: Create rate, commit. Call repo.delete(rate_id). Assert returns True. Call repo.get_by_id(rate_id). Assert returns None.
   - `test_delete_nonexistent_returns_false`: Call repo.delete("nonexistent-uuid"). Assert returns False.

IMPORTANT patterns to follow:
- All tests decorated with @pytest.mark.skip_if_no_db
- All tests use `await db_session.commit()` after writes
- Use uuid4() for UUID fields (score_id, report_id, valuation_id, analysis_id)
- RateRepository.create generates rate_id internally via str(uuid4()), so use the returned entity's rate_id for subsequent lookups
- StockRepository.create has a custom create() that takes StockCreate and sets created_at/updated_at internally
- Use expire_on_commit=False (already set in conftest) so entities remain accessible after commit
- Each test creates its own independent data (no shared mutable state between tests)
  </action>
  <verify>
    <automated>cd /Users/cengjianze/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/integration/test_repositories.py --co -q 2>&1 | tail -40</automated>
  </verify>
  <done>
    - test_repositories.py now contains all 8 test classes (4 from Task 1 + 4 new)
    - Total 38 test methods covering all 7 repositories + base CRUD
    - All tests decorated with @pytest.mark.skip_if_no_db
    - Tests can be collected by pytest
    - ruff check passes
  </done>
  <acceptance_criteria>
    - test_repositories.py contains `class TestYieldGapRepository` with test_create_yield_gap, test_get_by_analysis_id
    - test_repositories.py contains `class TestDividendRepository` with test_create_dividend, test_get_by_ticker
    - test_repositories.py contains `class TestRateRepository` with test_create_rate, test_get_by_rate_date
    - test_repositories.py contains `class TestBaseRepositoryCRUD` with test_get_by_id_returns_entity, test_delete_entity
    - `uv run pytest tests/integration/test_repositories.py --co -q` exits 0 and collects 38 tests
    - `uv run ruff check tests/integration/test_repositories.py` exits 0
    - `uv run mypy tests/integration/test_repositories.py` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository -> Database | Test database isolation prevents production data contamination |
| Test fixtures -> Session | Per-test rollback prevents cross-test data leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | I | Test data in production | mitigate | Separate stockvaluefinder_test database per D-05, conftest.py creates/drops tables per session |
| T-03-09 | D | Cross-test data leakage | mitigate | db_session fixture rolls back after each test, each test creates its own data |

</threat_model>

<verification>
- `uv run pytest tests/integration/test_repositories.py --co -q` -- collects 38 tests
- `uv run ruff check tests/integration/test_repositories.py` -- passes
- `uv run pytest tests/integration/test_repositories.py -x` -- passes when PostgreSQL is available
</verification>

<success_criteria>
- test_repositories.py created with tests for all 7 repositories + base CRUD (8 classes, 38 methods)
- All tests marked with @pytest.mark.skip_if_no_db for graceful handling when DB unavailable
- 20+ test methods covering create, read, update, delete, and domain-specific operations (upsert_by_report_id, get_by_ticker, etc.)
- Tests can be collected by pytest without errors
- ruff check passes, mypy passes
- Foreign key constraints satisfied: parent entities created before child entities
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-06-SUMMARY.md`
</output>
