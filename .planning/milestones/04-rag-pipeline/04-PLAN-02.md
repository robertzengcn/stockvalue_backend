---
wave: 2
depends_on: [04-01, 04-01B]
files_modified:
  - stockvaluefinder/rag/pdf_processor.py
  - stockvaluefinder/rag/embeddings.py
  - stockvaluefinder/rag/vector_store.py
  - stockvaluefinder/rag/retriever.py
must_haves:
  truths:
    - PDF processor extracts content with page references and table preservation
    - Embedding client generates 1024-dim vectors via OpenRouter API
    - Qdrant store creates collection and upserts chunks
    - Retriever returns parent-child pairs with page references
  artifacts:
    - stockvaluefinder/rag/pdf_processor.py
    - stockvaluefinder/rag/embeddings.py
    - stockvaluefinder/rag/vector_store.py
    - stockvaluefinder/rag/retriever.py
  key_links:
    - pdf_processor chunks -> embeddings generates vectors -> vector_store upserts -> retriever searches
requirements: DATA-04, DATA-05, DATA-06, DATA-07
---

# Phase 4 Plan 02: RAG Core Modules

**Goal:** Implement PDF processing, embedding generation, vector storage, and semantic retrieval.

## Tasks

<Task id="04-02-01">
<action>Implement PDF processor in rag/pdf_processor.py with functions: extract_pdf_content(pdf_bytes) returning list of dicts with type/text/page/bbox, chunk_into_parents(content_blocks, target_tokens=2000) returning parent chunks, chunk_parents_into_children(parent_chunks, target_tokens=500) returning child chunks with parent_id, detect_tables_and_preserve(text_content) identifying table boundaries. Use PyMuPDF (fitz) for extraction, tiktoken cl100k_base for token counting, preserve tables as atomic units even if exceeding 500 tokens</action>
<read_first>
- stockvaluefinder/rag/pdf_processor.py (existing stub)
- .planning/phases/04-rag-pipeline/04-RESEARCH.md §Pattern 4 (PDF Processing with PyMuPDF)
</read_first>
<acceptance_criteria>
- pdf_processor.py contains `def extract_pdf_content(pdf_bytes: bytes) -> list[dict]:`
- extract_pdf_content uses `fitz.open(stream=pdf_bytes, filetype="pdf")`
- pdf_processor.py contains `def chunk_into_parents(content_blocks: list[dict], target_tokens: int = 2000) -> list[DocumentChunk]:`
- pdf_processor.py contains `def chunk_parents_into_children(parent_chunks: list[DocumentChunk], target_tokens: int = 500) -> list[DocumentChunk]:`
- Tables are detected via `page.find_tables()` and exported as markdown
- Token counting uses `tiktoken.get_encoding("cl100k_base")`
- mypy passes: `uv run mypy stockvaluefinder/rag/pdf_processor.py` exits 0
</acceptance_criteria>
<verify>
<automated>uv run pytest tests/unit/test_rag/test_pdf_processor.py -x -q</automated>
</verify>
<done>
- All 4 functions implemented
- Tables preserved as atomic units
- mypy and tests pass
</done>
</Task>

<Task id="04-02-02">
<action>Implement embedding client in rag/embeddings.py with class BGEEmbeddingClient: __init__(api_url, api_key_env, model, batch_size), generate_embeddings(texts: list[str]) -> list[list[float]] using OpenRouter API via httpx.AsyncClient, generate_query_embedding(query: str) -> list[float] for single query. API call uses POST to api_url with headers Authorization: Bearer {api_key}, Content-Type: application/json, body {model, input}. Handle batch up to batch_size (default 32), retry with exponential backoff on 429/503</action>
<read_first>
- stockvaluefinder/rag/embeddings.py (existing stub)
- .planning/phases/04-rag-pipeline/04-RESEARCH.md §Pattern 3 (OpenAI-Compatible Embedding API)
- stockvaluefinder/external/rate_client.py (async HTTP pattern)
</read_first>
<acceptance_criteria>
- embeddings.py contains `class BGEEmbeddingClient:`
- BGEEmbeddingClient has __init__ with api_url, api_key_env, model, batch_size parameters
- BGEEmbeddingClient has `async def generate_embeddings(self, texts: list[str]) -> list[list[float]]:`
- API call uses `httpx.AsyncClient().post()` with correct headers and JSON body
- API key read from env var via `os.environ.get(api_key_env)`
- Returns list of 1024-dim float vectors
- mypy passes: `uv run mypy stockvaluefinder/rag/embeddings.py` exits 0
</acceptance_criteria>
<verify>
<automated>uv run pytest tests/unit/test_rag/test_embeddings.py -x -q</automated>
</verify>
<done>
- BGEEmbeddingClient implemented
- API key from env var
- mypy and tests pass
</done>
</Task>

<Task id="04-02-03">
<action>Implement Qdrant vector store in rag/vector_store.py with class QdrantVectorStore: __init__(url, collection, api_key, embedding_client), ensure_collection_exists() creating collection with 1024-dim COSINE distance and payload indexes for ticker/year/report_type, upsert_chunks(chunks: list[DocumentChunk]) converting chunks to PointStruct with vectors, search(query_vector, filter_dict, limit, score_threshold) returning list of scored chunks with payloads, delete_by_document_id(document_id) removing all points for a document</action>
<read_first>
- stockvaluefinder/rag/vector_store.py (existing stub)
- .planning/phases/04-rag-pipeline/04-RESEARCH.md §Pattern 2 (Qdrant Single Collection)
</read_first>
<acceptance_criteria>
- vector_store.py contains `class QdrantVectorStore:`
- __init__ accepts url, collection, api_key, embedding_client
- ensure_collection_exists uses `client.create_collection(vectors_config=VectorParams(size=1024, distance=Distance.COSINE))`
- ensure_collection_exists creates payload indexes for ticker, year, report_type
- upsert_chunks converts chunks to PointStruct with id=chunk.chunk_id, vector=embedding, payload=metadata dict
- search uses `client.search()` with Filter, limit, score_threshold parameters
- mypy passes: `uv run mypy stockvaluefinder/rag/vector_store.py` exits 0
</acceptance_criteria>
<verify>
<automated>uv run pytest tests/integration/test_rag_vector_store.py -x -q</automated>
</verify>
<done>
- QdrantVectorStore implemented
- Collection created with indexes
- mypy and tests pass
</done>
</Task>

<Task id="04-02-04">
<action>Implement semantic retriever in rag/retriever.py with class SemanticRetriever: __init__(vector_store, document_repo, db_session), search(query: str, ticker: str, year: int, limit: int, score_threshold: float) returning list of SearchResult with child chunk + parent content + page reference, search_with_multi_query_expansion(query: str, ticker: str, year: int, limit: int, score_threshold: float, num_variations: int=3) generating 3-5 query variations via LLM, aggregating results, deduplicating by chunk_id, fetch_parent_context(child_chunk_id) fetching parent content from PostgreSQL by parent_id</action>
<read_first>
- stockvaluefinder/rag/retriever.py (existing stub)
- .planning/phases/04-rag-pipeline/04-RESEARCH.md §Pattern 2 (Qdrant search with filters)
- stockvaluefinder/services/narrative_service.py (LLM call pattern)
</read_first>
<acceptance_criteria>
- retriever.py contains `class SemanticRetriever:`
- search method generates query embedding, calls vector_store.search with metadata filter, returns SearchResult objects
- search_with_multi_query_expansion calls LLM to generate 3-5 query variations, searches each, aggregates by score, deduplicates
- SearchResult is a frozen dataclass with fields: chunk_id, content, parent_content, page_number, section, score, ticker, year
- fetch_parent_context queries document_repo for parent chunk by parent_id
- mypy passes: `uv run mypy stockvaluefinder/rag/retriever.py` exits 0
</acceptance_criteria>
<verify>
<automated>uv run pytest tests/unit/test_rag/test_retriever.py -x -q</automated>
</verify>
<done>
- SemanticRetriever implemented
- Multi-query expansion working
- mypy and tests pass
</done>
</Task>

## Verification

After completing this wave:
- [ ] `uv run pytest tests/unit/test_rag/test_pdf_processor.py tests/unit/test_rag/test_embeddings.py tests/unit/test_rag/test_retriever.py -x -q` exits 0
- [ ] `uv run pytest tests/integration/test_rag_vector_store.py -x -q` exits 0 (requires Qdrant running)
- [ ] `uv run mypy stockvaluefinder/rag/` exits 0

## Threat Model

| Threat | STRIDE | Mitigation | Implemented In |
|--------|--------|------------|----------------|
| Malicious PDF upload | Tampering | Validate PDF structure with PyMuPDF, limit file size in route | Task 04-02-01 (extract_pdf_content raises on invalid PDF) |
| Prompt injection via PDF content | Tampering | Do not execute PDF content as code, sanitize query inputs | Task 04-02-04 (query expansion uses LLM safely) |
| DoS via large file upload | Denial of Service | File size limit (100MB), timeout on embedding calls | Task 04-02-02 (timeout=30s on httpx calls) |
