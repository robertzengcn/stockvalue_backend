---
wave: 3
depends_on: [04-01, 04-01B, 04-02]
files_modified:
  - stockvaluefinder/services/document_service.py
must_haves:
  truths:
    - DocumentService orchestrates full upload pipeline
    - Status tracking works at each step
  artifacts:
    - stockvaluefinder/services/document_service.py
  key_links:
    - DocumentService uses pdf_processor, embedding_client, vector_store, document_repo
requirements: DATA-03, DATA-04, DATA-05, DATA-06
---

# Phase 4 Plan 03: Document Service Layer

**Goal:** Create DocumentService to orchestrate the full upload pipeline.

## Tasks

<Task id="04-03-01">
<action>Create document service in services/document_service.py with class DocumentService: __init__(db_session, pdf_processor, embedding_client, vector_store, document_repo), process_upload(document_id, ticker, file_name, file_path, pdf_bytes) orchestrating extract_pdf_content -> chunk_into_parents -> chunk_parents_into_children -> generate_embeddings -> upsert_chunks, updating processing_status at each step, get_document_status(document_id) returning current status and chunk count, delete_document(document_id) removing from Qdrant and database</action>
<read_first>
- stockvaluefinder/services/risk_service.py (service orchestration pattern)
- stockvaluefinder/rag/pdf_processor.py (pdf_processor interface)
- stockvaluefinder/rag/embeddings.py (embedding_client interface)
- stockvaluefinder/rag/vector_store.py (vector_store interface)
- stockvaluefinder/repositories/document_repo.py (document_repo interface)
</read_first>
<acceptance_criteria>
- document_service.py exists with `class DocumentService:`
- process_upload method orchestrates full pipeline: parse -> chunk -> embed -> store
- process_upload updates processing_status: pending -> processing -> completed/failed
- process_upload returns DocumentUploadResponse with document_id, status, chunk_count, page_count
- get_document_status returns current status and chunk count
- delete_document removes from both Qdrant and database
- mypy passes: `uv run mypy stockvaluefinder/services/document_service.py` exits 0
</acceptance_criteria>
<verify>
<automated>uv run pytest tests/unit/test_services/test_document_service.py -x -q</automated>
</verify>
<done>
- DocumentService implemented
- Full pipeline orchestrated
- mypy and tests pass
</done>
</Task>

## Verification

After completing this wave:
- [ ] `uv run pytest tests/unit/test_services/test_document_service.py -x -q` exits 0
- [ ] `uv run mypy stockvaluefinder/services/document_service.py` exits 0

## Threat Model

| Threat | STRIDE | Mitigation | Implemented In |
|--------|--------|------------|----------------|
| DoS via large file upload | Denial of Service | File size limit checked before processing | Task 04-03-01 (validate before process_upload) |
