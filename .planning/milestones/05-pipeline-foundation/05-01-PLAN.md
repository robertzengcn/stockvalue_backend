---
phase: 05-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stockvaluefinder/pipeline/__init__.py
  - stockvaluefinder/pipeline/config.py
  - stockvaluefinder/pipeline/state.py
  - stockvaluefinder/pipeline/models.py
  - stockvaluefinder/db/models/pipeline_task.py
  - stockvaluefinder/db/models/pipeline_document.py
  - stockvaluefinder/utils/errors.py
  - alembic/versions/009_pipeline_tables.py
  - tests/unit/test_pipeline/__init__.py
  - tests/unit/test_pipeline/test_config.py
  - tests/unit/test_pipeline/test_state.py
  - tests/unit/test_pipeline/test_models.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - PIPE-04

must_haves:
  truths:
    - "PipelineConfig frozen dataclass validates configuration and has sensible defaults for retry, concurrency, timeouts, and reaper settings"
    - "PipelineState enum defines exactly 6 states and VALID_TRANSITIONS defines the linear progression PENDING->DOWNLOADING->PARSING->ANALYZING->DONE with FAILED from any step"
    - "StateTransitionError is raised when an invalid transition is attempted"
    - "pipeline_tasks and pipeline_documents tables are created in PostgreSQL via Alembic migration 009"
  artifacts:
    - path: "stockvaluefinder/pipeline/config.py"
      provides: "PipelineConfig frozen dataclass with validation"
      contains: "class PipelineConfig"
    - path: "stockvaluefinder/pipeline/state.py"
      provides: "PipelineState enum, VALID_TRANSITIONS map, validate_transition function, StateTransitionError"
      contains: "class PipelineState"
    - path: "stockvaluefinder/pipeline/models.py"
      provides: "Pydantic domain models for pipeline task and document"
      contains: "class PipelineTask"
    - path: "stockvaluefinder/db/models/pipeline_task.py"
      provides: "PipelineTaskDB ORM model"
      contains: "__tablename__ = \"pipeline_tasks\""
    - path: "stockvaluefinder/db/models/pipeline_document.py"
      provides: "PipelineDocumentDB ORM model"
      contains: "__tablename__ = \"pipeline_documents\""
    - path: "alembic/versions/009_pipeline_tables.py"
      provides: "Alembic migration for pipeline tables"
      contains: "revision: str = \"009\""
    - path: "stockvaluefinder/utils/errors.py"
      provides: "StateTransitionError exception class"
      contains: "class StateTransitionError"
  key_links:
    - from: "stockvaluefinder/pipeline/state.py"
      to: "stockvaluefinder/utils/errors.py"
      via: "StateTransitionError extends StockValueFinderError"
      pattern: "from stockvaluefinder.utils.errors import"
    - from: "stockvaluefinder/db/models/pipeline_task.py"
      to: "stockvaluefinder/pipeline/state.py"
      via: "default='pending' matches PipelineState.PENDING"
      pattern: "default=\"pending\""
---

<objective>
Create the pipeline foundation types: frozen PipelineConfig, custom state machine (PipelineState + VALID_TRANSITIONS), Pydantic domain models, ORM models, and Alembic migration. These are pure Python types with zero infrastructure dependency.

Purpose: Establish all contracts and database schema that subsequent plans (worker, health endpoint) build upon. Pure functions and types first, infrastructure second.
Output: New pipeline module, two ORM models, migration 009, StateTransitionError, and passing unit tests for config and state machine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline-foundation/05-CONTEXT.md
@.planning/phases/05-pipeline-foundation/05-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor MUST follow -->

From stockvaluefinder/config.py (frozen dataclass pattern):
```python
@dataclass(frozen=True)
class ValuationConfig:
    DEFAULT_BETA: float = 1.0
    DEFAULT_MARKET_RISK_PREMIUM: float = 0.06
```

From stockvaluefinder/utils/errors.py (error hierarchy):
```python
class StockValueFinderError(Exception):
    message: str
    details: dict[str, Any]
    def __init__(self, message: str, details: dict[str, Any] | None = None) -> None: ...

class CalculationError(StockValueFinderError): ...
class ExternalAPIError(StockValueFinderError): ...
```

From stockvaluefinder/db/models/risk.py (ORM model pattern):
```python
class RiskScoreDB(Base):
    __tablename__ = "risk_scores"
    score_id: Mapped[str] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid4)
    ticker: Mapped[str] = mapped_column(String(20), ForeignKey("stocks.ticker"), nullable=False, index=True)
```

From stockvaluefinder/models/api.py (Pydantic model pattern):
```python
class ApiResponse(BaseModel, Generic[DataType]):
    success: bool
    data: DataType | None = None
    model_config = {"frozen": True}
```

From stockvaluefinder/models/enums.py (StrEnum pattern):
```python
class RiskLevel(str, Enum):
    LOW = "LOW"
    MEDIUM = "MEDIUM"
```

From alembic/versions/008_add_documents_table.py (migration pattern):
```python
revision: str = "008"
down_revision: Union[str, Sequence[str], None] = "007"

def upgrade() -> None:
    op.create_table("documents", ...)

def downgrade() -> None:
    op.drop_table("documents")
```

From stockvaluefinder/db/base.py:
```python
async_session_maker = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PipelineConfig, PipelineState, StateTransitionError, and Pydantic domain models with tests</name>
  <files>
    stockvaluefinder/pipeline/__init__.py,
    stockvaluefinder/pipeline/config.py,
    stockvaluefinder/pipeline/state.py,
    stockvaluefinder/pipeline/models.py,
    stockvaluefinder/utils/errors.py,
    tests/unit/test_pipeline/__init__.py,
    tests/unit/test_pipeline/test_config.py,
    tests/unit/test_pipeline/test_state.py,
    tests/unit/test_pipeline/test_models.py
  </files>
  <read_first>
    stockvaluefinder/stockvaluefinder/config.py
    stockvaluefinder/stockvaluefinder/utils/errors.py
    stockvaluefinder/stockvaluefinder/models/api.py
    stockvaluefinder/stockvaluefinder/models/enums.py
    .planning/phases/05-pipeline-foundation/05-CONTEXT.md
    .planning/phases/05-pipeline-foundation/05-RESEARCH.md
  </read_first>
  <behavior>
    - Config test: PipelineConfig() creates with default values; max_retries=3, retry_delays=(2.0, 8.0, 30.0), stuck_timeout_minutes=30, reaper_interval_minutes=5, max_concurrent_tasks=5, request_delay_seconds=0.5, job_timeout_seconds=1800, redis_db=0, default_watchlist="CSI300"
    - Config test: PipelineConfig(max_retries=-1) raises ValueError
    - Config test: PipelineConfig(stuck_timeout_minutes=0) raises ValueError
    - Config test: PipelineConfig(retry_delays=(2.0,), max_retries=3) raises ValueError ("retry_delays has 1 entries, but max_retries is 3")
    - Config test: frozen=True -- assigning to any field raises FrozenInstanceError
    - State test: PipelineState has exactly 6 members: PENDING, DOWNLOADING, PARSING, ANALYZING, DONE, FAILED
    - State test: VALID_TRANSITIONS[PENDING] contains DOWNLOADING and FAILED only
    - State test: VALID_TRANSITIONS[DOWNLOADING] contains PARSING and FAILED only
    - State test: VALID_TRANSITIONS[PARSING] contains ANALYZING and FAILED only
    - State test: VALID_TRANSITIONS[ANALYZING] contains DONE and FAILED only
    - State test: VALID_TRANSITIONS[DONE] is empty frozenset
    - State test: VALID_TRANSITIONS[FAILED] is empty frozenset
    - State test: validate_transition(PENDING, DOWNLOADING) completes without exception
    - State test: validate_transition(PENDING, PARSING) raises StateTransitionError
    - State test: validate_transition(DONE, PENDING) raises StateTransitionError
    - State test: StateTransitionError.message contains "Invalid state transition: pending -> parsing"
    - State test: StateTransitionError.details contains {"current": "pending", "target": "parsing"}
    - Model test: PipelineTaskCreate Pydantic model validates required fields (ticker, business_key)
    - Model test: PipelineDocumentCreate Pydantic model validates task_id FK
  </behavior>
  <action>
    Write tests FIRST (TDD RED), then implement:

    1. Create `stockvaluefinder/pipeline/__init__.py` with module docstring and `__all__` exports.

    2. Add `StateTransitionError` to `stockvaluefinder/utils/errors.py` (append after CacheError class). Signature: `class StateTransitionError(StockValueFinderError)` with constructor `__init__(self, current: 'PipelineState', target: 'PipelineState')` that calls `super().__init__(message=f"Invalid state transition: {current.value} -> {target.value}", details={"current": current.value, "target": target.value})`. Note: use string forward reference for PipelineState since it is defined in the pipeline module.

    3. Create `stockvaluefinder/pipeline/config.py` with `PipelineConfig` frozen dataclass per D-12/D-13. Fields (all with defaults): `max_retries: int = 3`, `retry_delays: tuple[float, ...] = (2.0, 8.0, 30.0)`, `stuck_timeout_minutes: int = 30`, `reaper_interval_minutes: int = 5`, `max_concurrent_tasks: int = 5`, `request_delay_seconds: float = 0.5`, `job_timeout_seconds: int = 1800`, `redis_db: int = 0`, `default_watchlist: str = "CSI300"`. `__post_init__` validates: max_retries >= 0, stuck_timeout_minutes >= 1, len(retry_delays) >= max_retries. Raises ValueError with descriptive message. Follow exact pattern from existing `ValuationConfig` in config.py (frozen=True dataclass with UPPER_SNAKE_CASE field names).

    4. Create `stockvaluefinder/pipeline/state.py` with `PipelineState(StrEnum)` per D-14. Values: PENDING="pending", DOWNLOADING="downloading", PARSING="parsing", ANALYZING="analyzing", DONE="done", FAILED="failed". Define `VALID_TRANSITIONS: dict[PipelineState, frozenset[PipelineState]]` with the 6 entries per the state machine spec. Define `validate_transition(current: PipelineState, target: PipelineState) -> None` that raises StateTransitionError if target not in VALID_TRANSITIONS[current]. Import StateTransitionError from stockvaluefinder.utils.errors.

    5. Create `stockvaluefinder/pipeline/models.py` with Pydantic domain models:
       - `PipelineTaskCreate(BaseModel)`: fields ticker: str (with pattern `r"^\d{6}\.(SH|SZ|HK)$"`), business_key: str (with min_length=1), fiscal_year: int (ge=2000, le=2100), report_type: str. Config with json_schema_extra example.
       - `PipelineDocumentCreate(BaseModel)`: fields task_id: str, source_url: str | None = None, source_id: str | None = None, content_hash: str | None = None, file_path: str | None = None, file_size: int | None = None (ge=0). Config with json_schema_extra example.
       - `HealthStatus(BaseModel)`: fields status: str, components: dict[str, str], checked_at: str. model_config = {"frozen": True}.

    6. Create test directory `tests/unit/test_pipeline/__init__.py` (empty).

    7. Create `tests/unit/test_pipeline/test_config.py` testing all PipelineConfig validation (default creation, invalid max_retries, invalid stuck_timeout, retry_delays length mismatch, frozen check).

    8. Create `tests/unit/test_pipeline/test_state.py` testing all state transitions (valid ones pass, invalid ones raise StateTransitionError with correct message/details, terminal states have empty frozenset).

    9. Create `tests/unit/test_pipeline/test_models.py` testing Pydantic model validation (valid data creates, invalid ticker pattern fails, missing required fields fail).
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_pipeline/test_config.py tests/unit/test_pipeline/test_state.py tests/unit/test_pipeline/test_models.py -v -x 2>&1 | tail -40</automated>
  </verify>
  <done>
    - PipelineConfig frozen dataclass with __post_init__ validation passes all tests
    - PipelineState StrEnum has 6 members with correct VALID_TRANSITIONS
    - validate_transition raises StateTransitionError for invalid transitions
    - StateTransitionError extends StockValueFinderError with correct message format
    - Pydantic models PipelineTaskCreate, PipelineDocumentCreate, HealthStatus validate correctly
    - All tests pass: `uv run pytest tests/unit/test_pipeline/ -v`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ORM models and Alembic migration 009 for pipeline_tasks and pipeline_documents</name>
  <files>
    stockvaluefinder/db/models/pipeline_task.py,
    stockvaluefinder/db/models/pipeline_document.py,
    stockvaluefinder/db/models/__init__.py,
    alembic/versions/009_pipeline_tables.py
  </files>
  <read_first>
    stockvaluefinder/stockvaluefinder/db/models/risk.py
    stockvaluefinder/stockvaluefinder/db/models/stock.py
    stockvaluefinder/alembic/versions/008_add_documents_table.py
    stockvaluefinder/stockvaluefinder/db/base.py
    stockvaluefinder/stockvaluefinder/db/models/__init__.py
    .planning/phases/05-pipeline-foundation/05-CONTEXT.md
  </read_first>
  <behavior>
    - ORM model PipelineTaskDB has __tablename__ = "pipeline_tasks"
    - ORM model PipelineTaskDB has columns: task_id (UUID PK), ticker (String(20) FK to stocks.ticker, indexed), business_key (String(255), unique), state (String(20), default="pending", indexed), current_stage (String(50), nullable), retry_count (Integer, default=0), max_retries (Integer, default=3), error_message (Text, nullable), result_summary (JSONB, nullable), created_at (DateTime(timezone=True)), updated_at (DateTime(timezone=True) with onupdate)
    - ORM model PipelineDocumentDB has __tablename__ = "pipeline_documents"
    - ORM model PipelineDocumentDB has columns: document_id (UUID PK), task_id (UUID FK to pipeline_tasks.task_id, indexed), source_url (Text, nullable), source_id (String(255), nullable, indexed), content_hash (String(64), nullable, indexed), file_path (Text, nullable), file_size (BigInteger, nullable), downloaded_at (DateTime(timezone=True), nullable)
    - Migration 009 creates both tables with correct column types, indexes, unique constraints, and foreign keys
    - Migration 009 downgrade drops pipeline_documents first (FK dependency), then pipeline_tasks
  </behavior>
  <action>
    Create ORM models and Alembic migration matching the exact schema from CONTEXT.md decisions D-08, D-09, D-10, D-11:

    1. Create `stockvaluefinder/db/models/pipeline_task.py` defining `PipelineTaskDB(Base)` ORM model. Follow the exact pattern from `stockvaluefinder/db/models/risk.py` (Mapped[] columns, UUID(as_uuid=True) for PK, ForeignKey for ticker). Columns per D-09: task_id (UUID PK, default=uuid4), ticker (String(20), ForeignKey("stocks.ticker"), nullable=False, index=True), business_key (String(255), nullable=False, unique=True), state (String(20), nullable=False, default="pending", index=True), current_stage (String(50), nullable=True), retry_count (Integer, nullable=False, default=0), max_retries (Integer, nullable=False, default=3), error_message (Text, nullable=True), result_summary (JSONB, nullable=True), created_at (DateTime(timezone=True), nullable=False, default=lambda: datetime.now(timezone.utc)), updated_at (DateTime(timezone=True), nullable=False, default=lambda: datetime.now(timezone.utc), onupdate=lambda: datetime.now(timezone.utc)). Add `__repr__` returning task_id, ticker, state.

    2. Create `stockvaluefinder/db/models/pipeline_document.py` defining `PipelineDocumentDB(Base)` ORM model. Columns per D-10: document_id (UUID PK, default=uuid4), task_id (UUID, ForeignKey("pipeline_tasks.task_id"), nullable=False, index=True), source_url (Text, nullable=True), source_id (String(255), nullable=True, index=True), content_hash (String(64), nullable=True, index=True), file_path (Text, nullable=True), file_size (BigInteger, nullable=True), downloaded_at (DateTime(timezone=True), nullable=True). Add `__repr__` returning document_id, task_id.

    3. Create `alembic/versions/009_pipeline_tables.py` migration. revision="009", down_revision="008". Upgrade creates pipeline_tasks first (no FK to pipeline_documents), then pipeline_documents. Use `sa.Column()` with exact types matching ORM models. Add server_default for state="pending", retry_count=0, max_retries=3. Downgrade drops pipeline_documents first (has FK to pipeline_tasks), then pipeline_tasks. Follow exact structure from `008_add_documents_table.py`.

    4. Update `stockvaluefinder/db/models/__init__.py` to import the two new models so Alembic autogenerate and metadata discovery can find them. Add: `from stockvaluefinder.db.models.pipeline_task import PipelineTaskDB` and `from stockvaluefinder.db.models.pipeline_document import PipelineDocumentDB`.
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run python -c "from stockvaluefinder.db.models.pipeline_task import PipelineTaskDB; from stockvaluefinder.db.models.pipeline_document import PipelineDocumentDB; print(f'PipelineTaskDB: {PipelineTaskDB.__tablename__}'); print(f'PipelineDocumentDB: {PipelineDocumentDB.__tablename__}'); print('ORM models import OK')" && echo "---" && uv run python -c "from alembic.config import Config; from alembic import command; cfg = Config('alembic.ini'); command.upgrade(cfg, '009')" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - PipelineTaskDB ORM model imports cleanly with all columns per D-09
    - PipelineDocumentDB ORM model imports cleanly with all columns per D-10
    - Migration 009 revision="009", down_revision="008" creates both tables
    - Migration applies successfully: `alembic upgrade head`
    - Downgrade works: `alembic downgrade 008` then `alembic upgrade head`
    - Both models registered in db/models/__init__.py
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API input -> Pydantic models | Untrusted ticker strings, business_key values must be validated |
| Migration -> PostgreSQL | DDL changes to production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S | Health endpoint | accept | No auth in this phase; single-user system |
| T-05-02 | T | Pydantic model input | mitigate | PipelineTaskCreate validates ticker with regex pattern `r"^\d{6}\.(SH|SZ|HK)$"` and business_key min_length=1 |
| T-05-03 | T | State machine | mitigate | PipelineState StrEnum constrains valid state values; validate_transition rejects all invalid transitions |
| T-05-04 | D | Worker job queue | accept | Rate limiting deferred to API layer (future phase); arq has built-in job limits |
</threat_model>

<verification>
1. All unit tests pass: `cd stockvaluefinder && uv run pytest tests/unit/test_pipeline/ -v`
2. ORM models import without error: `uv run python -c "from stockvaluefinder.db.models.pipeline_task import PipelineTaskDB; print('OK')"`
3. Migration applies: `cd stockvaluefinder && uv run alembic upgrade head` (requires PostgreSQL running)
4. No lint errors: `cd stockvaluefinder && uv run ruff check stockvaluefinder/pipeline/ stockvaluefinder/db/models/pipeline_*.py`
</verification>

<success_criteria>
- PipelineConfig creates with defaults and rejects invalid values
- PipelineState has 6 states, VALID_TRANSITIONS defines linear progression
- StateTransitionError raised for all 16 invalid transition combinations (each of 6 states x 3-5 invalid targets)
- PipelineTaskDB and PipelineDocumentDB ORM models have all columns per D-09/D-10
- Migration 009 creates both tables with correct constraints
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-foundation/05-01-SUMMARY.md`
</output>
