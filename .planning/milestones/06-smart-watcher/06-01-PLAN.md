---
phase: 06-smart-watcher
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - stockvaluefinder/stockvaluefinder/pipeline/config.py
  - stockvaluefinder/stockvaluefinder/pipeline/models.py
  - stockvaluefinder/stockvaluefinder/db/models/watchlist.py
  - stockvaluefinder/stockvaluefinder/db/models/watcher_state.py
  - stockvaluefinder/stockvaluefinder/db/models/pending_disclosure.py
  - stockvaluefinder/stockvaluefinder/db/models/__init__.py
  - stockvaluefinder/alembic/versions/010_watcher_tables.py
  - stockvaluefinder/stockvaluefinder/pipeline/__init__.py
  - stockvaluefinder/tests/unit/test_pipeline/test_config.py
  - stockvaluefinder/tests/unit/test_pipeline/test_models.py
  - stockvaluefinder/tests/unit/test_pipeline/test_orm_models.py
autonomous: true
requirements: [WATCH-03, WATCH-04]

must_haves:
  truths:
    - "PipelineConfig has high_season_months, high_season_cron, off_season_cron fields with frozen dataclass validation"
    - "WatchlistItemCreate, WatcherStateUpdate, PendingDisclosureCreate Pydantic models validate input correctly"
    - "WatchlistDB, WatcherStateDB, PendingDisclosureDB ORM models map to correct PostgreSQL tables"
    - "Alembic migration 010 creates watchlist, watcher_state, and pending_disclosures tables with correct columns and indexes"
  artifacts:
    - path: "stockvaluefinder/stockvaluefinder/pipeline/config.py"
      provides: "PipelineConfig with high_season_months, high_season_cron, off_season_cron fields"
      contains: "high_season_months"
    - path: "stockvaluefinder/stockvaluefinder/db/models/watchlist.py"
      provides: "WatchlistDB ORM model"
      contains: "class WatchlistDB"
    - path: "stockvaluefinder/stockvaluefinder/db/models/watcher_state.py"
      provides: "WatcherStateDB ORM model"
      contains: "class WatcherStateDB"
    - path: "stockvaluefinder/stockvaluefinder/db/models/pending_disclosure.py"
      provides: "PendingDisclosureDB ORM model"
      contains: "class PendingDisclosureDB"
    - path: "stockvaluefinder/alembic/versions/010_watcher_tables.py"
      provides: "Migration creating watchlist, watcher_state, pending_disclosures tables"
      contains: "revision"
  key_links:
    - from: "stockvaluefinder/stockvaluefinder/db/models/__init__.py"
      to: "stockvaluefinder/stockvaluefinder/db/models/watchlist.py"
      via: "import WatchlistDB"
      pattern: "from stockvaluefinder.db.models.watchlist import WatchlistDB"
    - from: "stockvaluefinder/alembic/versions/010_watcher_tables.py"
      to: "stockvaluefinder/alembic/versions/009_pipeline_tables.py"
      via: "down_revision = '009'"
      pattern: "down_revision.*009"
---

<objective>
Extend PipelineConfig with season-aware polling fields and create all new ORM models (watchlist, watcher_state, pending_disclosures) plus Alembic migration 010.

Purpose: This plan establishes the data layer that Plans 06-02 (watcher service) and 06-03 (watchlist API) depend on. All types, tables, and migration are created first so downstream plans can reference them without guessing contracts.

Output: Updated PipelineConfig, 3 new Pydantic models, 3 new ORM models, Alembic migration 010, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-smart-watcher/06-CONTEXT.md
@.planning/phases/06-smart-watcher/06-RESEARCH.md
@.planning/phases/05-pipeline-foundation/05-01-SUMMARY.md

<interfaces>
<!-- Existing types that this plan extends -->

From stockvaluefinder/stockvaluefinder/pipeline/config.py:
```python
@dataclass(frozen=True)
class PipelineConfig:
    max_retries: int = 3
    retry_delays: tuple[float, ...] = (2.0, 8.0, 30.0)
    stuck_timeout_minutes: int = 30
    reaper_interval_minutes: int = 5
    max_concurrent_tasks: int = 5
    request_delay_seconds: float = 0.5
    job_timeout_seconds: int = 1800
    redis_db: int = 0
    default_watchlist: str = "CSI300"

    def __post_init__(self) -> None: ...
```

From stockvaluefinder/stockvaluefinder/pipeline/models.py:
```python
class PipelineTaskCreate(BaseModel):
    ticker: str = Field(..., pattern=r"^\d{4,6}\.(SH|SZ|HK)$")
    business_key: str = Field(..., min_length=1)
    fiscal_year: int = Field(..., ge=2000, le=2100)
    report_type: str = Field(...)
```

From stockvaluefinder/stockvaluefinder/db/base.py:
```python
class Base(DeclarativeBase): ...
```

From stockvaluefinder/stockvaluefinder/db/models/__init__.py:
```python
# Currently imports: StockDB, RateDataDB, FinancialReportDB, RiskScoreDB,
# DividendDataDB, YieldGapDB, ValuationResultDB, DocumentDB,
# PipelineTaskDB, PipelineDocumentDB
```

From stockvaluefinder/alembic/versions/009_pipeline_tables.py:
```python
revision: str = "009"
down_revision: Union[str, Sequence[str], None] = "008"
# Uses sa.String, sa.ForeignKey, sa.DateTime(timezone=True), postgresql.JSONB
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PipelineConfig and add new Pydantic models</name>
  <files>
    stockvaluefinder/stockvaluefinder/pipeline/config.py,
    stockvaluefinder/stockvaluefinder/pipeline/models.py,
    stockvaluefinder/stockvaluefinder/pipeline/__init__.py,
    stockvaluefinder/tests/unit/test_pipeline/test_config.py,
    stockvaluefinder/tests/unit/test_pipeline/test_models.py
  </files>
  <behavior>
    - Test 1: PipelineConfig accepts high_season_months as frozenset of ints (default {1, 2, 3, 4}) per D-07
    - Test 2: PipelineConfig accepts high_season_cron as string (default "0 9 * * *") per D-08
    - Test 3: PipelineConfig accepts off_season_cron as string (default "0 9 * * 1") per D-08
    - Test 4: PipelineConfig rejects high_season_months with values outside 1-12
    - Test 5: PipelineConfig rejects empty high_season_months
    - Test 6: PipelineConfig rejects empty cron strings
    - Test 7: WatchlistItemCreate Pydantic model validates ticker with regex ^\d{4,6}\.(SH|SZ|HK)$
    - Test 8: WatchlistItemCreate accepts name field (required, max 100 chars)
    - Test 9: WatcherStateUpdate Pydantic model has last_poll_time, last_akshare_success, last_cninfo_fallback, polls_count, errors_count fields
    - Test 10: PendingDisclosureCreate has ticker, stock_name, report_type, fiscal_year, disclosure_date, source, source_raw fields with appropriate validation
    - Test 11: Existing PipelineConfig fields (max_retries, retry_delays, etc.) still validate correctly after adding new fields
  </behavior>
  <action>
    Per D-07, D-08, D-09: Add three new fields to the existing PipelineConfig frozen dataclass:
    - `high_season_months: frozenset[int] = frozenset({1, 2, 3, 4})` -- months considered high reporting season (Jan-Apr)
    - `high_season_cron: str = "0 9 * * *"` -- cron expression for high season (daily 09:00)
    - `off_season_cron: str = "0 9 * * 1"` -- cron expression for off season (weekly Monday 09:00)
    Add __post_init__ validation: high_season_months must be non-empty subset of {1..12}, cron strings must be non-empty.

    Add to pipeline/models.py:
    - `WatchlistItemCreate(BaseModel)`: ticker (regex ^\d{4,6}\.(SH|SZ|HK)$), name (str, max_length=100)
    - `WatchlistItemResponse(BaseModel)`: ticker, name, added_at (datetime), is_active (bool)
    - `WatcherStateUpdate(BaseModel)`: last_poll_time (datetime | None), last_akshare_success (bool), last_cninfo_fallback (bool), polls_count (int, ge=0), errors_count (int, ge=0)
    - `PendingDisclosureCreate(BaseModel)`: ticker (regex), stock_name (str | None), report_type (str, must be one of "annual", "semi_annual", "q1", "q3"), fiscal_year (int, ge=2000, le=2100), disclosure_date (date | None), first_appointment (date | None), source (Literal["akshare", "cninfo"]), source_raw (dict[str, Any] | None)
    All models follow existing Pydantic BaseModel pattern with json_schema_extra examples.

    Update pipeline/__init__.py to export new models.
  </action>
  <verify>
    <automated>cd stockvaluefinder && uv run pytest tests/unit/test_pipeline/test_config.py tests/unit/test_pipeline/test_models.py -x -q</automated>
  </verify>
  <done>
    PipelineConfig has high_season_months, high_season_cron, off_season_cron with validation.
    WatchlistItemCreate, WatchlistItemResponse, WatcherStateUpdate, PendingDisclosureCreate Pydantic models exist with correct field types and validation.
    All new types exported from pipeline/__init__.py.
    All existing PipelineConfig tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ORM models and Alembic migration 010</name>
  <files>
    stockvaluefinder/stockvaluefinder/db/models/watchlist.py,
    stockvaluefinder/stockvaluefinder/db/models/watcher_state.py,
    stockvaluefinder/stockvaluefinder/db/models/pending_disclosure.py,
    stockvaluefinder/stockvaluefinder/db/models/__init__.py,
    stockvaluefinder/alembic/versions/010_watcher_tables.py,
    stockvaluefinder/tests/unit/test_pipeline/test_orm_models.py
  </files>
  <behavior>
    - Test 1: WatchlistDB has columns: ticker (String 20, PK), name (String 100), added_at (DateTime TZ), is_active (Boolean, default True)
    - Test 2: WatchlistDB __repr__ works
    - Test 3: WatcherStateDB has columns: watcher_id (String 50, PK, default "default"), last_poll_time (DateTime TZ, nullable), last_akshare_success (Boolean, default False), last_cninfo_fallback (Boolean, default False), polls_count (Integer, default 0), errors_count (Integer, default 0), updated_at (DateTime TZ)
    - Test 4: WatcherStateDB __repr__ works
    - Test 5: PendingDisclosureDB has columns: disclosure_id (UUID, PK), poll_id (UUID, not null), ticker (String 20, not null), stock_name (String 100, nullable), report_type (String 20, not null), fiscal_year (Integer, not null), disclosure_date (Date, nullable), first_appointment (Date, nullable), source (String 20, not null), source_raw (JSONB, nullable), processed (Boolean, default False), created_at (DateTime TZ), processed_at (DateTime TZ, nullable)
    - Test 6: PendingDisclosureDB __repr__ works
    - Test 7: db/models/__init__.py exports WatchlistDB, WatcherStateDB, PendingDisclosureDB
    - Test 8: Alembic migration 010 has down_revision = "009"
    - Test 9: Migration 010 upgrade creates all 3 tables, downgrade drops them in correct order
  </behavior>
  <action>
    Per D-13, D-16, D-17, and D-11 (staging table): Create three new ORM models following the exact pattern from PipelineTaskDB.

    1. `stockvaluefinder/stockvaluefinder/db/models/watchlist.py`:
       - `WatchlistDB(Base)` with __tablename__ = "watchlist"
       - ticker: String(20), primary_key=True (per D-13: PK)
       - name: String(100), nullable=False
       - added_at: DateTime(timezone=True), default=now(UTC)
       - is_active: Boolean, default=True

    2. `stockvaluefinder/stockvaluefinder/db/models/watcher_state.py`:
       - `WatcherStateDB(Base)` with __tablename__ = "watcher_state"
       - watcher_id: String(50), primary_key=True, default="default"
       - last_poll_time: DateTime(timezone=True), nullable=True
       - last_akshare_success: Boolean, default=False
       - last_cninfo_fallback: Boolean, default=False
       - polls_count: Integer, default=0
       - errors_count: Integer, default=0
       - updated_at: DateTime(timezone=True), default=now(UTC), onupdate=now(UTC)

    3. `stockvaluefinder/stockvaluefinder/db/models/pending_disclosure.py`:
       - `PendingDisclosureDB(Base)` with __tablename__ = "pending_disclosures"
       - disclosure_id: UUID(as_uuid=True), primary_key=True, default=uuid4
       - poll_id: UUID(as_uuid=True), nullable=False, index=True
       - ticker: String(20), nullable=False
       - stock_name: String(100), nullable=True
       - report_type: String(20), nullable=False
       - fiscal_year: Integer, nullable=False
       - disclosure_date: Date, nullable=True
       - first_appointment: Date, nullable=True
       - source: String(20), nullable=False
       - source_raw: JSONB, nullable=True
       - processed: Boolean, default=False, index=True
       - created_at: DateTime(timezone=True), default=now(UTC)
       - processed_at: DateTime(timezone=True), nullable=True

    4. Update `db/models/__init__.py` to import and export all 3 new models.

    5. Create `stockvaluefinder/alembic/versions/010_watcher_tables.py`:
       - revision = "010", down_revision = "009"
       - upgrade: create watchlist, watcher_state, pending_disclosures in order
       - Add index idx_pending_disclosures_poll on pending_disclosures(poll_id)
       - downgrade: drop in reverse order (pending_disclosures first, then watcher_state, then watchlist)
       Follow exact Alembic pattern from 009_pipeline_tables.py.
  </action>
  <verify>
    <automated>cd stockvaluefinder && uv run pytest tests/unit/test_pipeline/test_orm_models.py -x -q</automated>
  </verify>
  <done>
    WatchlistDB, WatcherStateDB, PendingDisclosureDB ORM models exist with correct columns and types.
    db/models/__init__.py exports all 3 new models.
    Alembic migration 010 creates watchlist, watcher_state, pending_disclosures with down_revision="009".
    All existing ORM model tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config validation | PipelineConfig.__post_init__ validates season months and cron strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | PipelineConfig fields | accept | Frozen dataclass prevents mutation; values come from code, not user input |
| T-06-02 | Tampering | Pydantic model validators | mitigate | ticker regex validation on WatchlistItemCreate and PendingDisclosureCreate prevents injection |

</threat_model>

<verification>
1. All pipeline config tests pass with new fields: `cd stockvaluefinder && uv run pytest tests/unit/test_pipeline/test_config.py -x`
2. All Pydantic model tests pass: `cd stockvaluefinder && uv run pytest tests/unit/test_pipeline/test_models.py -x`
3. All ORM model tests pass: `cd stockvaluefinder && uv run pytest tests/unit/test_pipeline/test_orm_models.py -x`
4. Migration file exists and has correct revision chain: `grep -c 'down_revision.*009' stockvaluefinder/alembic/versions/010_watcher_tables.py`
5. All existing pipeline tests still pass: `cd stockvaluefinder && uv run pytest tests/unit/test_pipeline/ -x -q`
</verification>

<success_criteria>
- PipelineConfig has high_season_months (frozenset[int]), high_season_cron (str), off_season_cron (str) with __post_init__ validation per D-07, D-09
- WatchlistItemCreate, WatchlistItemResponse, WatcherStateUpdate, PendingDisclosureCreate Pydantic models exist with correct field types and validation
- WatchlistDB, WatcherStateDB, PendingDisclosureDB ORM models match table schemas from D-13, D-16, D-11
- Alembic migration 010 creates all 3 tables with down_revision="009" per D-17
- All new and existing pipeline tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-smart-watcher/06-01-SUMMARY.md`
</output>
