---
phase: 11-policy-resonance-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stockvaluefinder/stockvaluefinder/config.py
  - stockvaluefinder/stockvaluefinder/models/policy.py
  - stockvaluefinder/stockvaluefinder/models/enums.py
  - stockvaluefinder/stockvaluefinder/services/policy_service.py
  - stockvaluefinder/tests/unit/test_services/test_policy_service.py
autonomous: true
requirements:
  - POL-03
  - POL-04

must_haves:
  truths:
    - "Resonance score returns 0-100 from weighted cosine similarity and LLM confidence"
    - "DCF terminal growth adjustment returns correct tier: Strongly Supportive (+1.5%), Supportive (+1.0%), Neutral (0%)"
    - "Adjusted terminal growth is clamped within ValuationConfig.MAX_TERMINAL_GROWTH = 10%"
    - "Zero relevant matches produces score 0.0, not NaN"
  artifacts:
    - path: "stockvaluefinder/stockvaluefinder/models/policy.py"
      provides: "Pydantic models for policy domain"
      exports: ["PolicyMatch", "ResonanceTier", "DCFAdjustment", "ResonanceResult", "PolicyMetadata"]
    - path: "stockvaluefinder/stockvaluefinder/services/policy_service.py"
      provides: "Pure calculation functions for resonance scoring"
      exports: ["calculate_resonance_score", "calculate_dcf_adjustment", "classify_resonance_tier", "verify_match_relevance"]
    - path: "stockvaluefinder/stockvaluefinder/config.py"
      provides: "PolicyResonanceConfig frozen dataclass"
      contains: "PolicyResonanceConfig"
    - path: "stockvaluefinder/tests/unit/test_services/test_policy_service.py"
      provides: "Unit tests for all pure functions"
      min_lines: 80
  key_links:
    - from: "stockvaluefinder/stockvaluefinder/services/policy_service.py"
      to: "stockvaluefinder/stockvaluefinder/config.py"
      via: "import PolicyResonanceConfig"
      pattern: "from stockvaluefinder.config import.*PolicyResonanceConfig"
    - from: "stockvaluefinder/stockvaluefinder/services/policy_service.py"
      to: "stockvaluefinder/stockvaluefinder/models/policy.py"
      via: "import domain models"
      pattern: "from stockvaluefinder.models.policy import"
---

<objective>
Create the pure calculation engine for the Policy Resonance system.

Purpose: Establish all stateless pure functions and domain models so that downstream plans (data layer, API wiring) can build against defined contracts. This plan covers the scoring formula (D-04), DCF adjustment tiers (D-07/D-08), match verification structure (D-05), and resonance threshold (D-06).

Output: policy_service.py with tested pure functions, policy.py domain models, PolicyResonanceConfig in config.py, and comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-policy-resonance-engine/11-CONTEXT.md
@.planning/phases/11-policy-resonance-engine/11-RESEARCH.md

<interfaces>
<!-- Existing config pattern the executor must follow -->

From stockvaluefinder/stockvaluefinder/config.py:
```python
@dataclass(frozen=True)
class ValuationConfig:
    MIN_TERMINAL_GROWTH: float = -0.05
    MAX_TERMINAL_GROWTH: float = 0.10

@dataclass(frozen=True)
class ROICConfig:
    MOAT_TREND_THRESHOLD: float = 0.005

@dataclass(frozen=True)
class CapitalAllocationConfig:
    CAPEX_GROWTH_THRESHOLD: float = 0.20

@dataclass(frozen=True)
class AppConfig:
    valuation: ValuationConfig
    risk: RiskConfig
    yield_config: YieldConfig
    external_data: ExternalDataConfig
    database: DatabaseConfig
    roic: ROICConfig
    capital_allocation: CapitalAllocationConfig
    # Must add: policy_resonance: PolicyResonanceConfig

settings = AppConfig.get_instance()
```

From stockvaluefinder/stockvaluefinder/models/enums.py:
```python
from enum import StrEnum
class RiskLevel(StrEnum): ...
class ValuationLevel(StrEnum): ...
class Market(StrEnum): ...
class YieldRecommendation(StrEnum): ...
# Must add: class ResonanceTier(StrEnum)
```

From stockvaluefinder/stockvaluefinder/services/narrative_service.py:
```python
# _parse_llm_response pattern for LLM JSON parsing:
# 1. Try extracting from markdown code blocks
# 2. Try parsing whole content as JSON
# 3. Try finding JSON object in mixed text
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain models, config, and enums for policy resonance</name>
  <files>
    stockvaluefinder/stockvaluefinder/models/policy.py,
    stockvaluefinder/stockvaluefinder/models/enums.py,
    stockvaluefinder/stockvaluefinder/config.py
  </files>
  <read_first>
    stockvaluefinder/stockvaluefinder/models/enums.py,
    stockvaluefinder/stockvaluefinder/config.py,
    stockvaluefinder/stockvaluefinder/models/document.py,
    stockvaluefinder/stockvaluefinder/models/capital_allocation.py
  </read_first>
  <acceptance_criteria>
    - `grep "class ResonanceTier" stockvaluefinder/stockvaluefinder/models/enums.py` returns match with STRONGLY_SUPPORTIVE, SUPPORTIVE, NEUTRAL values
    - `grep "class PolicyMatch" stockvaluefinder/stockvaluefinder/models/policy.py` returns match
    - `grep "class ResonanceResult" stockvaluefinder/stockvaluefinder/models/policy.py` returns match
    - `grep "class DCFAdjustment" stockvaluefinder/stockvaluefinder/models/policy.py` returns match
    - `grep "class PolicyMetadata" stockvaluefinder/stockvaluefinder/models/policy.py` returns match
    - `grep "class PolicyResonanceConfig" stockvaluefinder/stockvaluefinder/config.py` returns match with frozen=True
    - `grep "policy_resonance" stockvaluefinder/stockvaluefinder/config.py` returns match showing PolicyResonanceConfig added to AppConfig and global instance
    - `grep "policy_resonance_config" stockvaluefinder/stockvaluefinder/config.py` returns match for global instance
    - All Pydantic models use `model_config = {"frozen": True}` pattern where appropriate
    - PolicyResonanceConfig contains: MATCH_LIMIT=5, COSINE_WEIGHT=0.60, LLM_WEIGHT=0.40, RESONANCE_THRESHOLD=40.0, STRONG_TIER_THRESHOLD=80.0, STRONG_ADJUSTMENT=0.015, MODERATE_ADJUSTMENT=0.01, MAX_ADJUSTMENT_CAP=0.015, VECTOR_SEARCH_THRESHOLD=0.5, BUSINESS_DESC_CACHE_TTL=86400
  </acceptance_criteria>
  <behavior>
    - PolicyMatch has fields: chunk_content (str), chunk_id (str), document_id (str), score (float), relevant (bool), confidence (float), reason (str)
    - PolicyMetadata has fields: title (str), policy_type (str), issuing_body (str), effective_date (str | None), industry_tags (list[str])
    - DCFAdjustment has fields: tier (ResonanceTier), adjustment_pct (float), adjusted_terminal_growth (float), original_terminal_growth (float)
    - ResonanceResult has fields: ticker (str), resonance_score (float), tier (ResonanceTier), matched_policies (list[PolicyMatch]), dcf_adjustment (DCFAdjustment), policy_count (int), analyzed_at (datetime)
    - PolicyUploadResponse has fields: document_id (str), title (str), chunk_count (int), page_count (int), status (str)
    - ResonanceTier enum: STRONGLY_SUPPORTIVE, SUPPORTIVE, NEUTRAL
  </behavior>
  <action>
    1. Create `stockvaluefinder/stockvaluefinder/models/policy.py` with Pydantic models:
       - `PolicyMatch(BaseModel)`: represents a single matched policy chunk with LLM verification results. Fields: chunk_content (str), chunk_id (str), document_id (str), score (float, ge=0, le=1), relevant (bool), confidence (float, ge=0, le=1), reason (str). Frozen config.
       - `PolicyMetadata(BaseModel)`: metadata extracted from policy doc. Fields: title (str), policy_type (str), issuing_body (str), effective_date (str | None), industry_tags (list[str]). Frozen config.
       - `DCFAdjustment(BaseModel)`: DCF terminal growth adjustment result. Fields: tier (ResonanceTier), adjustment_pct (float), adjusted_terminal_growth (float), original_terminal_growth (float). Frozen config.
       - `ResonanceResult(BaseModel)`: full resonance analysis result. Fields: ticker (str), resonance_score (float, ge=0, le=100), tier (ResonanceTier), matched_policies (list[PolicyMatch]), dcf_adjustment (DCFAdjustment), policy_count (int, ge=0), analyzed_at (datetime). Frozen config.
       - `PolicyUploadResponse(BaseModel)`: response after policy upload. Fields: document_id (str), title (str), chunk_count (int, ge=0), page_count (int, ge=1), status (str). Frozen config.
       - `ResonanceRequest(BaseModel)`: request for resonance analysis. Fields: ticker (str, pattern r"^\d{6}\.(SH|SZ|HK)$"), terminal_growth (float = 0.025, description="Base terminal growth rate for DCF adjustment calculation"). Include json_schema_extra example.
       - `PolicyMetadataExtraction(BaseModel)`: LLM-extracted metadata. Fields: title (str), policy_type (str, description="industry/fiscal/monetary/trade"), issuing_body (str), effective_date (str | None), industry_tags (list[str]).

    2. Add `ResonanceTier(StrEnum)` to `stockvaluefinder/stockvaluefinder/models/enums.py` with values: STRONGLY_SUPPORTIVE = "strongly_supportive", SUPPORTIVE = "supportive", NEUTRAL = "neutral".

    3. Add `PolicyResonanceConfig` frozen dataclass to `stockvaluefinder/stockvaluefinder/config.py` (after CapitalAllocationConfig, before RAGConfig):
       - MATCH_LIMIT: int = 5 (D-02)
       - COSINE_WEIGHT: float = 0.60 (D-04)
       - LLM_WEIGHT: float = 0.40 (D-04)
       - RESONANCE_THRESHOLD: float = 40.0 (D-06)
       - STRONG_TIER_THRESHOLD: float = 80.0 (D-07)
       - STRONG_ADJUSTMENT: float = 0.015 (D-07, +1.5%)
       - MODERATE_ADJUSTMENT: float = 0.01 (D-07, +1.0%)
       - NEUTRAL_ADJUSTMENT: float = 0.0 (D-07)
       - MAX_ADJUSTMENT_CAP: float = 0.015 (D-08)
       - VECTOR_SEARCH_THRESHOLD: float = 0.5
       - BUSINESS_DESC_CACHE_TTL: int = 86400 (24h)

    4. Add `policy_resonance: PolicyResonanceConfig` field to AppConfig dataclass, add `PolicyResonanceConfig()` to `get_instance()`, create global `policy_resonance_config = PolicyResonanceConfig()`, add to `__all__`.
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run python -c "from stockvaluefinder.models.policy import PolicyMatch, ResonanceResult, DCFAdjustment, PolicyMetadata, PolicyUploadResponse, ResonanceRequest; from stockvaluefinder.models.enums import ResonanceTier; from stockvaluefinder.config import PolicyResonanceConfig, policy_resonance_config; assert policy_resonance_config.MATCH_LIMIT == 5; assert policy_resonance_config.COSINE_WEIGHT == 0.6; print('All imports OK')"</automated>
  </verify>
  <done>
    - policy.py created with all Pydantic models (PolicyMatch, PolicyMetadata, DCFAdjustment, ResonanceResult, PolicyUploadResponse, ResonanceRequest, PolicyMetadataExtraction)
    - ResonanceTier enum added to enums.py with STRONGLY_SUPPORTIVE, SUPPORTIVE, NEUTRAL
    - PolicyResonanceConfig frozen dataclass added to config.py with all D-04 through D-08 constants
    - AppConfig updated with policy_resonance field
    - All imports succeed without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Policy service pure functions with TDD tests</name>
  <files>
    stockvaluefinder/stockvaluefinder/services/policy_service.py,
    stockvaluefinder/tests/unit/test_services/test_policy_service.py
  </files>
  <read_first>
    stockvaluefinder/stockvaluefinder/services/capex_service.py,
    stockvaluefinder/stockvaluefinder/services/roic_service.py,
    stockvaluefinder/stockvaluefinder/config.py,
    stockvaluefinder/stockvaluefinder/models/policy.py,
    stockvaluefinder/stockvaluefinder/services/narrative_service.py
  </read_first>
  <acceptance_criteria>
    - `grep "def calculate_resonance_score" stockvaluefinder/stockvaluefinder/services/policy_service.py` returns match
    - `grep "def calculate_dcf_adjustment" stockvaluefinder/stockvaluefinder/services/policy_service.py` returns match
    - `grep "def classify_resonance_tier" stockvaluefinder/stockvaluefinder/services/policy_service.py` returns match
    - `grep "def parse_llm_verification" stockvaluefinder/stockvaluefinder/services/policy_service.py` returns match
    - `grep "def parse_metadata_extraction" stockvaluefinder/stockvaluefinder/services/policy_service.py` returns match
    - `uv run pytest tests/unit/test_services/test_policy_service.py -x` passes all tests (from stockvaluefinder/ directory)
    - Test file covers: resonance score with relevant matches, resonance score with zero relevant matches returns 0.0, DCF adjustment for each tier (>=80, 40-79, <40), DCF adjustment clamped at MAX_TERMINAL_GROWTH, tier classification, LLM verification JSON parsing, metadata extraction JSON parsing
  </acceptance_criteria>
  <behavior>
    - Test calculate_resonance_score with 2 relevant matches (score 0.85+0.75, confidence 0.9+0.8) -> 81.0
    - Test calculate_resonance_score with 0 relevant matches -> 0.0
    - Test calculate_resonance_score with mixed relevant/irrelevant matches -> only relevant contribute
    - Test calculate_dcf_adjustment with score 85.0 -> STRONGLY_SUPPORTIVE, adjustment 0.015
    - Test calculate_dcf_adjustment with score 60.0 -> SUPPORTIVE, adjustment 0.01
    - Test calculate_dcf_adjustment with score 30.0 -> NEUTRAL, adjustment 0.0
    - Test calculate_dcf_adjustment clamps at MAX_TERMINAL_GROWTH=0.10 when original=0.095 and adjustment=0.015
    - Test classify_resonance_tier maps scores correctly to ResonanceTier values
    - Test parse_llm_verification with valid JSON -> dict with relevant/confidence/reason
    - Test parse_llm_verification with code-block-wrapped JSON -> dict
    - Test parse_llm_verification with invalid input -> None
    - Test parse_metadata_extraction with valid JSON -> dict with title/policy_type/issuing_body/effective_date/industry_tags
  </behavior>
  <action>
    1. Create test file `tests/unit/test_services/test_policy_service.py` FIRST (TDD RED phase). Write tests for:

       a. `test_calculate_resonance_score_relevant_matches`: Pass list of 3 dicts with score, relevant=True, confidence values. Assert result matches D-04 formula: `0.6 * (avg_cosine * 100) + 0.4 * (avg_confidence * 100)`.
       b. `test_calculate_resonance_score_no_relevant_matches`: Pass list where all relevant=False. Assert result is exactly 0.0 (not NaN).
       c. `test_calculate_resonance_score_empty_matches`: Pass empty list. Assert result is 0.0.
       d. `test_calculate_resonance_score_mixed_relevant`: Pass list with some relevant=True and some relevant=False. Assert only relevant matches contribute.
       e. `test_calculate_dcf_adjustment_strongly_supportive`: score=85.0, original_terminal_growth=0.025. Assert tier=STRONGLY_SUPPORTIVE, adjustment_pct=0.015, adjusted_terminal_growth=0.04.
       f. `test_calculate_dcf_adjustment_supportive`: score=60.0, original_terminal_growth=0.03. Assert tier=SUPPORTIVE, adjustment_pct=0.01, adjusted_terminal_growth=0.04.
       g. `test_calculate_dcf_adjustment_neutral`: score=30.0, original_terminal_growth=0.025. Assert tier=NEUTRAL, adjustment_pct=0.0, adjusted_terminal_growth=0.025.
       h. `test_calculate_dcf_adjustment_clamps_at_max`: score=90.0, original_terminal_growth=0.095, max_terminal_growth=0.10. Assert adjusted_terminal_growth=0.10 (clamped, not 0.11).
       i. `test_classify_resonance_tier_strong`: score=80.0 -> STRONGLY_SUPPORTIVE.
       j. `test_classify_resonance_tier_supportive`: score=40.0 -> SUPPORTIVE.
       k. `test_classify_resonance_tier_neutral`: score=39.9 -> NEUTRAL.
       l. `test_parse_llm_verification_valid_json`: Input `'{"relevant": true, "confidence": 0.9, "reason": "test"}'` -> parsed dict.
       m. `test_parse_llm_verification_code_block`: Input `'```json\n{"relevant": false, "confidence": 0.1, "reason": "unrelated"}\n```'` -> parsed dict.
       n. `test_parse_llm_verification_invalid`: Input `'not json at all'` -> None.
       o. `test_parse_metadata_extraction_valid`: Input JSON with title, policy_type, issuing_body, effective_date, industry_tags.
       p. `test_parse_metadata_extraction_missing_optional`: Input JSON without effective_date (should default to None).

    2. Run tests -- they MUST fail (RED).

    3. Create `stockvaluefinder/stockvaluefinder/services/policy_service.py` with these pure functions:

       a. `calculate_resonance_score(verified_matches: list[dict[str, Any]], config: PolicyResonanceConfig | None = None) -> float`:
          - If config is None, use global policy_resonance_config.
          - Filter to relevant=True matches only.
          - If none, return 0.0.
          - Calculate avg_cosine = mean of "score" values.
          - Calculate avg_confidence = mean of "confidence" values.
          - Return `config.COSINE_WEIGHT * (avg_cosine * 100) + config.LLM_WEIGHT * (avg_confidence * 100)`.
          - Round to 2 decimal places.

       b. `classify_resonance_tier(score: float, config: PolicyResonanceConfig | None = None) -> ResonanceTier`:
          - >= config.STRONG_TIER_THRESHOLD -> STRONGLY_SUPPORTIVE
          - >= config.RESONANCE_THRESHOLD -> SUPPORTIVE
          - else -> NEUTRAL

       c. `calculate_dcf_adjustment(resonance_score: float, original_terminal_growth: float, max_terminal_growth: float = 0.10, config: PolicyResonanceConfig | None = None) -> DCFAdjustment`:
          - Determine tier via classify_resonance_tier.
          - Map tier to adjustment: STRONGLY_SUPPORTIVE -> config.STRONG_ADJUSTMENT, SUPPORTIVE -> config.MODERATE_ADJUSTMENT, NEUTRAL -> config.NEUTRAL_ADJUSTMENT.
          - Clamp: `min(original_terminal_growth + adjustment, max_terminal_growth)`.
          - Return DCFAdjustment with tier, adjustment_pct, adjusted_terminal_growth (round to 6), original_terminal_growth.

       d. `parse_llm_verification(content: str) -> dict[str, Any] | None`:
          - Reuse NarrativeService._parse_llm_response pattern:
            1. Try markdown code block extraction.
            2. Try plain JSON parse.
            3. Try finding JSON in mixed text.
          - Validate parsed dict has "relevant" (bool) and "confidence" (float) keys.
          - Return None on any parse failure.

       e. `parse_metadata_extraction(content: str) -> dict[str, Any] | None`:
          - Same JSON parsing pattern as parse_llm_verification.
          - Validate parsed dict has "title", "policy_type", "issuing_body" keys.
          - Default "effective_date" to None if missing.
          - Default "industry_tags" to [] if missing.

    4. Run tests -- they MUST pass (GREEN).

    5. Add docstrings with Examples section using `>>>` doctest format for calculate_resonance_score and calculate_dcf_adjustment.
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_services/test_policy_service.py -x -v</automated>
  </verify>
  <done>
    - policy_service.py created with 5 pure functions (calculate_resonance_score, classify_resonance_tier, calculate_dcf_adjustment, parse_llm_verification, parse_metadata_extraction)
    - All functions use PolicyResonanceConfig with None default that falls back to global instance
    - calculate_resonance_score returns 0.0 for zero relevant matches (not NaN)
    - calculate_dcf_adjustment clamps adjusted_terminal_growth at max_terminal_growth
    - parse_llm_verification and parse_metadata_extraction handle code blocks, plain JSON, and mixed text
    - All 16+ unit tests pass
    - Test coverage covers all edge cases (empty list, all-irrelevant, clamping, boundary scores)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response -> parse functions | Untrusted text from DeepSeek API parsed as structured data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | parse_llm_verification | mitigate | Validate parsed dict has required keys (relevant, confidence); return None on invalid |
| T-11-02 | Spoofing | parse_metadata_extraction | mitigate | Validate parsed dict has required keys (title, policy_type, issuing_body); default optional fields |
| T-11-03 | Denial of Service | calculate_resonance_score | accept | Pure function, no external calls, negligible compute cost |
</threat_model>

<verification>
1. All unit tests pass: `cd stockvaluefinder && uv run pytest tests/unit/test_services/test_policy_service.py -x`
2. All imports resolve: `cd stockvaluefinder && uv run python -c "from stockvaluefinder.services.policy_service import calculate_resonance_score, calculate_dcf_adjustment; print('OK')"`
3. Linting passes: `cd stockvaluefinder && uv run ruff check stockvaluefinder/services/policy_service.py stockvaluefinder/models/policy.py`
</verification>

<success_criteria>
- calculate_resonance_score returns correct weighted score for relevant matches and 0.0 for no relevant matches
- calculate_dcf_adjustment returns correct tier-based adjustment with clamping at MAX_TERMINAL_GROWTH
- All pure functions have comprehensive unit tests covering normal and edge cases
- All models use frozen config, proper type hints, and follow established patterns
- PolicyResonanceConfig added to AppConfig without breaking existing config
</success_criteria>

<output>
After completion, create `.planning/phases/11-policy-resonance-engine/11-01-SUMMARY.md`
</output>
