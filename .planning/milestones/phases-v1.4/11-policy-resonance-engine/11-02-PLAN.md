---
phase: 11-policy-resonance-engine
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - stockvaluefinder/stockvaluefinder/external/akshare_client.py
  - stockvaluefinder/stockvaluefinder/external/data_service.py
  - stockvaluefinder/stockvaluefinder/db/models/stock.py
  - stockvaluefinder/stockvaluefinder/db/models/policy.py
  - stockvaluefinder/stockvaluefinder/repositories/policy_repo.py
  - stockvaluefinder/alembic/versions/013_policy_tables.py
  - stockvaluefinder/tests/unit/test_external/test_akshare_business_desc.py
autonomous: true
requirements:
  - POL-01
  - POL-02

must_haves:
  truths:
    - "AKShare stock_profile_cninfo() returns business description for stock business matching"
    - "Business descriptions cached in Redis with 24h TTL"
    - "Policy documents stored in dedicated policy_documents Qdrant collection"
    - "PolicyDocumentDB ORM model persists upload metadata to PostgreSQL"
    - "StockDB has business_description column for persistent storage"
  artifacts:
    - path: "stockvaluefinder/stockvaluefinder/external/akshare_client.py"
      provides: "get_stock_business_description() method"
      contains: "stock_profile_cninfo"
    - path: "stockvaluefinder/stockvaluefinder/external/data_service.py"
      provides: "get_business_description() with Redis cache"
      contains: "get_business_description"
    - path: "stockvaluefinder/stockvaluefinder/db/models/policy.py"
      provides: "PolicyDocumentDB ORM model"
      contains: "class PolicyDocumentDB"
    - path: "stockvaluefinder/stockvaluefinder/repositories/policy_repo.py"
      provides: "PolicyDocumentRepository"
      contains: "class PolicyDocumentRepository"
    - path: "stockvaluefinder/alembic/versions/013_policy_tables.py"
      provides: "Migration for policy_documents table + stocks.business_description"
      contains: "policy_documents"
    - path: "stockvaluefinder/tests/unit/test_external/test_akshare_business_desc.py"
      provides: "Unit tests for AKShare business description"
      min_lines: 40
  key_links:
    - from: "stockvaluefinder/stockvaluefinder/external/data_service.py"
      to: "stockvaluefinder/stockvaluefinder/external/akshare_client.py"
      via: "delegates to AKShareClient.get_stock_business_description()"
      pattern: "get_stock_business_description"
    - from: "stockvaluefinder/stockvaluefinder/external/data_service.py"
      to: "stockvaluefinder/stockvaluefinder/utils/cache.py"
      via: "Redis cache with TTL"
      pattern: "build_cache_key.*business_description"
---

<objective>
Build the data access layer for the Policy Resonance Engine.

Purpose: Provide AKShare business description fetching (with the CORRECT stock_profile_cninfo function, not stock_individual_info_em), Redis caching, the Qdrant policy_documents collection setup, PolicyDocumentDB ORM model, Alembic migration, and repository. This enables Plan 03 to wire the API endpoints.

Output: AKShare business description fetch with caching, PolicyDocumentDB ORM, Alembic migration 013, PolicyDocumentRepository, and StockDB.business_description column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-policy-resonance-engine/11-CONTEXT.md
@.planning/phases/11-policy-resonance-engine/11-RESEARCH.md
@.planning/phases/11-policy-resonance-engine/11-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan uses -->

From stockvaluefinder/stockvaluefinder/config.py (Plan 01 adds):
```python
@dataclass(frozen=True)
class PolicyResonanceConfig:
    BUSINESS_DESC_CACHE_TTL: int = 86400  # 24h cache for business descriptions
    VECTOR_SEARCH_THRESHOLD: float = 0.5
    MATCH_LIMIT: int = 5

policy_resonance_config = PolicyResonanceConfig()
```

From stockvaluefinder/stockvaluefinder/models/policy.py (Plan 01 creates):
```python
class PolicyMetadata(BaseModel):
    title: str
    policy_type: str
    issuing_body: str
    effective_date: str | None
    industry_tags: list[str]

class PolicyUploadResponse(BaseModel):
    document_id: str
    title: str
    chunk_count: int
    page_count: int
    status: str
```

<!-- Existing patterns this plan must follow -->

From stockvaluefinder/stockvaluefinder/external/data_service.py:
```python
# _cached_fetch pattern:
async def _cached_fetch(self, key_parts, ttl, fetch_fn):
    prefix = key_parts[0]
    identifiers = key_parts[1:]
    cache_key = build_cache_key(self._cache_version, prefix, *identifiers)
    # Try cache -> if miss -> fetch -> store -> return
```

From stockvaluefinder/stockvaluefinder/external/akshare_client.py:
```python
class AKShareClient:
    # _run_sync() wraps sync AKShare calls in thread pool
    async def _run_sync(self, func, *args):
        loop = asyncio.get_event_loop()
        return await loop.run_in_executor(self._executor, func, *args)
```

From stockvaluefinder/stockvaluefinder/rag/vector_store.py:
```python
class QdrantVectorStore:
    def __init__(self, url, collection, api_key=None, embedding_client=None):
        # Lazy Qdrant client init
    def ensure_collection_exists(self):
        # Creates collection + payload indexes
    async def upsert_chunks(self, chunks: list[DocumentChunk]):
        # Generate embeddings -> build points -> batch upsert
    async def search(self, query_vector, filter_dict=None, limit=10, score_threshold=0.7):
        # Vector similarity search with filtering
```

From stockvaluefinder/alembic/versions/012_capital_allocation_scores_table.py:
```python
revision: str = "012"
down_revision: Union[str, Sequence[str], None] = "011"
def upgrade() -> None:
    op.create_table(...)
    op.create_index(...)
def downgrade() -> None:
    op.drop_table(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AKShare business description fetch and ORM model</name>
  <files>
    stockvaluefinder/stockvaluefinder/external/akshare_client.py,
    stockvaluefinder/stockvaluefinder/external/data_service.py,
    stockvaluefinder/stockvaluefinder/db/models/stock.py,
    stockvaluefinder/stockvaluefinder/db/models/policy.py,
    stockvaluefinder/tests/unit/test_external/test_akshare_business_desc.py
  </files>
  <read_first>
    stockvaluefinder/stockvaluefinder/external/akshare_client.py,
    stockvaluefinder/stockvaluefinder/external/data_service.py,
    stockvaluefinder/stockvaluefinder/db/models/stock.py,
    stockvaluefinder/stockvaluefinder/db/models/capital_allocation.py
  </read_first>
  <acceptance_criteria>
    - `grep "def get_stock_business_description" stockvaluefinder/stockvaluefinder/external/akshare_client.py` returns match
    - `grep "stock_profile_cninfo" stockvaluefinder/stockvaluefinder/external/akshare_client.py` returns match (NOT stock_individual_info_em)
    - `grep "async def get_business_description" stockvaluefinder/stockvaluefinder/external/data_service.py` returns match
    - `grep "business_description" stockvaluefinder/stockvaluefinder/db/models/stock.py` returns match showing new Text column
    - `grep "class PolicyDocumentDB" stockvaluefinder/stockvaluefinder/db/models/policy.py` returns match
    - PolicyDocumentDB has columns: document_id (UUID PK), title, policy_type, issuing_body, effective_date, industry_tags (JSONB), file_path, page_count, chunk_count, upload_date, created_at
    - `uv run pytest tests/unit/test_external/test_akshare_business_desc.py -x` passes
    - Test mocks ak.stock_profile_cninfo return value and verifies field extraction
  </acceptance_criteria>
  <action>
    1. Add `get_stock_business_description(self, symbol: str) -> dict[str, str]` to AKShareClient:
       - IMPORTANT: Use `stock_profile_cninfo(symbol=symbol)` NOT `stock_individual_info_em()`. Per research correction, stock_individual_info_em does NOT return business descriptions.
       - Accept 6-digit stock code (e.g., '600519'). The function accepts 6-digit codes directly.
       - Extract `主营业务` field as `main_business` and `经营范围` field as `business_scope` from the first row of the returned DataFrame.
       - Return dict: `{"main_business": str, "business_scope": str}`.
       - If df is None or empty, return `{"main_business": "", "business_scope": ""}`.
       - Wrap AKShare sync call in `self._run_sync()` following existing pattern.
       - Handle exceptions with logger.warning and return empty dict.

    2. Add `async def get_business_description(self, ticker: str) -> dict[str, str]` to ExternalDataService:
       - Use `_cached_fetch()` pattern with key `("business_description", ticker)`.
       - TTL = `policy_resonance_config.BUSINESS_DESC_CACHE_TTL` (86400 seconds = 24h).
       - Fetch function calls `self._akshare_client.get_stock_business_description(symbol)` where symbol is extracted from ticker (strip .SH/.SZ suffix).
       - Follow exact pattern from existing `get_buyback_data()` and `get_roic_inputs()` methods.

    3. Add `business_description` column to StockDB in `stock.py`:
       - `business_description: Mapped[str | None] = mapped_column(Text, nullable=True, comment="Stock business description from AKShare")`
       - Import `Text` from sqlalchemy.

    4. Create `stockvaluefinder/stockvaluefinder/db/models/policy.py`:
       - `PolicyDocumentDB(Base)` with tablename `policy_documents`.
       - Columns: document_id (UUID PK), title (String 500), policy_type (String 50), issuing_body (String 200), effective_date (Date, nullable), industry_tags (JSONB), file_path (String 500), page_count (Integer), chunk_count (Integer), upload_date (DateTime), created_at (DateTime), updated_at (DateTime).
       - Follow existing ORM model patterns from `capital_allocation.py` and `roic.py`.

    5. Create test file `tests/unit/test_external/test_akshare_business_desc.py`:
       - Test with mocked `ak.stock_profile_cninfo` returning DataFrame with `主营业务` and `经营范围` columns.
       - Test extraction of correct fields.
       - Test empty DataFrame returns empty dict.
       - Test that stock_individual_info_em is NOT used (grep for it in the method should fail).
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_external/test_akshare_business_desc.py -x -v</automated>
  </verify>
  <done>
    - AKShareClient.get_stock_business_description() uses stock_profile_cninfo() (NOT stock_individual_info_em)
    - ExternalDataService.get_business_description() caches with 24h Redis TTL
    - StockDB has nullable business_description Text column
    - PolicyDocumentDB ORM model created with all required columns
    - All unit tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alembic migration and policy repository</name>
  <files>
    stockvaluefinder/alembic/versions/013_policy_tables.py,
    stockvaluefinder/stockvaluefinder/repositories/policy_repo.py,
    stockvaluefinder/tests/unit/test_repositories/test_policy_repo.py
  </files>
  <read_first>
    stockvaluefinder/alembic/versions/012_capital_allocation_scores_table.py,
    stockvaluefinder/stockvaluefinder/repositories/capital_allocation_repo.py,
    stockvaluefinder/stockvaluefinder/repositories/base.py,
    stockvaluefinder/stockvaluefinder/db/models/policy.py
  </read_first>
  <acceptance_criteria>
    - `grep "revision.*013" stockvaluefinder/alembic/versions/013_policy_tables.py` returns match
    - `grep "down_revision.*012" stockvaluefinder/alembic/versions/013_policy_tables.py` returns match
    - `grep "policy_documents" stockvaluefinder/alembic/versions/013_policy_tables.py` returns match (creates table)
    - `grep "business_description" stockvaluefinder/alembic/versions/013_policy_tables.py` returns match (adds column to stocks)
    - `grep "class PolicyDocumentRepository" stockvaluefinder/stockvaluefinder/repositories/policy_repo.py` returns match
    - `grep "async def create" stockvaluefinder/stockvaluefinder/repositories/policy_repo.py` returns match
    - `grep "async def get_by_document_id" stockvaluefinder/stockvaluefinder/repositories/policy_repo.py` returns match
    - `grep "async def get_all_policies" stockvaluefinder/stockvaluefinder/repositories/policy_repo.py` returns match
    - `uv run pytest tests/unit/test_repositories/test_policy_repo.py -x` passes
  </acceptance_criteria>
  <action>
    1. Create Alembic migration `stockvaluefinder/alembic/versions/013_policy_tables.py`:
       - revision = "013", down_revision = "012".
       - upgrade():
         a. Add column `business_description` (Text, nullable) to `stocks` table via `op.add_column()`.
         b. Create `policy_documents` table with columns: document_id (UUID PK), title (String 500), policy_type (String 50), issuing_body (String 200), effective_date (Date nullable), industry_tags (JSONB), file_path (String 500), page_count (Integer), chunk_count (Integer), upload_date (DateTime), created_at (DateTime), updated_at (DateTime).
         c. Create indexes on policy_documents: document_id, policy_type, issuing_body, upload_date.
       - downgrade():
         a. Drop `policy_documents` table.
         b. Drop `business_description` column from `stocks`.
       - Follow exact pattern from 012 migration.

    2. Create `stockvaluefinder/stockvaluefinder/repositories/policy_repo.py`:
       - `PolicyDocumentRepository` inheriting from `BaseRepository[PolicyDocumentDB, PolicyDocumentCreate, PolicyDocumentUpdate]`.
       - Need PolicyDocumentCreate and PolicyDocumentUpdate Pydantic schemas (add to models/policy.py if not already there, or define inline).
       - Methods:
         a. `async def get_by_document_id(self, document_id: str) -> PolicyDocumentDB | None`
         b. `async def get_all_policies(self, limit: int = 100, offset: int = 0) -> list[PolicyDocumentDB]`
         c. `async def get_by_policy_type(self, policy_type: str) -> list[PolicyDocumentDB]`
         d. `async def delete_by_document_id(self, document_id: str) -> bool`
       - Follow pattern from CapitalAllocationRepository.

    3. Create test file `tests/unit/test_repositories/test_policy_repo.py`:
       - Use mock AsyncSession.
       - Test get_by_document_id with mocked result.
       - Test get_all_policies with mocked result.
       - Test create with mocked session.
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_repositories/test_policy_repo.py -x -v</automated>
  </verify>
  <done>
    - Alembic migration 013 creates policy_documents table and adds business_description to stocks
    - PolicyDocumentRepository provides CRUD operations for policy documents
    - Migration follows revision chain: 012 -> 013
    - All repository tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AKShare API -> client | External data source, may return unexpected formats or be unavailable |
| User upload -> filesystem | File path from upload, must sanitize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | AKShare business description | mitigate | Validate DataFrame has expected columns before extraction; return empty dict on failure |
| T-11-05 | Denial of Service | get_business_description | mitigate | Redis cache with 24h TTL prevents repeated AKShare calls |
| T-11-06 | Information Disclosure | file_path in PolicyDocumentDB | accept | File path is server-side only, not exposed to API consumers directly |
</threat_model>

<verification>
1. AKShare test passes: `cd stockvaluefinder && uv run pytest tests/unit/test_external/test_akshare_business_desc.py -x`
2. Repository test passes: `cd stockvaluefinder && uv run pytest tests/unit/test_repositories/test_policy_repo.py -x`
3. Migration file exists: `ls stockvaluefinder/alembic/versions/013_policy_tables.py`
4. stock_profile_cninfo used (NOT stock_individual_info_em): `grep "stock_profile_cninfo" stockvaluefinder/stockvaluefinder/external/akshare_client.py`
</verification>

<success_criteria>
- AKShareClient uses stock_profile_cninfo() for business descriptions (NOT stock_individual_info_em)
- Business descriptions cached in Redis with 24h TTL via ExternalDataService
- PolicyDocumentDB ORM model with all D-10 metadata fields
- Alembic migration 013 creates policy_documents table and adds business_description to stocks
- PolicyDocumentRepository provides basic CRUD
- All new code has corresponding unit tests that pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-policy-resonance-engine/11-02-SUMMARY.md`
</output>
