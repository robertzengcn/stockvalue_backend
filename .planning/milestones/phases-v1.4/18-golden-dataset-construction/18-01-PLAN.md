---
phase: 18-golden-dataset-construction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stockvaluefinder/tests/golden/manifest.yaml
  - stockvaluefinder/tests/golden/600519.SH/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/601398.SH/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/601318.SH/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/000063.SZ/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/000002.SZ/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/601088.SH/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/000333.SZ/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/601857.SH/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/600036.SH/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/600585.SH/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/002475.SZ/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/600276.SH/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/600900.SH/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/provenance_template.md
  - stockvaluefinder/tests/golden/__init__.py
  - stockvaluefinder/tests/golden/conftest.py
autonomous: true
requirements:
  - GOLD-01
  - GOLD-02
  - GOLD-04

must_haves:
  truths:
    - "Golden test directory structure exists under tests/golden/ with 14 stock subdirectories"
    - "manifest.yaml lists all 14 golden stocks with ticker, name, sector, years, verification status"
    - "Each stock/year directory has an expected_metrics.yaml template containing all metric names from metric_registry.yaml"
    - "provenance_template.md provides the standard format for documenting value sources"
    - "conftest.py exposes golden_loader fixture that loads manifest and individual stock data"
    - "golden_stock_ids returns ALL manifest entries; verified_golden_stock_ids filters by l3_verified"
  artifacts:
    - path: "stockvaluefinder/tests/golden/manifest.yaml"
      provides: "Golden dataset catalog for test discovery"
      contains: "golden_stocks"
    - path: "stockvaluefinder/tests/golden/conftest.py"
      provides: "pytest fixtures for golden test loading"
      exports: ["golden_manifest", "golden_loader", "golden_stock_ids", "verified_golden_stock_ids", "golden_manifest_entries"]
    - path: "stockvaluefinder/tests/golden/600519.SH/2023/expected_metrics.yaml"
      provides: "Metric template for Moutai golden values"
      contains: "ticker"
  key_links:
    - from: "stockvaluefinder/tests/golden/conftest.py"
      to: "stockvaluefinder/validation/loader.py"
      via: "imports load_metric_registry for metric name resolution"
      pattern: "from stockvaluefinder.validation.loader import load_metric_registry"
    - from: "stockvaluefinder/tests/golden/conftest.py"
      to: "stockvaluefinder/tests/golden/manifest.yaml"
      via: "reads manifest at fixture setup time"
      pattern: "manifest.yaml"
---

<objective>
Create the golden dataset directory structure, manifest, and scaffolding that will hold hand-verified expected values for 14 CSI 300 stocks.

Purpose: Establish the test data infrastructure so that Phase 18-02 can populate real values and Phase 19-21 can consume them. The manifest drives test discovery; the expected_metrics.yaml templates enumerate every metric from the registry; the conftest.py provides reusable fixtures. Per anchor stock approach: 600519.SH will be fully verified in Plan 18-02; the other 13 stocks receive templates for future human verification.

Output: 14 stock directories each with an expected_metrics.yaml template, a root manifest.yaml, provenance_template.md, and conftest.py with golden_loader fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-metric-registry-foundation/17-02-SUMMARY.md

@stockvaluefinder/stockvaluefinder/validation/schema.py
@stockvaluefinder/stockvaluefinder/validation/metric_registry.yaml
@stockvaluefinder/stockvaluefinder/validation/loader.py
@stockvaluefinder/stockvaluefinder/validation/comparators.py

<interfaces>
<!-- Phase 17 output -- the validation module public API -->

From stockvaluefinder/validation/schema.py:
```python
class Tolerance(BaseModel):
    absolute: float | None = None
    relative: float | None = None

class MetricDefinition(BaseModel):
    display_name: str
    category: str  # risk | roic | valuation | yield | capex | policy | alpha
    function: str
    params: list[InputField]
    returns: str
    tolerance: Tolerance
    priority: str  # P0 | P1 | P2
    variants: dict[str, Variant] = {}

class MetricRegistry(BaseModel):
    metrics: dict[str, MetricDefinition]
    def get(self, name: str, sector: str | None = None) -> MetricDefinition
    def metrics_by_category(self, category: str) -> list[tuple[str, MetricDefinition]]
    def p0_metrics(self) -> list[tuple[str, MetricDefinition]]
    def all_metrics(self) -> list[tuple[str, MetricDefinition]]
```

From stockvaluefinder/validation/loader.py:
```python
def load_metric_registry() -> MetricRegistry
```

From stockvaluefinder/validation/comparators.py:
```python
class ComparisonResult:  # frozen dataclass
    metric_name: str
    expected: float
    computed: float
    delta: float
    tolerance_applied: Tolerance
    passed: bool

def compare_within_tolerance(expected: float, computed: float, tolerance: Tolerance) -> ComparisonResult
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create golden directory structure, manifest.yaml, and provenance template</name>
  <files>
    stockvaluefinder/tests/golden/__init__.py
    stockvaluefinder/tests/golden/manifest.yaml
    stockvaluefinder/tests/golden/provenance_template.md
    stockvaluefinder/tests/golden/600519.SH/2023/.gitkeep
    stockvaluefinder/tests/golden/601398.SH/2023/.gitkeep
    stockvaluefinder/tests/golden/601318.SH/2023/.gitkeep
    stockvaluefinder/tests/golden/000063.SZ/2023/.gitkeep
    stockvaluefinder/tests/golden/000002.SZ/2023/.gitkeep
    stockvaluefinder/tests/golden/601088.SH/2023/.gitkeep
    stockvaluefinder/tests/golden/000333.SZ/2023/.gitkeep
    stockvaluefinder/tests/golden/601857.SH/2023/.gitkeep
    stockvaluefinder/tests/golden/600036.SH/2023/.gitkeep
    stockvaluefinder/tests/golden/600585.SH/2023/.gitkeep
    stockvaluefinder/tests/golden/002475.SZ/2023/.gitkeep
    stockvaluefinder/tests/golden/600276.SH/2023/.gitkeep
    stockvaluefinder/tests/golden/600900.SH/2023/.gitkeep
  </files>
  <action>
Create the golden dataset directory structure under `stockvaluefinder/tests/golden/`.

**Directory structure** (per GOLD-02, from validation plan section 4.3):
```
tests/golden/
  __init__.py
  manifest.yaml
  provenance_template.md
  600519.SH/2023/    # Kweichow Moutai -- consumer staples
  601398.SH/2023/    # ICBC -- banking
  601318.SH/2023/    # Ping An Insurance -- insurance
  000063.SZ/2023/    # ZTE -- technology
  000002.SZ/2023/    # Vanke -- real estate
  601088.SH/2023/    # China Shenhua -- energy/high-dividend
  000333.SZ/2023/    # Midea Group -- consumer discretionary
  601857.SH/2023/    # PetroChina -- energy
  600036.SH/2023/    # China Merchants Bank -- banking
  600585.SH/2023/    # Anhui Conch Cement -- materials
  002475.SZ/2023/    # Luxshare Precision -- industrials/technology
  600276.SH/2023/    # Jiangsu Hengrui Medicine -- pharmaceuticals
  600900.SH/2023/    # Yangtze Power -- utilities
```

14 stocks total (per GOLD-01: 12-15 required). Covers all sectors: consumer staples, banking (2), insurance, technology (2), real estate, energy (2), consumer discretionary, materials, industrials, pharmaceuticals, utilities.

Place a `.gitkeep` in each stock/year directory to preserve the empty directory in git. The actual files (expected_metrics.yaml, provenance.md, raw JSON) will be populated in plan 18-02.

**manifest.yaml** -- catalogs all 14 golden stocks (per GOLD-04). Format:

```yaml
# Golden Dataset Manifest
# Drives test discovery for L2/L3 validation
# Each entry: ticker, name, sector, years, verification status, provenance

golden_stocks:
  - ticker: "600519.SH"
    name: "Kweichow Moutai"
    sector: "consumer_staples"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P0: M-Score, ROIC, F-Score anchor stock"

  - ticker: "601398.SH"
    name: "Industrial and Commercial Bank of China"
    sector: "banking"
    is_financial: true
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P0: Financial-sector ROIC/NOPAT formula validation"

  - ticker: "601318.SH"
    name: "Ping An Insurance"
    sector: "insurance"
    is_financial: true
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "Insurance sector income field mapping"

  - ticker: "000063.SZ"
    name: "ZTE Corporation"
    sector: "technology"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P1: CapEx, FCF, high R&D"

  - ticker: "000002.SZ"
    name: "China Vanke"
    sector: "real_estate"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P0: High leverage LVGI, real estate sector"

  - ticker: "601088.SH"
    name: "China Shenhua Energy"
    sector: "energy"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P1: High dividend, Yield Gap validation"

  - ticker: "000333.SZ"
    name: "Midea Group"
    sector: "consumer_discretionary"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P1: Diversified industrials, FCF positive"

  - ticker: "601857.SH"
    name: "PetroChina"
    sector: "energy"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P1: Large-cap energy, WACC components"

  - ticker: "600036.SH"
    name: "China Merchants Bank"
    sector: "banking"
    is_financial: true
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P0: Second banking stock for financial ROIC cross-check"

  - ticker: "600585.SH"
    name: "Anhui Conch Cement"
    sector: "materials"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P1: Cyclical materials, CapEx discipline"

  - ticker: "002475.SZ"
    name: "Luxshare Precision"
    sector: "industrials"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P1: Industrial growth, expansion discipline"

  - ticker: "600276.SH"
    name: "Jiangsu Hengrui Medicine"
    sector: "pharmaceuticals"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P1: Pharma sector, R&D heavy, goodwill"

  - ticker: "600900.SH"
    name: "China Yangtze Power"
    sector: "utilities"
    is_financial: false
    years: [2023]
    l3_verified: false
    provenance: "pending"
    notes: "P1: Utilities, stable dividend, Yield Gap"
```

**provenance_template.md** -- template for documenting value sources (per GOLD-02/GOLD-05). Place at `tests/golden/provenance_template.md`. Content:

```markdown
# Provenance: {TICKER} {FISCAL_YEAR}

## Source Document
- **Annual Report**: {Company Name} {Year} Annual Report
- **CNINFO Link**: http://www.cninfo.com.cn/...
- **Exchange Filing ID**: ...
- **Report Date**: YYYY-MM-DD

## Verified Values

### Income Statement (Page X)
| Metric | Value | Source Page | Line Item (Chinese) | Notes |
|--------|-------|-------------|---------------------|-------|
| Revenue | X | p.XX | 营业收入 | ... |
| Net Income | X | p.XX | 净利润 | ... |

### Balance Sheet (Page X)
| Metric | Value | Source Page | Line Item (Chinese) | Notes |
|--------|-------|-------------|---------------------|-------|
| Total Assets | X | p.XX | 资产总计 | ... |

### Cash Flow Statement (Page X)
| Metric | Value | Source Page | Line Item (Chinese) | Notes |
|--------|-------|-------------|---------------------|-------|
| Operating Cash Flow | X | p.XX | 经营活动产生的现金流量净额 | ... |

## Assumptions and Adjustments
- [Document any adjustments made, e.g., "Used consolidated figures, not parent-only"]

## Verifier
- **Name**: ...
- **Date**: YYYY-MM-DD
- **Method**: Manual reading of annual report PDF / CNINFO download
```

Create `__init__.py` as an empty file to make `tests/golden/` a Python package.
  </action>
  <verify>
    cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && python -c "
import yaml
from pathlib import Path
m = yaml.safe_load(Path('tests/golden/manifest.yaml').read_text())
stocks = m['golden_stocks']
assert len(stocks) == 14, f'Expected 14 stocks, got {len(stocks)}'
tickers = [s['ticker'] for s in stocks]
sectors = set(s['sector'] for s in stocks)
financials = [s['ticker'] for s in stocks if s['is_financial']]
assert len(financials) >= 3, f'Expected >= 3 financial stocks, got {len(financials)}'
required_sectors = {'consumer_staples', 'banking', 'insurance', 'technology', 'real_estate', 'energy', 'pharmaceuticals', 'utilities', 'materials', 'industrials', 'consumer_discretionary'}
assert required_sectors.issubset(sectors), f'Missing sectors: {required_sectors - sectors}'
for s in stocks:
    d = Path(f'tests/golden/{s[\"ticker\"]}/{s[\"years\"][0]}')
    assert d.exists(), f'Missing directory: {d}'
print(f'Manifest OK: {len(stocks)} stocks across {len(sectors)} sectors')
"
  </verify>
  <done>
    - 14 stock directories created under tests/golden/{TICKER}/2023/
    - manifest.yaml lists all 14 stocks with sector, is_financial flag, years, and verification status
    - provenance_template.md exists with standard format for documenting value sources
    - All 11 required sectors represented (consumer_staples, banking, insurance, technology, real_estate, energy, pharmaceuticals, utilities, materials, industrials, consumer_discretionary)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create expected_metrics.yaml templates for all 14 stocks and conftest.py with golden_loader fixture</name>
  <files>
    stockvaluefinder/tests/golden/600519.SH/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/601398.SH/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/601318.SH/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/000063.SZ/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/000002.SZ/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/601088.SH/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/000333.SZ/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/601857.SH/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/600036.SH/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/600585.SH/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/002475.SZ/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/600276.SH/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/600900.SH/2023/expected_metrics.yaml
    stockvaluefinder/tests/golden/conftest.py
  </files>
  <action>
Create expected_metrics.yaml templates for each of the 14 golden stocks. Each template enumerates ALL metrics from the metric_registry.yaml as a placeholder, so that plan 18-02 only needs to fill in the `value` fields.

**expected_metrics.yaml format** (per GOLD-02, aligned with metric_registry.yaml entries). Write this template to ALL 14 stock/year directories. Each file is identical except for the `ticker` and `fiscal_year` fields.

Standard template (non-financial stocks):
```yaml
# Golden Expected Metrics: {TICKER} FY{YEAR}
# Source: Annual report / CNINFO filing -- NOT AKShare (per GOLD-05)
# Verification status: pending (values need to be hand-verified)

ticker: "{TICKER}"
fiscal_year: {YEAR}
source: "pending_verification"
verified_date: null
verified_by: null

metrics:
  dsri:
    value: null
    tolerance: {absolute: 0.05}
    source_page: null
  gmi:
    value: null
    tolerance: {absolute: 0.05}
    source_page: null
  aqi:
    value: null
    tolerance: {absolute: 0.05}
    source_page: null
  sgi:
    value: null
    tolerance: {absolute: 0.05}
    source_page: null
  depi:
    value: null
    tolerance: {absolute: 0.05}
    source_page: null
  sgai:
    value: null
    tolerance: {absolute: 0.05}
    source_page: null
  lvgi:
    value: null
    tolerance: {absolute: 0.05}
    source_page: null
  tata:
    value: null
    tolerance: {absolute: 0.05}
    source_page: null
  m_score:
    value: null
    tolerance: {absolute: 0.05}
    source_page: null
  f_score:
    value: null
    tolerance: {absolute: 0}
    source_page: null
  detect_存贷双高:
    value: null
    tolerance: {absolute: 0.01}
    source_page: null
  goodwill_ratio:
    value: null
    tolerance: {relative: 0.01}
    source_page: null
  profit_cash_divergence:
    value: null
    tolerance: {absolute: 0.01}
    source_page: null
  nopat:
    value: null
    tolerance: {relative: 0.02}
    source_page: null
  invested_capital:
    value: null
    tolerance: {relative: 0.02}
    source_page: null
  roic:
    value: null
    tolerance: {relative: 0.01}
    source_page: null
  roic_wacc_spread:
    value: null
    tolerance: {absolute: 0.001}
    source_page: null
  wacc:
    value: null
    tolerance: {relative: 0.01}
    source_page: null
  present_value:
    value: null
    tolerance: {relative: 0.02}
    source_page: null
  terminal_value:
    value: null
    tolerance: {relative: 0.02}
    source_page: null
  margin_of_safety:
    value: null
    tolerance: {relative: 0.01}
    source_page: null
  net_dividend_yield:
    value: null
    tolerance: {relative: 0.01}
    source_page: null
  yield_gap:
    value: null
    tolerance: {relative: 0.01}
    source_page: null
  buyback_yield:
    value: null
    tolerance: {relative: 0.01}
    source_page: null
  capital_allocation_score:
    value: null
    tolerance: {absolute: 0.5}
    source_page: null
  resonance_score:
    value: null
    tolerance: {absolute: 1.0}
    source_page: null
  dcf_adjustment:
    value: null
    tolerance: {absolute: 0.001}
    source_page: null
  alpha_score:
    value: null
    tolerance: {absolute: 0.1}
    source_page: null
```

For the 3 financial-sector stocks (601398.SH, 601318.SH, 600036.SH), add an additional metric after `roic`:
```yaml
  nopat_financial:
    value: null
    tolerance: {relative: 0.02}
    source_page: null
    notes: "Financial-sector variant: NOPAT = OPERATE_PROFIT * (1-T)"
```

**conftest.py** -- provides pytest fixtures for loading golden data. Place at `tests/golden/conftest.py`.

CRITICAL: The `golden_stock_ids` fixture returns ALL manifest entries (not just verified ones). A separate `verified_golden_stock_ids` fixture filters by `l3_verified`. This is the anchor stock approach -- all stocks are visible to test discovery, but L3 tests only run against verified entries.

```python
"""Pytest fixtures for golden dataset loading.

Provides:
- golden_manifest: Parsed manifest.yaml
- golden_stock_ids: List of (ticker, year) tuples for ALL stocks in manifest
- verified_golden_stock_ids: List of (ticker, year) tuples filtered by l3_verified=True
- golden_loader: Callable that loads expected_metrics.yaml for a given ticker/year
- golden_manifest_entries: Dict keyed by ticker for sector/is_financial lookup
"""

from __future__ import annotations

from pathlib import Path
from typing import Any

import pytest
import yaml


GOLDEN_DIR = Path(__file__).parent


@pytest.fixture(scope="session")
def golden_manifest() -> dict[str, Any]:
    """Load and return the golden manifest (manifest.yaml)."""
    manifest_path = GOLDEN_DIR / "manifest.yaml"
    content = manifest_path.read_text(encoding="utf-8")
    return yaml.safe_load(content)


@pytest.fixture(scope="session")
def golden_stock_ids(golden_manifest: dict[str, Any]) -> list[tuple[str, int]]:
    """Return list of (ticker, year) tuples for ALL stocks in manifest.

    Returns ALL entries regardless of l3_verified status. This is used
    for test discovery -- the l3_verified flag is used to skip tests,
    not to hide stocks from the fixture.
    """
    ids: list[tuple[str, int]] = []
    for entry in golden_manifest["golden_stocks"]:
        for year in entry["years"]:
            ids.append((entry["ticker"], year))
    return ids


@pytest.fixture(scope="session")
def verified_golden_stock_ids(golden_manifest: dict[str, Any]) -> list[tuple[str, int]]:
    """Return list of (ticker, year) tuples ONLY for l3_verified stocks.

    Used by L3 golden tests to parametrize only against fully verified
    entries. Stocks not yet hand-verified are excluded.
    """
    ids: list[tuple[str, int]] = []
    for entry in golden_manifest["golden_stocks"]:
        if entry.get("l3_verified", False):
            for year in entry["years"]:
                ids.append((entry["ticker"], year))
    return ids


@pytest.fixture(scope="session")
def golden_loader() -> Any:
    """Return a callable that loads expected_metrics.yaml for a ticker/year.

    Usage::

        def test_moutai(golden_loader):
            metrics = golden_loader("600519.SH", 2023)
            assert metrics["ticker"] == "600519.SH"

    Returns:
        Callable that accepts (ticker: str, year: int) and returns parsed YAML dict.
    """

    def _load(ticker: str, year: int) -> dict[str, Any]:
        path = GOLDEN_DIR / ticker / str(year) / "expected_metrics.yaml"
        if not path.exists():
            msg = f"Golden data not found: {path}"
            raise FileNotFoundError(msg)
        content = path.read_text(encoding="utf-8")
        return yaml.safe_load(content)

    return _load


@pytest.fixture(scope="session")
def golden_manifest_entries(golden_manifest: dict[str, Any]) -> dict[str, dict[str, Any]]:
    """Return dict keyed by ticker of manifest entries.

    Useful for looking up sector/is_financial by ticker.
    """
    return {entry["ticker"]: entry for entry in golden_manifest["golden_stocks"]}
```

Remove the `.gitkeep` files from directories that now have expected_metrics.yaml (all 14).
  </action>
  <verify>
    cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && python -c "
import yaml
from pathlib import Path

tickers = [
    '600519.SH', '601398.SH', '601318.SH', '000063.SZ',
    '000002.SZ', '601088.SH', '000333.SZ', '601857.SH',
    '600036.SH', '600585.SH', '002475.SZ', '600276.SH',
    '600900.SH',
]
for t in tickers:
    p = Path(f'tests/golden/{t}/2023/expected_metrics.yaml')
    assert p.exists(), f'Missing: {p}'
    data = yaml.safe_load(p.read_text())
    assert data['ticker'] == t, f'Wrong ticker in {t}: {data[\"ticker\"]}'
    assert data['fiscal_year'] == 2023, f'Wrong year in {t}'
    assert 'metrics' in data, f'Missing metrics key in {t}'
    metric_names = set(data['metrics'].keys())
    assert len(metric_names) >= 28, f'{t} has {len(metric_names)} metrics, expected >= 28'

# Verify financial stocks have nopat_financial
for t in ['601398.SH', '601318.SH', '600036.SH']:
    data = yaml.safe_load(Path(f'tests/golden/{t}/2023/expected_metrics.yaml').read_text())
    assert 'nopat_financial' in data['metrics'], f'{t} missing nopat_financial'

# Verify conftest.py fixture behavior: golden_stock_ids returns ALL, verified_golden_stock_ids filters
from tests.golden.conftest import GOLDEN_DIR
manifest = yaml.safe_load((GOLDEN_DIR / 'manifest.yaml').read_text())
all_ids = []
verified_ids = []
for entry in manifest['golden_stocks']:
    for year in entry['years']:
        all_ids.append((entry['ticker'], year))
        if entry.get('l3_verified', False):
            verified_ids.append((entry['ticker'], year))
assert len(all_ids) == 14, f'Expected 14 all_ids, got {len(all_ids)}'
# Before plan 18-02 runs, none are verified, so verified_ids should be empty
assert len(verified_ids) == 0, f'Expected 0 verified_ids before plan 18-02, got {len(verified_ids)}'

print(f'All 14 expected_metrics.yaml templates validated')
print('Financial-sector variants verified')
print('conftest.py fixtures verified: golden_stock_ids returns ALL 14, verified_golden_stock_ids filters by l3_verified')
"
  </verify>
  <done>
    - All 14 stocks have expected_metrics.yaml templates with 28 metric entries matching metric_registry.yaml
    - Financial stocks (601398.SH, 601318.SH, 600036.SH) include nopat_financial variant
    - conftest.py provides golden_manifest, golden_stock_ids (ALL stocks), verified_golden_stock_ids (l3_verified only), golden_loader, golden_manifest_entries fixtures
    - golden_stock_ids returns ALL 14 entries; verified_golden_stock_ids returns only l3_verified=true entries
    - Each metric entry has value (null), tolerance, and source_page fields
    - Templates are ready for population in plan 18-02 (anchor stock 600519.SH first)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden YAML files -> test code | Test data loaded from filesystem; must be well-formed YAML |
| manifest.yaml -> test discovery | Malformed manifest could cause test discovery failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | S | manifest.yaml | accept | Test data file, no user input, not exposed to network |
| T-18-02 | T | expected_metrics.yaml | accept | File integrity managed by git, no runtime modification |
| T-18-03 | I | conftest.py path traversal | mitigate | golden_loader uses GOLDEN_DIR = Path(__file__).parent which anchors to package directory |
</threat_model>

<verification>
1. Manifest loads cleanly with PyYAML and contains 14 stock entries
2. All 14 stock directories contain expected_metrics.yaml with 28 metric entries
3. conftest.py fixtures import successfully (golden_manifest, golden_loader, golden_stock_ids, verified_golden_stock_ids, golden_manifest_entries)
4. Financial stocks include nopat_financial variant metric
5. All sectors from GOLD-01 represented in manifest
6. golden_stock_ids returns all 14 entries; verified_golden_stock_ids returns 0 (pre-verification)
7. `cd stockvaluefinder && uv run python -c "from tests.golden.conftest import golden_manifest"` succeeds
</verification>

<success_criteria>
- 14 golden stock directories with expected_metrics.yaml templates exist
- manifest.yaml catalogs all entries with sector, is_financial, years, verification status
- provenance_template.md documents standard format for value source tracking
- conftest.py provides 5 reusable fixtures for golden test loading
- golden_stock_ids returns ALL stocks; verified_golden_stock_ids returns only l3_verified entries
- Every metric from metric_registry.yaml has a corresponding entry in the templates
- No .gitkeep files remain in directories with expected_metrics.yaml
</success_criteria>

<output>
After completion, create `.planning/phases/18-golden-dataset-construction/18-01-SUMMARY.md`
</output>
