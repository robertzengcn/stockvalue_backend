---
phase: 24-golden-dataset-expansion
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]  # needs generalized compute_golden_values.py from 24-01 Task 1
files_modified:
  - stockvaluefinder/tests/golden/000063.SZ/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/000063.SZ/2023/provenance.md
  - stockvaluefinder/tests/golden/000002.SZ/2023/expected_metrics.yaml
  - stockvaluefinder/tests/golden/000002.SZ/2023/provenance.md
  - stockvaluefinder/tests/golden/manifest.yaml
autonomous: false  # requires human cross-verification against annual reports
requirements: [GOLD-02, GOLD-03, LV3-01, LV3-02]

must_haves:
  truths:
    - "000063.SZ expected_metrics.yaml has populated values for all 16 frozen-data-derivable metrics, cross-verified against annual report"
    - "000002.SZ expected_metrics.yaml has populated values for all 16 frozen-data-derivable metrics, cross-verified against annual report"
    - "Both stocks flipped to l3_verified: true in manifest.yaml after verification"
    - "pytest -m golden passes with the 2 newly verified stocks added to parametrization (Phase 24 total: 5 verified stocks)"
    - "reconcile --ticker 000063.SZ --year 2023 returns p0_all_pass=True"
    - "reconcile --ticker 000002.SZ --year 2023 returns p0_all_pass=True"
    - "NOPAT for both stocks computed using the non-financial branch (TOTAL_PROFIT + FINANCE_EXPENSE)"
  artifacts:
    - path: "stockvaluefinder/tests/golden/000063.SZ/2023/expected_metrics.yaml"
      provides: "Populated golden values for ZTE FY2023, P0 cross-verified"
      contains: "verified_date"
    - path: "stockvaluefinder/tests/golden/000063.SZ/2023/provenance.md"
      provides: "Annual-report page citations + tech-sector R&D notes"
      contains: "VERIFIED"
    - path: "stockvaluefinder/tests/golden/000002.SZ/2023/expected_metrics.yaml"
      provides: "Populated golden values for Vanke FY2023, P0 cross-verified"
      contains: "verified_date"
    - path: "stockvaluefinder/tests/golden/000002.SZ/2023/provenance.md"
      provides: "Annual-report page citations + real-estate high-leverage LVGI notes"
      contains: "VERIFIED"
    - path: "stockvaluefinder/tests/golden/manifest.yaml"
      provides: "000063.SZ and 000002.SZ flipped to l3_verified: true"
      contains: "l3_verified: true"
  key_links:
    - from: "stockvaluefinder/tests/golden/compute_golden_values.py"
      to: "stockvaluefinder/tests/golden/{000063.SZ,000002.SZ}/2023/expected_metrics.yaml"
      via: "CLI invocation: uv run python tests/golden/compute_golden_values.py --ticker {T} --year 2023"
      pattern: "--ticker"
    - from: "stockvaluefinder/stockvaluefinder/services/roic_service.py"
      to: "stockvaluefinder/tests/golden/000063.SZ/2023/expected_metrics.yaml"
      via: "calculate_nopat(profit_data, is_financial=False) — exercises non-financial branch"
      pattern: "TOTAL_PROFIT"
---

<objective>
Hand-verify the two non-financial stocks (000063.SZ ZTE technology + 000002.SZ Vanke real estate) by running the now-generalized compute_golden_values.py and cross-verifying P0 metrics against each company's 2023 annual report.

Purpose: Close v1.4 audit gaps GOLD-02, GOLD-03, LV3-01, LV3-02 for the two non-financial stocks. This exercises the non-financial-sector NOPAT branch (`is_financial=False`, uses TOTAL_PROFIT + FINANCE_EXPENSE) plus two sector-specific stress points:
- ZTE: significant R&D expense + tech-sector revenue recognition (long-cycle contracts)
- Vanke: high leverage (real estate developer with large interest-bearing debt) — stresses LVGI sub-index of M-Score, and the FINANCE_EXPENSE adjustment in NOPAT formula

Output:
- Populated expected_metrics.yaml + annotated provenance.md for ZTE and Vanke
- manifest.yaml: both stocks l3_verified: true
- Golden test suite passes at 5/14 verified stocks total (Moutai + ICBC + Ping An + ZTE + Vanke)
- reconcile CLI confirms p0_all_pass=True for both
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.4-MILESTONE-AUDIT.md
@.planning/phases/24-golden-dataset-expansion/24-01-PLAN.md
@.planning/phases/24-golden-dataset-expansion/24-01-SUMMARY.md  # 24-02 starts after 24-01 completes

<interfaces>
<!-- Same compute_golden_values.py from 24-01 -->

From stockvaluefinder/tests/golden/compute_golden_values.py (post-24-01):
```bash
uv run python tests/golden/compute_golden_values.py --ticker 000063.SZ --year 2023
uv run python tests/golden/compute_golden_values.py --ticker 000002.SZ --year 2023
```

From stockvaluefinder/tests/golden/manifest.yaml:
```yaml
- ticker: 000063.SZ
  name: 中兴通讯 ZTE
  sector: technology
  is_financial: false        # exercises non-financial NOPAT branch
  years: [2023]
  l3_verified: false         # flip to true after verification

- ticker: 000002.SZ
  name: 万科 A
  sector: real_estate
  is_financial: false        # exercises non-financial NOPAT branch
  years: [2023]
  l3_verified: false
```

Frozen AKShare data for both stocks already exists from Phase 18-02.
Provenance.md skeletons already exist with verification instructions.
</interfaces>
</context>

<tasks>

<task type="manual" tdd="false">
  <name>Task 1: Compute + cross-verify ZTE (000063.SZ) against 2023 annual report</name>
  <files>
    stockvaluefinder/tests/golden/000063.SZ/2023/expected_metrics.yaml,
    stockvaluefinder/tests/golden/000063.SZ/2023/provenance.md
  </files>
  <read_first>
    stockvaluefinder/tests/golden/000063.SZ/2023/expected_metrics.yaml,
    stockvaluefinder/tests/golden/600519.SH/2023/provenance.md (template),
    stockvaluefinder/tests/golden/601398.SH/2023/provenance.md (post-24-01, as sibling example for financial-vs-non-financial framing)
  </read_first>
  <behavior>
    - Run generalized compute_golden_values.py against 000063.SZ
    - Cross-verify P0 metrics against ZTE's 2023 annual report
    - Document tech-sector specifics (R&D capitalization treatment, long-cycle contract revenue) in provenance.md
    - Update expected_metrics.yaml and provenance.md to VERIFIED state
  </behavior>
  <action>
    1. Populate expected_metrics.yaml from frozen AKShare data:
       ```bash
       cd stockvaluefinder
       uv run python tests/golden/compute_golden_values.py --ticker 000063.SZ --year 2023
       ```
       Confirm output: 14 metrics populated (non-null), 12 metrics intentionally null.
    2. Download ZTE 2023 annual report PDF:
       - CNINFO: http://www.cninfo.com.cn (search 中兴通讯 000063 → 2023年年度报告)
       - Or ZTE IR page: https://ir.zte.com.cn → Financial Reports
    3. Cross-verify each P0 metric against the annual report (same procedure as 24-01 Task 2). Tech-sector specifics to watch:
       - **R&D expense (研发费用)**: ZTE capitalizes a portion of R&D as 开发支出 on balance sheet. Confirm AKShare reports R&D consistently (whether expensed or capitalized) and that the SGAI sub-index treats it correctly.
       - **Revenue recognition**: Long-cycle contracts (5G base stations, infrastructure) may use percentage-of-completion. Confirm TOTAL_OPERATE_INCOME matches the annual report's revenue line.
       - **NOPAT (non-financial branch)**: TOTAL_PROFIT + FINANCE_EXPENSE × (1 - tax_rate). FINANCE_EXPENSE for ZTE is meaningful (it has bonds outstanding) — confirm the sign convention (negative = income, positive = expense in AKShare).
    4. For each P0 metric, fill in `source_page` in expected_metrics.yaml.
    5. Set top-level fields:
       ```yaml
       source: frozen_akshare_verified
       verified_date: '2026-MM-DD'
       verified_by: '{your name} (annual report cross-check)'
       ```
    6. Update provenance.md:
       - Status: VERIFIED on YYYY-MM-DD
       - Raw financial data table with page citations
       - Tech-sector specifics section: how R&D is treated, any percentage-of-completion revenue notes
       - Discrepancies section listing any AKShare-vs-annual-report differences
    7. Run `pytest -m golden`: should still pass (manifest still l3_verified=false; flip in Task 3).
  </action>
  <verify>
    <automated>grep -c "verified_date: '2026" stockvaluefinder/tests/golden/000063.SZ/2023/expected_metrics.yaml && grep -c "Page\|p\." stockvaluefinder/tests/golden/000063.SZ/2023/provenance.md && grep -c "VERIFIED" stockvaluefinder/tests/golden/000063.SZ/2023/provenance.md</automated>
    <manual>R&D capitalization treatment and revenue recognition explicitly noted in provenance.md.</manual>
  </verify>
  <done>
    - 000063.SZ expected_metrics.yaml fully populated and verified against annual report
    - provenance.md status VERIFIED with tech-sector notes
    - No regression in pytest -m golden
  </done>
</task>

<task type="manual" tdd="false">
  <name>Task 2: Compute + cross-verify Vanke (000002.SZ) against 2023 annual report</name>
  <files>
    stockvaluefinder/tests/golden/000002.SZ/2023/expected_metrics.yaml,
    stockvaluefinder/tests/golden/000002.SZ/2023/provenance.md
  </files>
  <read_first>
    stockvaluefinder/tests/golden/000002.SZ/2023/expected_metrics.yaml,
    stockvaluefinder/tests/golden/600519.SH/2023/provenance.md (template)
  </read_first>
  <behavior>
    - Run generalized compute_golden_values.py against 000002.SZ
    - Cross-verify P0 metrics against Vanke's 2023 annual report
    - Document real-estate specifics (high leverage, presale revenue, project completion) in provenance.md
    - Update expected_metrics.yaml and provenance.md to VERIFIED state
  </behavior>
  <action>
    1. Populate expected_metrics.yaml from frozen AKShare:
       ```bash
       cd stockvaluefinder
       uv run python tests/golden/compute_golden_values.py --ticker 000002.SZ --year 2023
       ```
    2. Download Vanke 2023 annual report PDF (CNINFO: 万科A 000002 → 2023年年度报告).
    3. Cross-verify each P0 metric. Real-estate specifics to watch:
       - **LVGI sub-index of M-Score**: Vanke's leverage is high (real estate developers carry significant interest-bearing debt). LVGI should likely be > 1.0 reflecting leverage change FY2022→FY2023. Verify the underlying debt-to-equity ratio against the balance sheet.
       - **Presale revenue (合同负债)**: Vanke books cash from presales as 合同负债 (contract liabilities) until project completion. Confirm AKShare's TOTAL_OPERATE_INCOME matches the annual report's recognized revenue (not presale cash).
       - **Goodwill ratio**: Should likely be 0 or very small for Vanke (organic developer, few M&A). Verify against balance sheet's 商誉 line.
       - **NOPAT (non-financial branch)**: TOTAL_PROFIT + FINANCE_EXPENSE × (1 - tax_rate). FINANCE_EXPENSE for Vanke is large (interest on construction debt). Verify against income statement.
       - **存贷双高 detection**: Vanke historically had both high cash and high debt. Confirm the boolean flag matches what the balance sheet shows.
    4. Fill in source_page citations for every P0 metric.
    5. Update expected_metrics.yaml top-level: source=frozen_akshare_verified, verified_date, verified_by.
    6. Update provenance.md:
       - Status VERIFIED
       - Raw financial data table with page citations
       - Real-estate specifics section: how presale revenue is treated, leverage observations, any 存贷双高 commentary
       - Discrepancies section
    7. Run `pytest -m golden`: should still pass.
  </action>
  <verify>
    <automated>grep -c "verified_date: '2026" stockvaluefinder/tests/golden/000002.SZ/2023/expected_metrics.yaml && grep -c "Page\|p\." stockvaluefinder/tests/golden/000002.SZ/2023/provenance.md && grep -c "VERIFIED" stockvaluefinder/tests/golden/000002.SZ/2023/provenance.md</automated>
    <manual>Real-estate specifics (presale revenue, high leverage, 存贷双高 status) explicitly noted in provenance.md.</manual>
  </verify>
  <done>
    - 000002.SZ expected_metrics.yaml fully populated and verified
    - provenance.md status VERIFIED with real-estate sector notes
    - No regression in pytest -m golden
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Flip manifest.yaml + verify expanded golden test suite + final P0 reconcile checks</name>
  <files>
    stockvaluefinder/tests/golden/manifest.yaml
  </files>
  <read_first>
    stockvaluefinder/tests/golden/manifest.yaml,
    stockvaluefinder/tests/golden/test_l3_golden.py
  </read_first>
  <behavior>
    - 000063.SZ and 000002.SZ entries flip to l3_verified: true and provenance: frozen_akshare_verified
    - pytest -m golden parametrizes over 5 stocks (Moutai + ICBC + Ping An + ZTE + Vanke)
    - All golden tests pass
    - reconcile CLI reports p0_all_pass=True for both newly verified stocks
    - Phase 24 closes audit gaps for all 4 in-scope tickers
  </behavior>
  <action>
    1. Edit `stockvaluefinder/tests/golden/manifest.yaml`:
       - 000063.SZ: `l3_verified: true`, `provenance: frozen_akshare_verified`
       - 000002.SZ: `l3_verified: true`, `provenance: frozen_akshare_verified`
       - Update `notes:` for each with verification date.
    2. Run the L3 golden suite (now expecting 5 stocks):
       ```bash
       cd stockvaluefinder && DATABASE_URL=postgresql+asyncpg://x/y uv run pytest -m golden --override-ini="addopts=" --no-header -v
       ```
       Test count grows to roughly 110 (5 × ~22 per stock). All must pass.
    3. Reconcile CLI checks:
       ```bash
       cd stockvaluefinder
       for T in 000063.SZ 000002.SZ; do
         DATABASE_URL=postgresql+asyncpg://x/y uv run python -m stockvaluefinder.tools.reconcile --ticker $T --year 2023 --json | \
           python3 -c "import sys, json; d = json.load(sys.stdin); assert d['p0_all_pass'], (T, d); print(f'$T: P0', d['summary'])"
       done
       ```
       Both must return p0_all_pass=True. Failure here means either a calculate_* bug on the non-financial branch (file an issue) or a verifier transcription error (re-check Tasks 1/2).
    4. Final cross-check: confirm 5 stocks total now verified.
       ```bash
       grep -c "l3_verified: true" stockvaluefinder/tests/golden/manifest.yaml
       ```
       Expected output: 5
  </action>
  <verify>
    <automated>cd stockvaluefinder && grep -c "l3_verified: true" tests/golden/manifest.yaml && DATABASE_URL=postgresql+asyncpg://x/y uv run pytest -m golden --override-ini="addopts=" --no-header -q 2>&1 | grep -E "passed|failed"</automated>
  </verify>
  <done>
    - manifest.yaml has 5 stocks with l3_verified: true
    - pytest -m golden passes with 5-stock parametrization
    - reconcile p0_all_pass=True for 000063.SZ and 000002.SZ
    - Phase 24 fully closes audit gaps for GOLD-02, GOLD-03, LV3-01, LV3-02 across all 4 in-scope tickers (raises coverage from 1/14 → 5/14)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same as 24-01.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | expected_metrics.yaml | mitigate | Provenance.md page citations |
| T-24-02-02 | Bug | calculate_nopat non-financial branch | detect | Cross-verification surfaces NOPAT formula bugs (especially the FINANCE_EXPENSE sign convention which is a known footgun) |
| T-24-02-03 | Bug | LVGI sub-index calculation | detect | Vanke is the highest-leverage stock in the dataset; if LVGI computes wrong it will show up here |
</threat_model>

<verification>
1. `tests/golden/000063.SZ/2023/expected_metrics.yaml` has source=frozen_akshare_verified, page citations on P0
2. `tests/golden/000002.SZ/2023/expected_metrics.yaml` same
3. Both provenance.md files have status VERIFIED with sector-specific notes
4. `grep -c "l3_verified: true" manifest.yaml` returns 5
5. `pytest -m golden` passes with all 5 stocks parametrized
6. `reconcile --ticker 000063.SZ --year 2023 --json` returns p0_all_pass=True
7. `reconcile --ticker 000002.SZ --year 2023 --json` returns p0_all_pass=True
</verification>

<success_criteria>
- 000063.SZ (ZTE, technology) golden values populated, cross-verified, l3_verified=true
- 000002.SZ (Vanke, real estate) golden values populated, cross-verified, l3_verified=true
- Tech-sector R&D and real-estate presale-revenue specifics documented in provenance.md
- Non-financial NOPAT branch exercised (with FINANCE_EXPENSE adjustment confirmed for both stocks)
- Vanke verification stress-tests the LVGI sub-index on a known high-leverage issuer
- L3 golden test suite passes with 5 verified stocks (Moutai + ICBC + Ping An + ZTE + Vanke)
- All 4 in-scope audit gaps closed for Phase 24
</success_criteria>

<output>
After completion, create `.planning/phases/24-golden-dataset-expansion/24-02-SUMMARY.md`
</output>
