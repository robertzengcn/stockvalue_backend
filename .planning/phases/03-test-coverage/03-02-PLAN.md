---
phase: 03-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - stockvaluefinder/tests/unit/test_services/test_valuation_service.py
  - stockvaluefinder/tests/unit/test_services/test_yield_service.py
autonomous: true
requirements: [TEST-02, TEST-03]

must_haves:
  truths:
    - "analyze_dcf_valuation orchestrator returns ValuationResult with all fields populated for known inputs"
    - "DCFValuationService.analyze delegates to analyze_dcf_valuation and returns identical results"
    - "determine_valuation_level correctly classifies UNDERVALUED/FAIR_VALUE/OVERVALUED at boundary margins"
    - "analyze_yield_gap orchestrator returns YieldGap with correct net yield, yield gap, and recommendation"
    - "YieldAnalyzer.analyze delegates to analyze_yield_gap and returns identical results"
  artifacts:
    - path: "stockvaluefinder/tests/unit/test_services/test_valuation_service.py"
      provides: "Extended valuation service tests including orchestrator and service class"
      min_lines: 300
    - path: "stockvaluefinder/tests/unit/test_services/test_yield_service.py"
      provides: "Extended yield service tests including orchestrator and service class"
      min_lines: 200
  key_links:
    - from: "test_valuation_service.py"
      to: "stockvaluefinder/stockvaluefinder/services/valuation_service.py"
      via: "direct function imports and assertion on ValuationResult fields"
      pattern: "analyze_dcf_valuation"
    - from: "test_yield_service.py"
      to: "stockvaluefinder/stockvaluefinder/services/yield_service.py"
      via: "direct function imports and assertion on YieldGap fields"
      pattern: "analyze_yield_gap"
---

<objective>
Extend valuation_service and yield_service tests to achieve 80%+ coverage for both.

Purpose: Valuation has 25% coverage (45 statements uncovered) and yield has 41% (16 statements uncovered). The main gaps are the orchestrator functions (analyze_dcf_valuation, analyze_yield_gap) and service class wrappers (DCFValuationService, YieldAnalyzer) which have zero test coverage.

Output: Extended test files for both services with tests for orchestrators, service classes, and boundary conditions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-test-coverage/03-CONTEXT.md
@stockvaluefinder/stockvaluefinder/services/valuation_service.py
@stockvaluefinder/stockvaluefinder/services/yield_service.py
@stockvaluefinder/tests/unit/test_services/test_valuation_service.py
@stockvaluefinder/tests/unit/test_services/test_yield_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend valuation_service tests for orchestrator and service class</name>
  <files>stockvaluefinder/tests/unit/test_services/test_valuation_service.py</files>
  <read_first>
    stockvaluefinder/tests/unit/test_services/test_valuation_service.py
    stockvaluefinder/stockvaluefinder/services/valuation_service.py
    stockvaluefinder/stockvaluefinder/models/valuation.py
  </read_first>
  <behavior>
    - analyze_dcf_valuation with Rf=0.03, beta=1.0, ERP=0.06, base_fcf=100000, shares=1000 returns ValuationResult with wacc=0.09, intrinsic_value computed correctly, margin_of_safety computed correctly, valuation_level determined correctly
    - analyze_dcf_valuation produces audit_trail with wacc, fcf_projections, present_values, terminal_value, intrinsic_value, margin_of_safety entries
    - DCFValuationService.analyze returns same result as analyze_dcf_valuation for identical inputs
    - determine_valuation_level(0.30) == UNDERVALUED (exactly at threshold)
    - determine_valuation_level(0.2999) == FAIR_VALUE (just below threshold)
    - determine_valuation_level(-0.30) == OVERVALUED (exactly at threshold)
    - determine_valuation_level(-0.2999) == FAIR_VALUE (just above threshold)
  </behavior>
  <action>
Per D-01 (valuation_service second priority) and D-03 (extend only):

Add the following NEW test classes to the END of test_valuation_service.py. Do NOT modify any existing tests.

Add imports at the top: `from stockvaluefinder.services.valuation_service import analyze_dcf_valuation, determine_valuation_level, DCFValuationService` and `from stockvaluefinder.models.valuation import DCFParams` and `from decimal import Decimal` and `from uuid import uuid4`.

1. **TestDetermineValuationLevel** class with tests:
   - `test_undervalued_at_30_percent`: determine_valuation_level(0.30) == ValuationLevel.UNDERVALUED
   - `test_undervalued_above_30_percent`: determine_valuation_level(0.50) == ValuationLevel.UNDERVALUED
   - `test_fair_value_between_thresholds`: determine_valuation_level(0.10) == ValuationLevel.FAIR_VALUE
   - `test_overvalued_at_negative_30_percent`: determine_valuation_level(-0.30) == ValuationLevel.OVERVALUED
   - `test_overvalued_below_negative_30_percent`: determine_valuation_level(-0.50) == ValuationLevel.OVERVALUED
   - `test_boundary_just_below_30_percent`: determine_valuation_level(0.2999) == ValuationLevel.FAIR_VALUE
   - `test_boundary_just_above_negative_30_percent`: determine_valuation_level(-0.2999) == ValuationLevel.FAIR_VALUE

2. **TestAnalyzeDCFValuation** class with tests:
   - `test_full_dcf_analysis_with_known_inputs`: Create DCFParams with risk_free_rate=0.03, beta=1.0, market_risk_premium=0.06, growth_rate_stage1=0.08, years_stage1=5, growth_rate_stage2=0.04, years_stage2=5, terminal_growth=0.02. Call analyze_dcf_valuation(ticker="600519.SH", current_price=Decimal("1800"), base_fcf=58150000000, shares_outstanding=1256197900, dcf_params=params, valuation_id=uuid4()). Assert ValuationResult fields: wacc==0.09, intrinsic_value is a positive Decimal, margin_of_safety is a float, valuation_level is a ValuationLevel enum, audit_trail is a dict with keys: wacc, fcf_projections (list of 10 entries), present_values (2 entries), terminal_value, intrinsic_value, margin_of_safety.
   - `test_dcf_analysis_audit_trail_structure`: Same call as above, verify audit_trail["fcf_projections"] has 10 entries (5 stage1 + 5 stage2), each with stage, year, growth_rate, fcf keys. Verify audit_trail["present_values"] has entries for "explicit_period" and "terminal_value".
   - `test_dcf_analysis_zero_growth`: Create DCFParams with growth_rate_stage1=0.0, growth_rate_stage2=0.0, terminal_growth=0.0. Verify result is computed without errors.
   - `test_dcf_analysis_overvalued_stock`: Create params where intrinsic value < current price. Verify margin_of_safety < 0 and valuation_level is OVERVALUED.

3. **TestDCFValuationService** class with tests:
   - `test_service_analyze_delegates_to_function`: Create DCFValuationService instance, call analyze with same params as TestAnalyzeDCFValuation.test_full_dcf_analysis_with_known_inputs. Assert the result is identical (same intrinsic_value, wacc, margin_of_safety).

Note: Import ValuationLevel from stockvaluefinder.models.enums.
  </action>
  <verify>
    <automated>uv run pytest stockvaluefinder/tests/unit/test_services/test_valuation_service.py -x -q --tb=short 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - test_valuation_service.py contains TestDetermineValuationLevel with 7 tests
    - test_valuation_service.py contains TestAnalyzeDCFValuation with 4 tests
    - test_valuation_service.py contains TestDCFValuationService with 1 test
    - All existing tests still pass
    - valuation_service.py coverage is 80%+
  </done>
  <acceptance_criteria>
    - test_valuation_service.py contains `class TestDetermineValuationLevel`
    - test_valuation_service.py contains `class TestAnalyzeDCFValuation`
    - test_valuation_service.py contains `class TestDCFValuationService`
    - test_valuation_service.py contains `test_full_dcf_analysis_with_known_inputs`
    - test_valuation_service.py contains `test_service_analyze_delegates_to_function`
    - `uv run pytest stockvaluefinder/tests/unit/test_services/test_valuation_service.py -x` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend yield_service tests for orchestrator and service class</name>
  <files>stockvaluefinder/tests/unit/test_services/test_yield_service.py</files>
  <read_first>
    stockvaluefinder/tests/unit/test_services/test_yield_service.py
    stockvaluefinder/stockvaluefinder/services/yield_service.py
    stockvaluefinder/stockvaluefinder/models/yield_gap.py
  </read_first>
  <behavior>
    - analyze_yield_gap with A_SHARE market returns YieldGap with net_dividend_yield == gross_dividend_yield (no tax)
    - analyze_yield_gap with HK_SHARE market returns YieldGap with net_dividend_yield == gross_dividend_yield * 0.80 (20% tax)
    - analyze_yield_gap with positive yield gap (> 2%) returns ATTRACTIVE recommendation
    - analyze_yield_gap with negative yield gap (< -1%) returns UNATTRACTIVE recommendation
    - YieldAnalyzer.analyze returns same result as analyze_yield_gap for identical inputs
    - analyze_yield_gap populates all YieldGap fields: ticker, cost_basis, current_price, gross_dividend_yield, net_dividend_yield, risk_free_bond_rate, risk_free_deposit_rate, yield_gap, recommendation, market, analysis_id
  </behavior>
  <action>
Per D-01 (yield_service third priority) and D-03 (extend only):

Add the following NEW test classes to the END of test_yield_service.py. Do NOT modify any existing tests.

Add imports: `from stockvaluefinder.services.yield_service import analyze_yield_gap, YieldAnalyzer` and `from decimal import Decimal` and `from uuid import uuid4`.

1. **TestAnalyzeYieldGap** class with tests:
   - `test_full_analysis_a_share`: Call analyze_yield_gap(ticker="600519.SH", cost_basis=Decimal("1800"), current_price=Decimal("1850"), gross_dividend_yield=0.022, risk_free_bond_rate=0.028, risk_free_deposit_rate=0.025, market=Market.A_SHARE, analysis_id=uuid4()). Assert: net_dividend_yield == 0.022 (no tax), yield_gap = 0.022 - max(0.028, 0.025) = 0.022 - 0.028 = -0.006, recommendation == NEUTRAL. Assert all YieldGap fields are populated.
   - `test_full_analysis_hk_share`: Same but with Market.HK_SHARE. Assert: net_dividend_yield == 0.022 * 0.80 = 0.0176, yield_gap = 0.0176 - 0.028 = -0.0104.
   - `test_analysis_attractive_recommendation`: Use gross_dividend_yield=0.06, bond_rate=0.03, deposit_rate=0.025. Net yield = 0.06, yield_gap = 0.06 - 0.03 = 0.03 > 0.02 -> ATTRACTIVE.
   - `test_analysis_unattractive_recommendation`: Use gross_dividend_yield=0.01, bond_rate=0.04, deposit_rate=0.035. Net yield = 0.01, yield_gap = 0.01 - 0.04 = -0.03 < -0.01 -> UNATTRACTIVE.
   - `test_analysis_populates_all_fields`: Verify YieldGap has: ticker, cost_basis, current_price, gross_dividend_yield, net_dividend_yield, risk_free_bond_rate, risk_free_deposit_rate, yield_gap, recommendation, market, analysis_id, calculated_at.

2. **TestYieldAnalyzer** class with tests:
   - `test_analyzer_delegates_to_function`: Create YieldAnalyzer instance, call analyze with same params as test_full_analysis_a_share. Assert result is identical to calling analyze_yield_gap directly.

All tests use Market from stockvaluefinder.models.enums.
  </action>
  <verify>
    <automated>uv run pytest stockvaluefinder/tests/unit/test_services/test_yield_service.py -x -q --tb=short 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - test_yield_service.py contains TestAnalyzeYieldGap with 5 tests
    - test_yield_service.py contains TestYieldAnalyzer with 1 test
    - All existing tests still pass
    - yield_service.py coverage is 80%+
  </done>
  <acceptance_criteria>
    - test_yield_service.py contains `class TestAnalyzeYieldGap`
    - test_yield_service.py contains `class TestYieldAnalyzer`
    - test_yield_service.py contains `test_full_analysis_a_share`
    - test_yield_service.py contains `test_full_analysis_hk_share`
    - test_yield_service.py contains `test_analyzer_delegates_to_function`
    - `uv run pytest stockvaluefinder/tests/unit/test_services/test_yield_service.py -x` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test data -> valuation/yield service | Pure function inputs; no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | S | Test fixtures | accept | Synthetic data only |
</threat_model>

<verification>
- `uv run pytest stockvaluefinder/tests/unit/test_services/test_valuation_service.py stockvaluefinder/tests/unit/test_services/test_yield_service.py -x`
- `uv run pytest --cov=stockvaluefinder.services.valuation_service --cov=stockvaluefinder.services.yield_service --cov-report=term-missing`
</verification>

<success_criteria>
- valuation_service.py has 80%+ test coverage
- yield_service.py has 80%+ test coverage
- All 18+ new tests pass alongside all existing tests
- No lint or type errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-02-SUMMARY.md`
</output>
