---
phase: 03-test-coverage
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - stockvaluefinder/tests/unit/test_utils/test_validators.py
  - stockvaluefinder/tests/unit/test_utils/test_cache_utils.py
  - stockvaluefinder/tests/unit/test_services/test_narrative_service.py
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "All 6 validator functions reject invalid inputs and accept valid inputs"
    - "validate_ticker_format normalizes lowercase to uppercase"
    - "validate_positive_decimal rejects negative values and non-numeric strings"
    - "validate_percentage enforces configurable min/max bounds"
    - "validate_chinese_name accepts Chinese characters and rejects empty strings"
    - "CacheManager methods (connect, disconnect, get, set, delete, exists, clear, delete_by_pattern) all tested with mocked Redis"
    - "NarrativeService.generate_dcf_explanation tested with mocked LLM (per D-06)"
    - "Error classes (DataValidationError, CalculationError, ExternalAPIError, CacheError) store details correctly"
  artifacts:
    - path: "stockvaluefinder/tests/unit/test_utils/test_validators.py"
      provides: "Complete validator test coverage"
      min_lines: 150
    - path: "stockvaluefinder/tests/unit/test_utils/test_cache_utils.py"
      provides: "Extended cache manager method tests"
      min_lines: 250
    - path: "stockvaluefinder/tests/unit/test_services/test_narrative_service.py"
      provides: "Extended narrative service tests including generate_dcf_explanation"
      min_lines: 200
  key_links:
    - from: "test_validators.py"
      to: "stockvaluefinder/stockvaluefinder/utils/validators.py"
      via: "Direct function calls with known inputs"
      pattern: "validate_ticker_format"
---

<objective>
Create validator tests, extend cache and narrative tests to achieve 80%+ coverage.

Purpose: validators.py has 0% coverage (42 statements), cache.py has 16% (143 statements), narrative_service.py has 21% (73 statements), errors.py has 34% (25 statements). These are utility and supporting modules needed per D-02 (full coverage push).

Output: New test_validators.py; extended test_cache_utils.py; extended test_narrative_service.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-test-coverage/03-CONTEXT.md
@stockvaluefinder/stockvaluefinder/utils/validators.py
@stockvaluefinder/stockvaluefinder/utils/cache.py
@stockvaluefinder/stockvaluefinder/utils/errors.py
@stockvaluefinder/stockvaluefinder/services/narrative_service.py
@stockvaluefinder/tests/unit/test_utils/test_cache_utils.py
@stockvaluefinder/tests/unit/test_services/test_narrative_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_validators.py and extend test_cache_utils.py</name>
  <files>stockvaluefinder/tests/unit/test_utils/test_validators.py, stockvaluefinder/tests/unit/test_utils/test_cache_utils.py</files>
  <read_first>
    stockvaluefinder/stockvaluefinder/utils/validators.py
    stockvaluefinder/stockvaluefinder/utils/cache.py
    stockvaluefinder/stockvaluefinder/utils/errors.py
    stockvaluefinder/tests/unit/test_utils/test_cache_utils.py
  </read_first>
  <behavior>
    Validators (test_validators.py - NEW file):
    - validate_ticker_format("600519.sh") == "600519.SH" (normalizes case)
    - validate_ticker_format("600519.SH") == "600519.SH" (already uppercase)
    - validate_ticker_format("000002.SZ") == "000002.SZ" (Shenzhen market)
    - validate_ticker_format("0700.HK") == "0700.HK" (HK market)
    - validate_ticker_format("INVALID") raises ValueError with "Invalid ticker format"
    - validate_ticker_format("12345.SH") raises ValueError (5 digits)
    - validate_ticker_format("600519.XX") raises ValueError (invalid market)
    - validate_market_enum("A_SHARE") == Market.A_SHARE
    - validate_market_enum(Market.HK_SHARE) == Market.HK_SHARE (passthrough)
    - validate_market_enum("INVALID") raises ValueError
    - validate_positive_decimal(1.5) == Decimal("1.5")
    - validate_positive_decimal(0) == Decimal("0") (zero is accepted, >= 0)
    - validate_positive_decimal(-1.0) raises ValueError with "must be positive"
    - validate_positive_decimal("abc") raises ValueError with "valid number"
    - validate_percentage(0.05) == Decimal("0.05") (within 0-1 range)
    - validate_percentage(1.5) raises ValueError (above max 1.0)
    - validate_percentage(-0.1) raises ValueError (below min 0.0)
    - validate_percentage(0.5, min_value=0.0, max_value=0.5) == Decimal("0.5") (boundary)
    - validate_chinese_name("贵州茅台") == "贵州茅台"
    - validate_chinese_name("ABC Corp") == "ABC Corp"
    - validate_chinese_name("") raises ValueError with "cannot be empty"
    - validate_chinese_name("test@#$") raises ValueError with "Invalid name"
    - validate_rate(0.0235) == Decimal("0.0235") (within 0-20% range)
    - validate_rate(0.25) raises ValueError (above 20%)

    Cache (extend test_cache_utils.py):
    - CacheManager.connect with mock Redis ping succeeds and sets _connected=True
    - CacheManager.disconnect closes Redis and sets _connected=False
    - CacheManager.get returns deserialized JSON from Redis
    - CacheManager.get with None value returns None (key not found)
    - CacheManager.set with TTL calls redis.setex
    - CacheManager.set without TTL calls redis.set
    - CacheManager.delete returns True when key exists
    - CacheManager.delete returns False when key does not exist
    - CacheManager.exists returns True when key exists
    - CacheManager.exists returns False when key does not exist
    - CacheManager.clear calls redis.flushdb
    - CacheManager.delete_by_pattern with matching keys returns count
    - CacheManager.delete_by_pattern with no matching keys returns 0
    - CacheManager.redis property raises CacheError when not connected
  </action>
  <action>
Per D-02 (full coverage push including utils) and D-03 (extend only for cache):

Part A - Create NEW file stockvaluefinder/tests/unit/test_utils/test_validators.py:

```python
"""Unit tests for validators module."""

import pytest
from decimal import Decimal

from stockvaluefinder.models.enums import Market
from stockvaluefinder.utils.validators import (
    validate_ticker_format,
    validate_market_enum,
    validate_positive_decimal,
    validate_percentage,
    validate_chinese_name,
    validate_rate,
)
```

Create test classes:
1. **TestValidateTickerFormat** - 7 tests (valid SH/SZ/HK, case normalization, invalid format/5-digit/bad-market)
2. **TestValidateMarketEnum** - 4 tests (string A_SHARE, enum passthrough HK_SHARE, invalid string, case-sensitive)
3. **TestValidatePositiveDecimal** - 5 tests (valid float, zero, negative raises, non-numeric string raises, Decimal input)
4. **TestValidatePercentage** - 5 tests (valid 0.05, above max 1.0, below min 0.0, custom min/max, boundary value)
5. **TestValidateChineseName** - 5 tests (Chinese chars, English, empty raises, special chars raises, whitespace stripped)
6. **TestValidateRate** - 3 tests (valid 0.0235, above 20% raises, zero accepted)

Part B - Extend test_cache_utils.py:

Add NEW test class **TestCacheManagerMethods** to the END of the file. Do NOT modify existing tests.

Create tests for each CacheManager method using the established pattern (mock AsyncMock Redis, set cache._redis, set cache._connected=True):

1. `test_connect_success`: Create CacheManager, mock ConnectionPool.from_url to return mock pool, mock Redis to return mock with ping returning awaitable True. Call connect(). Assert _connected=True.
2. `test_disconnect_closes_redis`: Set _connected=True, _redis=AsyncMock. Call disconnect(). Assert _redis.close called, _connected=False.
3. `test_get_returns_deserialized_json`: Mock redis.get to return json.dumps({"key": "value"}). Call get("test_key"). Assert returns {"key": "value"}.
4. `test_get_returns_none_for_missing_key`: Mock redis.get to return None. Call get("missing"). Assert returns None.
5. `test_set_with_ttl_calls_setex`: Mock redis.setex. Call set("key", {"data": 1}, ttl=60). Assert redis.setex called with key, 60, json string.
6. `test_set_without_ttl_calls_set`: Mock redis.set. Call set("key", {"data": 1}). Assert redis.set called.
7. `test_delete_existing_key`: Mock redis.delete to return 1. Call delete("key"). Assert returns True.
8. `test_delete_nonexistent_key`: Mock redis.delete to return 0. Call delete("key"). Assert returns False.
9. `test_exists_returns_true`: Mock redis.exists to return 1. Call exists("key"). Assert returns True.
10. `test_exists_returns_false`: Mock redis.exists to return 0. Call exists("key"). Assert returns False.
11. `test_clear_calls_flushdb`: Mock redis.flushdb. Call clear(). Assert redis.flushdb called.
12. `test_delete_by_pattern_with_matches`: Mock redis.scan_iter to yield keys ["key1", "key2"]. Mock redis.delete to return 2. Call delete_by_pattern("test:*"). Assert returns 2.
13. `test_delete_by_pattern_no_matches`: Mock redis.scan_iter to yield nothing. Call delete_by_pattern("test:*"). Assert returns 0.
14. `test_redis_property_raises_when_not_connected`: Access cache.redis when _connected=False. Assert raises CacheError.

Import CacheError from stockvaluefinder.utils.errors.
  </action>
  <verify>
    <automated>uv run pytest stockvaluefinder/tests/unit/test_utils/ -x -q --tb=short 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - test_validators.py exists with 6 test classes covering all 6 validator functions
    - test_cache_utils.py has TestCacheManagerMethods with 14 new tests
    - validators.py coverage is 80%+
    - cache.py coverage significantly improved
    - All existing tests still pass
  </done>
  <acceptance_criteria>
    - stockvaluefinder/tests/unit/test_utils/test_validators.py exists
    - test_validators.py contains `class TestValidateTickerFormat`
    - test_validators.py contains `class TestValidateChineseName`
    - test_cache_utils.py contains `class TestCacheManagerMethods`
    - test_cache_utils.py contains `test_connect_success`
    - test_cache_utils.py contains `test_redis_property_raises_when_not_connected`
    - `uv run pytest stockvaluefinder/tests/unit/test_utils/ -x` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend narrative_service tests for generate_dcf_explanation and error handling</name>
  <files>stockvaluefinder/tests/unit/test_services/test_narrative_service.py</files>
  <read_first>
    stockvaluefinder/tests/unit/test_services/test_narrative_service.py
    stockvaluefinder/stockvaluefinder/services/narrative_service.py
  </read_first>
  <behavior>
    - generate_dcf_explanation with mocked LLM returns DCFExplanation with all 6 fields populated
    - generate_dcf_explanation returns None when LLM is None
    - generate_dcf_explanation returns None on unparseable response
    - generate_dcf_explanation returns None on exception
    - _safe_json_parse handles valid JSON, invalid JSON, and non-dict JSON (like arrays)
  </behavior>
  <action>
Per D-06 (narrative service tested with mocked LLM responses, verify service handles responses correctly, graceful fallback to None on errors):

Add NEW test classes to the END of test_narrative_service.py. Do NOT modify existing tests.

1. **TestGenerateDCFExplanation** class:
   - `test_success_returns_dcf_explanation`: Mock LLM to return JSON with keys: step_by_step, data_inputs, wacc_explanation, fcf_analysis, reliability, conclusion. Call generate_dcf_explanation(ticker="600519.SH", result_data={"intrinsic_value": 220000, "audit_trail": {}}). Assert result is DCFExplanation with all fields matching the mock response. Assert generated_at is a datetime, llm_provider is "deepseek".
   - `test_returns_none_when_llm_none`: Set _llm=None, _llm_initialized=True. Call generate_dcf_explanation. Assert returns None.
   - `test_returns_none_on_unparseable`: Mock LLM to return "I cannot help with this." (non-JSON). Assert returns None.
   - `test_returns_none_on_exception`: Mock LLM to raise ValueError. Assert returns None.

2. **TestSafeJsonParse** class:
   - `test_valid_dict_json`: _safe_json_parse('{"key": "value"}') returns {"key": "value"}
   - `test_invalid_json`: _safe_json_parse("not json") returns None
   - `test_array_json_returns_none`: _safe_json_parse('[1, 2, 3]') returns None (not a dict)
   - `test_null_json_returns_none`: _safe_json_parse('null') returns None

Import DCFExplanation from stockvaluefinder.models.narrative.
  </action>
  <verify>
    <automated>uv run pytest stockvaluefinder/tests/unit/test_services/test_narrative_service.py -x -q --tb=short 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - test_narrative_service.py has TestGenerateDCFExplanation with 4 tests
    - test_narrative_service.py has TestSafeJsonParse with 4 tests
    - narrative_service.py coverage is 80%+
    - All existing tests still pass
  </done>
  <acceptance_criteria>
    - test_narrative_service.py contains `class TestGenerateDCFExplanation`
    - test_narrative_service.py contains `test_success_returns_dcf_explanation`
    - test_narrative_service.py contains `class TestSafeJsonParse`
    - `uv run pytest stockvaluefinder/tests/unit/test_services/test_narrative_service.py -x` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validators -> input data | Tests verify rejection of malformed inputs (V5 ASVS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | I | Error message content | accept | Error messages in tests are controlled; verify they don't leak internal details |
</threat_model>

<verification>
- `uv run pytest stockvaluefinder/tests/unit/test_utils/ stockvaluefinder/tests/unit/test_services/test_narrative_service.py -x`
- `uv run pytest --cov=stockvaluefinder.utils.validators --cov=stockvaluefinder.utils.cache --cov=stockvaluefinder.services.narrative_service --cov-report=term-missing`
</verification>

<success_criteria>
- validators.py has 80%+ test coverage
- cache.py coverage significantly improved
- narrative_service.py has 80%+ test coverage
- All 30+ new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-04-SUMMARY.md`
</output>
