---
phase: 03-test-coverage
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-05"]
files_modified:
  - stockvaluefinder/tests/integration/test_repositories.py
autonomous: true
requirements: [TEST-06]

must_haves:
  truths:
    - "BaseRepository.create persists entity to database and returns it with generated ID"
    - "BaseRepository.get_by_id retrieves entity by primary key"
    - "BaseRepository.get_all returns paginated list of entities"
    - "BaseRepository.update modifies entity fields and returns updated entity"
    - "BaseRepository.delete removes entity and returns True"
    - "Domain-specific repository methods (get_by_ticker, upsert_by_report_id) work correctly"
    - "All repository tests use test database and rollback after each test"
  artifacts:
    - path: "stockvaluefinder/tests/integration/test_repositories.py"
      provides: "Repository CRUD integration tests for all 7 repositories"
      contains: "class TestStockRepository"
      min_lines: 300
  key_links:
    - from: "test_repositories.py"
      to: "stockvaluefinder/stockvaluefinder/repositories/"
      via: "Repository instances with real async DB session"
      pattern: "Repository.*session"
---

<objective>
Create integration tests for database persistence layer (all 7 repositories).

Purpose: Per D-02 (full coverage including repositories) and D-04 (E2E with database). Repository tests verify that SQLAlchemy ORM models map correctly to database tables and CRUD operations work with real PostgreSQL. Uses test database infrastructure from Plan 05.

Output: test_repositories.py with CRUD tests for stock, financial_report, risk_score, valuation_result, yield_gap, dividend, and rate repositories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-test-coverage/03-CONTEXT.md
@stockvaluefinder/stockvaluefinder/repositories/base.py
@stockvaluefinder/stockvaluefinder/repositories/stock_repo.py
@stockvaluefinder/stockvaluefinder/repositories/financial_repo.py
@stockvaluefinder/stockvaluefinder/repositories/risk_repo.py
@stockvaluefinder/stockvaluefinder/repositories/valuation_repo.py
@stockvaluefinder/stockvaluefinder/repositories/yield_repo.py
@stockvaluefinder/stockvaluefinder/repositories/dividend_repo.py
@stockvaluefinder/stockvaluefinder/repositories/rate_repo.py
@stockvaluefinder/tests/integration/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repository integration tests for all 7 repositories</name>
  <files>stockvaluefinder/tests/integration/test_repositories.py</files>
  <read_first>
    stockvaluefinder/stockvaluefinder/repositories/base.py
    stockvaluefinder/stockvaluefinder/repositories/stock_repo.py
    stockvaluefinder/stockvaluefinder/repositories/financial_repo.py
    stockvaluefinder/stockvaluefinder/repositories/risk_repo.py
    stockvaluefinder/stockvaluefinder/repositories/valuation_repo.py
    stockvaluefinder/stockvaluefinder/repositories/yield_repo.py
    stockvaluefinder/stockvaluefinder/repositories/dividend_repo.py
    stockvaluefinder/stockvaluefinder/repositories/rate_repo.py
    stockvaluefinder/tests/integration/conftest.py
  </read_first>
  <action>
Per D-02 (all 7 repositories) and D-06 (TEST-06: database persistence):

Create stockvaluefinder/tests/integration/test_repositories.py.

First read ALL repository files to understand:
1. The exact model classes each repo uses
2. The Create/Update Pydantic schemas
3. Domain-specific methods (e.g., get_by_ticker, upsert_by_report_id)
4. The ORM model column names and types

Then create the test file:

```python
"""Integration tests for repository CRUD operations with real database."""

import pytest
from decimal import Decimal
from uuid import uuid4

from conftest import skip_if_no_db

# Import repository classes and models
# ... (read actual repository files to determine exact imports)
```

Structure:

1. **TestStockRepository** class with tests:
   - `test_create_stock`: Create StockRepository with db_session, call create with StockCreate(ticker="600519.SH", name="贵州茅台", market="A_SHARE", sector="白酒"). Assert returned entity has ticker and auto-generated ID.
   - `test_get_by_ticker`: Create a stock, then call repo.get_by_ticker("600519.SH"). Assert returns the same entity.
   - `test_get_by_ticker_not_found`: Call get_by_ticker("999999.SH"). Assert returns None.
   - `test_get_all`: Create 2 stocks, call get_all(limit=10). Assert returns list with at least 2 items.

2. **TestFinancialReportRepository** class:
   - `test_create_financial_report`: Create a financial report. Assert persisted with correct fields.
   - `test_get_by_report_id`: Create report, retrieve by report_id. Assert matches.

3. **TestRiskScoreRepository** class:
   - `test_create_risk_score`: Create risk score with RiskScoreCreate. Assert persisted.
   - `test_upsert_by_report_id`: Create a risk score, then upsert with same report_id but different m_score. Assert updated (not duplicated).

4. **TestValuationRepository** class:
   - `test_create_valuation_result`: Create valuation result. Assert persisted.
   - `test_get_by_valuation_id`: Retrieve by valuation_id. Assert matches.

5. **TestYieldGapRepository** class:
   - `test_create_yield_gap`: Create yield gap analysis. Assert persisted with correct recommendation.

6. **TestDividendRepository** class:
   - `test_create_dividend`: Create dividend record. Assert persisted.

7. **TestRateRepository** class:
   - `test_create_rate`: Create interest rate record. Assert persisted.

8. **TestBaseRepositoryCRUD** class (generic CRUD tests using one repository):
   - `test_get_by_id_returns_entity`: Create entity, get_by_id, assert matches.
   - `test_get_by_id_returns_none_for_missing`: get_by_id with non-existent ID returns None.
   - `test_update_entity`: Create entity, update a field, assert field changed.
   - `test_delete_entity`: Create entity, delete it, assert get_by_id returns None.
   - `test_delete_nonexistent_returns_false`: Delete non-existent ID returns False.

IMPORTANT:
- Read each repository file to find the exact constructor parameters and Create schema fields
- Use the `db_session` fixture from integration conftest.py
- Mark ALL tests with `@skip_if_no_db`
- Create parent entities first (e.g., stock before financial report) if foreign key constraints exist
- Each test creates its own data (no shared mutable state)
- Tests use `await session.commit()` explicitly since db_session fixture only rolls back
  </action>
  <verify>
    <automated>uv run pytest stockvaluefinder/tests/integration/test_repositories.py --co -q 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - test_repositories.py exists with tests for all 7 repositories + base CRUD
    - All tests marked with skip_if_no_db
    - At least 15 test methods covering create, read, update, delete operations
    - Tests can be collected by pytest
  </done>
  <acceptance_criteria>
    - stockvaluefinder/tests/integration/test_repositories.py exists
    - test_repositories.py contains `class TestStockRepository`
    - test_repositories.py contains `class TestRiskScoreRepository`
    - test_repositories.py contains `test_upsert_by_report_id`
    - test_repositories.py contains `class TestBaseRepositoryCRUD`
    - test_repositories.py contains `skip_if_no_db`
    - `uv run pytest stockvaluefinder/tests/integration/test_repositories.py --co -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository -> Database | Test database isolation prevents production data contamination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | I | Test data in production | mitigate | Separate stockvaluefinder_test database per D-05 |
</threat_model>

<verification>
- `uv run pytest stockvaluefinder/tests/integration/test_repositories.py --co -q`
- `uv run pytest stockvaluefinder/tests/integration/test_repositories.py -x` (when DB available)
</verification>

<success_criteria>
- test_repositories.py created with tests for all 7 repositories
- All tests marked with skip_if_no_db for graceful handling
- 15+ test methods covering CRUD and domain-specific operations
- Tests can be collected by pytest
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-06-SUMMARY.md`
</output>
