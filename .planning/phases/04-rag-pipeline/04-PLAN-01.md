---
wave: 1
depends_on: []
files_modified:
  - stockvaluefinder/pyproject.toml
  - stockvaluefinder/config.py
must_haves:
  truths:
    - pymupdf installed and importable
    - RAGConfig dataclass exists with all 14 fields
  artifacts:
    - stockvaluefinder/pyproject.toml (pymupdf dependency)
    - stockvaluefinder/config.py (RAGConfig)
  key_links:
    - RAGConfig provides defaults for all RAG modules
requirements: DATA-03 (partial), DATA-06 (partial)
---

# Phase 4 Plan 01: Dependencies & Configuration

**Goal:** Install PyMuPDF and add RAGConfig to config.py.

## Tasks

<Task id="04-01-01">
<action>Install PyMuPDF dependency by running `uv add pymupdf`</action>
<read_first>
- stockvaluefinder/pyproject.toml (current dependencies)
</read_first>
<acceptance_criteria>
- pyproject.toml contains `pymupdf >= 1.27.2` in dependencies
- uv.lock is updated after `uv add pymupdf`
- Command `uv run python -c "import fitz; print(fitz.__version__)"` exits 0
</acceptance_criteria>
<verify>
<automated>uv run python -c "import fitz; print(fitz.__version__)"</automated>
</verify>
<done>
- pyproject.toml updated with pymupdf
- uv.lock regenerated
- Import test passes
</done>
</Task>

<Task id="04-01-02">
<action>Add RAGConfig dataclass to config.py with these fields: QDRANT_URL="http://localhost:6333", QDRANT_COLLECTION="annual_reports", QDRANT_API_KEY=None, EMBEDDING_API_URL="https://openrouter.ai/api/v1/embeddings", EMBEDDING_API_KEY_ENV="OPENROUTER_API_KEY", EMBEDDING_MODEL="baai/bge-m3", EMBEDDING_DIMENSIONS=1024, EMBEDDING_BATCH_SIZE=32, CHILD_CHUNK_TOKENS=500, PARENT_CHUNK_TOKENS=2000, CHUNK_OVERLAP_TOKENS=50, SEARCH_SCORE_THRESHOLD=0.7, SEARCH_RESULT_LIMIT=10, MULTI_QUERY_COUNT=3, UPLOAD_DIR="./uploads", MAX_FILE_SIZE_MB=100. Export singleton `rag_config = RAGConfig()`</action>
<read_first>
- stockvaluefinder/config.py (existing config pattern)
</read_first>
<acceptance_criteria>
- config.py contains `@dataclass(frozen=True) class RAGConfig:`
- RAGConfig has all 14 fields listed in action with correct default values
- config.py exports `rag_config = RAGConfig()` singleton
- mypy passes: `uv run mypy stockvaluefinder/config.py` exits 0
</acceptance_criteria>
<verify>
<automated>uv run mypy stockvaluefinder/config.py</automated>
</verify>
<done>
- RAGConfig defined with all fields
- Singleton exported
- mypy passes
</done>
</Task>

## Verification

After completing this wave:
- [ ] `uv run python -c "import fitz"` exits 0
- [ ] `uv run mypy stockvaluefinder/config.py` exits 0

## Threat Model

| Threat | STRIDE | Mitigation | Implemented In |
|--------|--------|------------|----------------|
| API key exposure | Information Disclosure | Store in env var via RAGConfig, never log | Task 04-01-02 (EMBEDDING_API_KEY_ENV) |
