---
phase: 20-l2-field-mapping-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stockvaluefinder/tests/unit/test_l2/test_l2_snapshot_traceability.py
  - stockvaluefinder/pytest.ini
autonomous: true
requirements:
  - LV2-01
  - LV2-02
  - LV2-05

must_haves:
  truths:
    - "Frozen AKShare JSON snapshot tests assert key fields are non-null after _extract_akshare_* functions process them"
    - "IndexAuditDetail numerator/denominator values match the expected sub-index value when recomputed from frozen data"
    - "All L2 tests are marked @pytest.mark.l2_mapping and pass without network access"
  artifacts:
    - path: "stockvaluefinder/tests/unit/test_l2/test_l2_snapshot_traceability.py"
      provides: "L2 snapshot + traceability tests"
      min_lines: 150
    - path: "stockvaluefinder/pytest.ini"
      provides: "l2_mapping marker registration"
      contains: "l2_mapping"
  key_links:
    - from: "test_l2_snapshot_traceability.py"
      to: "stockvaluefinder/external/data_service.py"
      via: "import _coalesce_akshare_field, _extract_akshare_revenue, _extract_akshare_cost_of_goods, _extract_akshare_sga_expense, _extract_akshare_accounts_receivable"
      pattern: "from stockvaluefinder.external.data_service import"
    - from: "test_l2_snapshot_traceability.py"
      to: "stockvaluefinder/services/risk_service.py"
      via: "import calculate_mscore_indices for traceability tests"
      pattern: "from stockvaluefinder.services.risk_service import calculate_mscore_indices"
    - from: "test_l2_snapshot_traceability.py"
      to: "tests/golden/*/raw_akshare_*.json"
      via: "json.load frozen AKShare data for all 14 stocks"
      pattern: "golden.*raw_akshare"
---

<objective>
L2 mapping snapshot tests and field traceability tests.

Purpose: Verify that AKShare field extraction functions produce non-null values for all key financial fields across all 14 golden stocks (LV2-01), and that IndexAuditDetail numerator/denominator values are internally consistent with computed sub-index values for the anchor stock (LV2-02). Register the l2_mapping pytest marker (LV2-05).

Output: Test file with ~40-50 tests covering snapshot extraction and traceability, pytest.ini updated with l2_mapping marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@stockvaluefinder/tests/golden/conftest.py
@stockvaluefinder/tests/golden/manifest.yaml
@stockvaluefinder/tests/golden/600519.SH/2023/raw_akshare_income.json
@stockvaluefinder/tests/golden/600519.SH/2023/raw_akshare_balance.json
@stockvaluefinder/tests/golden/600519.SH/2023/raw_akshare_cashflow.json
@stockvaluefinder/tests/golden/600519.SH/2023/expected_metrics.yaml
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From stockvaluefinder/external/data_service.py:
```python
def _coalesce_akshare_field(record: dict[str, Any], *keys: str) -> Any | None:
    """Return first present, non-null, non-NaN value for any of *keys."""

def _akshare_field_str(record: dict[str, Any], *keys: str, default: str = "0") -> str:
    """Coalesce AKShare fields and stringify for standardized report dicts."""

def _extract_akshare_revenue(income: dict[str, Any]) -> str:
    # Keys tried: TOTAL_OPERATE_INCOME, OPERATE_INCOME, INSURANCE_INCOME

def _extract_akshare_cost_of_goods(income: dict[str, Any]) -> str:
    # Keys tried: OPERATE_COST; fallback: OPERATE_EXPENSE for financials

def _extract_akshare_sga_expense(income: dict[str, Any]) -> str:
    # Keys tried: TOTAL_OPERATE_COST

def _extract_akshare_accounts_receivable(balance: dict[str, Any]) -> str:
    # Keys tried: ACCOUNTS_RECE, PREMIUM_RECE, FINANCE_RECE, NOTE_RECE
```

From stockvaluefinder/services/risk_service.py:
```python
def calculate_mscore_indices(
    current_report: dict[str, Any],
    previous_report: dict[str, Any],
    source_name: str = "unknown",
) -> dict[str, Any]:
    # Returns dict with dsri, gmi, aqi, sgi, depi, sgai, lvgi, tata,
    # non_calculable, audit_trail (dict[str, IndexAuditDetail]), red_flags
```

From stockvaluefinder/models/risk.py:
```python
class IndexAuditDetail(BaseModel):
    model_config = {"frozen": True}
    value: float
    numerator: float
    denominator: float
    source_fields: dict[str, str]
    non_calculable: bool = False
    reason: str | None = None
```

From stockvaluefinder/validation/comparators.py:
```python
def compare_within_tolerance(expected: float, computed: float, tolerance: Tolerance) -> ComparisonResult
```

From stockvaluefinder/validation/schema.py:
```python
class Tolerance(BaseModel):
    absolute: float | None = None
    relative: float | None = None
    model_config = {"frozen": True}
```

Frozen AKShare JSON structure (tests/golden/{ticker}/{year}/raw_akshare_{statement}.json):
```json
{
  "_metadata": { "ticker": "...", "fiscal_year": ..., "source": "..." },
  "records": [ { "FIELD_NAME": value, ... } ]
}
```
Key AKShare fields for income: TOTAL_OPERATE_INCOME, OPERATE_INCOME, NETPROFIT, OPERATE_COST, TOTAL_OPERATE_COST, TOTAL_PROFIT, INCOME_TAX, OPERATE_PROFIT, FINANCE_EXPENSE
Key AKShare fields for balance: TOTAL_ASSETS, TOTAL_LIABILITIES, TOTAL_EQUITY, ACCOUNTS_RECE, TOTAL_CURRENT_ASSETS, FIXED_ASSET, MONETARYFUNDS, LONG_LOAN, GOODWILL, TOTAL_PARENT_EQUITY, SHORT_LOAN, BOND_PAYABLE, TREASURY_SHARES
Key AKShare fields for cashflow: NETCASH_OPERATE
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Register l2_mapping marker and create test scaffold with frozen data loader</name>
  <files>stockvaluefinder/pytest.ini, stockvaluefinder/tests/unit/test_l2/__init__.py, stockvaluefinder/tests/unit/test_l2/conftest.py</files>
  <action>
1. Update stockvaluefinder/pytest.ini: add `l2_mapping: L2 field mapping verification tests (frozen AKShare JSON, no network)` to the existing markers section. Keep the existing l1_formula marker.

2. Create stockvaluefinder/tests/unit/test_l2/__init__.py as an empty file.

3. Create stockvaluefinder/tests/unit/test_l2/conftest.py with session-scoped fixtures:
   - `_load_frozen_json(ticker, year, statement)` helper: reads `tests/golden/{ticker}/{year}/raw_akshare_{statement}.json`, returns `records[0]` dict. Use `import json` and `Path`. Handle NaN values by post-processing: walk the dict and replace any `float('nan')` / `float('inf')` / `float('-inf')` with `None`. Python `json.loads` will parse `NaN` as `float('nan')` since the files contain raw `NaN` tokens (not valid JSON but Python tolerates it).
   - `frozen_akshare_data` fixture: returns a callable `(ticker, year) -> dict[str, dict]` that provides `{"income": ..., "balance": ..., "cashflow": ...}` by calling the loader for each statement. Cache results in a dict keyed by `(ticker, year)` to avoid re-reading files.
   - `build_standardized_report_from_frozen(income, balance, cashflow, ticker)` helper function (NOT a fixture, a regular function): replicates the AKShare report building logic from data_service.py lines 1156-1209 using the `_extract_akshare_*` and `_coalesce_akshare_field` functions. Import them directly from `stockvaluefinder.external.data_service`. This function produces the same standardized dict that `_get_financial_report_from_akshare` produces, but from frozen JSON. Include ALL fields: ticker, report_id (uuid4), period, report_type, fiscal_year, fiscal_quarter, revenue, net_income, operating_cash_flow, gross_margin, assets_total, liabilities_total, equity_total, accounts_receivable, inventory, fixed_assets, goodwill, cash_and_equivalents, interest_bearing_debt, cost_of_goods, sga_expense, total_current_assets, ppe, long_term_debt, total_liabilities, report_source="AKShare(frozen)". For gross_margin, replicate `_calculate_gross_margin_from_akshare` logic inline using `_coalesce_akshare_field` for revenue and cost.
   - Also provide `roic_inputs_from_frozen(income, balance)` helper that returns the profit/balance dict matching the structure from `get_roic_inputs` (with TOTAL_PROFIT, FINANCE_EXPENSE, INCOME_TAX, OPERATE_PROFIT, NETPROFIT in profit dict, and TOTAL_PARENT_EQUITY, SHORT_LOAN, LONG_LOAN, BOND_PAYABLE, TREASURY_SHARES, TOTAL_ASSETS, TOTAL_LIABILITIES, MONETARYFUNDS in balance dict).

For NaN handling: The frozen files contain literal `NaN` tokens (not valid JSON). Python's `json.loads` treats these as `float('nan')`. Post-process by walking the dict and replacing any `math.isnan(v)` or `math.isinf(v)` float values with `None`.
  </action>
  <verify>
    <automated>python -c "from tests.unit.test_l2.conftest import build_standardized_report_from_frozen; print('conftest OK')" 2>&1</automated>
  </verify>
  <done>pytest.ini has l2_mapping marker. test_l2 directory exists with __init__.py and conftest.py containing frozen data loader and standardized report builder. NaN values in frozen JSON properly handled (converted to None).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: L2 snapshot tests + field traceability tests</name>
  <files>stockvaluefinder/tests/unit/test_l2/test_l2_snapshot_traceability.py</files>
  <behavior>
    Snapshot tests (LV2-01) -- ALL 14 golden stocks parametrized:
    - _extract_akshare_revenue(income) returns non-null, non-zero string
    - _extract_akshare_cost_of_goods(income) returns non-null string
    - _extract_akshare_sga_expense(income) returns non-null, non-zero string
    - _extract_akshare_accounts_receivable(balance) returns non-null string
    - _coalesce_akshare_field(income, "NETPROFIT") is not None and non-zero
    - _coalesce_akshare_field(cashflow, "NETCASH_OPERATE") is not None
    - _coalesce_akshare_field(balance, "TOTAL_ASSETS") is not None
    - _coalesce_akshare_field(balance, "TOTAL_LIABILITIES") is not None
    - _coalesce_akshare_field(balance, "MONETARYFUNDS") is not None
    - _coalesce_akshare_field(balance, "TOTAL_EQUITY") is not None
    - _coalesce_akshare_field(balance, "TOTAL_CURRENT_ASSETS") is not None
    - _coalesce_akshare_field(balance, "FIXED_ASSET") is not None
    - _coalesce_akshare_field(balance, "TOTAL_PARENT_EQUITY") is not None
    - Standardized report has all M-Score required fields as non-zero floats
    - ROIC input field TOTAL_PROFIT is present and non-zero
    - ROIC input field INCOME_TAX is present and non-zero

    Traceability tests (LV2-02) -- anchor stock 600519.SH:
    - calculate_mscore_indices returns audit_trail with all 8 index keys
    - For each non-DEPI index: abs(numerator / denominator - value) < 0.001
    - TATA: numerator equals (net_income - operating_cash_flow), denominator equals total_assets
    - SGI: numerator equals revenue_current, denominator equals revenue_previous
    - Each non-DEPI index has source_fields with at least 1 entry
    - DEPI: value=1.0, reason contains "MVP", non_calculable=False
    - Each index is finite and in reasonable range (-10, 100)
  </action>
  <action>
Create stockvaluefinder/tests/unit/test_l2/test_l2_snapshot_traceability.py with `pytest.mark.l2_mapping` applied as a module-level marker via `pytestmark = [pytest.mark.l2_mapping]`.

STRUCTURE:

1. Module-level setup:
   - Import: json, math, pathlib.Path, pytest, yaml
   - From data_service: _coalesce_akshare_field, _extract_akshare_revenue, _extract_akshare_cost_of_goods, _extract_akshare_sga_expense, _extract_akshare_accounts_receivable
   - From risk_service: calculate_mscore_indices
   - From validation: compare_within_tolerance, Tolerance
   - From test_l2.conftest: build_standardized_report_from_frozen
   - Module-level marker: `pytestmark = [pytest.mark.l2_mapping]`
   - Load manifest at module level: `GOLDEN_DIR = Path(__file__).resolve().parents[3] / "golden"`, parse manifest.yaml, extract ALL_STOCK_IDS as list of (ticker, year) tuples.
   - Helper `_load_frozen(ticker, year, statement)` reads JSON, returns records[0] with NaN->None conversion.

2. SNAPSHOT TESTS using `@pytest.mark.parametrize("ticker,year", ALL_STOCK_IDS)`:
   - `test_revenue_extraction`: income = _load_frozen(ticker, year, "income"); result = _extract_akshare_revenue(income); assert result is not None and float(result) != 0.0
   - `test_cost_of_goods_extraction`: result = _extract_akshare_cost_of_goods(income); assert result is not None (allow "0" for financials)
   - `test_sga_expense_extraction`: result = _extract_akshare_sga_expense(income); assert float(result) != 0.0
   - `test_accounts_receivable_extraction`: result = _extract_akshare_accounts_receivable(balance); assert result is not None
   - `test_netprofit_present`: result = _coalesce_akshare_field(income, "NETPROFIT"); assert result is not None and float(result) != 0.0
   - `test_netcash_operate_present`: result = _coalesce_akshare_field(cashflow, "NETCASH_OPERATE"); assert result is not None
   - `test_total_assets_present`: result = _coalesce_akshare_field(balance, "TOTAL_ASSETS"); assert result is not None
   - `test_total_liabilities_present`: result = _coalesce_akshare_field(balance, "TOTAL_LIABILITIES"); assert result is not None
   - `test_monetaryfunds_present`: result = _coalesce_akshare_field(balance, "MONETARYFUNDS"); assert result is not None
   - `test_total_equity_present`: result = _coalesce_akshare_field(balance, "TOTAL_EQUITY"); assert result is not None
   - `test_total_current_assets_present`: result = _coalesce_akshare_field(balance, "TOTAL_CURRENT_ASSETS"); assert result is not None
   - `test_fixed_asset_present`: result = _coalesce_akshare_field(balance, "FIXED_ASSET"); assert result is not None
   - `test_total_parent_equity_present`: result = _coalesce_akshare_field(balance, "TOTAL_PARENT_EQUITY"); assert result is not None
   - `test_standardized_report_mscore_fields`: report = build_standardized_report_from_frozen(income, balance, cashflow, ticker); for field in ["revenue", "net_income", "operating_cash_flow", "accounts_receivable", "cost_of_goods", "total_current_assets", "ppe", "sga_expense", "total_liabilities"]: assert float(report.get(field, 0)) != 0.0. Also assert float(report.get("assets_total", report.get("total_assets", 0))) != 0.0.
   - `test_roic_profit_fields`: assert _coalesce_akshare_field(income, "TOTAL_PROFIT") is not None and float(result) != 0.0. assert _coalesce_akshare_field(income, "INCOME_TAX") is not None.

3. TRACEABILITY TESTS (class TestMscoreIndexTraceability, not parametrized, uses 600519.SH only):
   Setup: load frozen 600519.SH 2023 income/balance/cashflow. Build standardized report for current. Create synthetic previous by multiplying all float fields by 0.95. Call `calculate_mscore_indices(current_report, previous_report, source_name="AKShare")`.

   - `test_audit_trail_has_all_8_indices`: assert set(result["audit_trail"].keys()) == {"dsri", "gmi", "aqi", "sgi", "depi", "sgai", "lvgi", "tata"}
   - `test_numerator_denominator_consistency`: for index_name in ["dsri", "gmi", "aqi", "sgi", "sgai", "lvgi", "tata"]: detail = audit_trail[index_name]; if detail.denominator != 0: assert abs(detail.numerator / detail.denominator - detail.value) < 0.001
   - `test_tata_traceability`: detail = audit_trail["tata"]; expected_numerator = float(current_report["net_income"]) - float(current_report["operating_cash_flow"]); expected_denominator = float(current_report["assets_total"]); assert abs(detail.numerator - expected_numerator) / max(abs(expected_numerator), 1) < 0.01; assert abs(detail.denominator - expected_denominator) / max(abs(expected_denominator), 1) < 0.01
   - `test_sgi_traceability`: detail = audit_trail["sgi"]; assert abs(detail.numerator - float(current_report["revenue"])) / max(abs(float(current_report["revenue"])), 1) < 0.01; assert abs(detail.denominator - float(previous_report["revenue"])) / max(abs(float(previous_report["revenue"])), 1) < 0.01
   - `test_source_fields_populated`: for index_name in ["dsri", "gmi", "aqi", "sgi", "sgai", "lvgi", "tata"]: assert len(audit_trail[index_name].source_fields) >= 1
   - `test_depi_mvp_hardcoded`: depi = audit_trail["depi"]; assert depi.value == 1.0; assert "MVP" in (depi.reason or ""); assert depi.non_calculable is False
   - `test_all_indices_finite_and_reasonable`: for index_name in ["dsri", "gmi", "aqi", "sgi", "depi", "sgai", "lvgi", "tata"]: val = result[index_name]; assert math.isfinite(val); assert -10 < val < 100
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && DATABASE_URL="postgresql+asyncpg://test:test@localhost:5432/test" uv run pytest tests/unit/test_l2/test_l2_snapshot_traceability.py -v -m l2_mapping --no-header 2>&1 | tail -30</automated>
  </verify>
  <done>All snapshot tests pass for all 14 golden stocks. Traceability tests pass for anchor stock 600519.SH. All tests marked @pytest.mark.l2_mapping. No network access needed (reads frozen JSON files only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen JSON -> test assertions | Frozen data is trusted (committed to git, verified in Phase 18). Tests verify extraction logic, not data accuracy. |
| test code -> production _extract_* functions | Tests import internal helper functions directly. If refactored, tests must update. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | S | frozen JSON files | accept | Files committed to git; tampering requires repo access |
| T-20-02 | T | _extract_* functions | accept | Tests verify output, not input authenticity |
| T-20-03 | I | NaN handling | mitigate | conftest NaN-to-None conversion prevents silent NaN propagation |
| T-20-04 | D | test isolation | accept | Tests are read-only against frozen data, no mutation possible |
</threat_model>

<verification>
1. Run L2 snapshot + traceability tests:
   `DATABASE_URL="postgresql+asyncpg://test:test@localhost:5432/test" uv run pytest tests/unit/test_l2/test_l2_snapshot_traceability.py -v -m l2_mapping`
2. Verify marker registration:
   `uv run pytest --markers | grep l2_mapping`
3. Verify existing L1 tests still pass:
   `DATABASE_URL="postgresql+asyncpg://test:test@localhost:5432/test" uv run pytest tests/unit/test_services/test_l1_risk_service.py -v -m l1_formula --no-header`
</verification>

<success_criteria>
1. All 14 golden stocks have non-null values after extraction for: revenue, NETPROFIT, NETCASH_OPERATE, TOTAL_ASSETS, TOTAL_LIABILITIES, MONETARYFUNDS, TOTAL_EQUITY, TOTAL_CURRENT_ASSETS, FIXED_ASSET, TOTAL_PARENT_EQUITY
2. Standardized report dict contains all M-Score required fields with non-zero float values for all 14 stocks
3. IndexAuditDetail for 600519.SH shows numerator/denominator consistency: ratio within 0.001 of value
4. TATA and SGI traceability: numerator and denominator match known input fields
5. DEPI is hardcoded to 1.0 with MVP reason
6. All tests marked @pytest.mark.l2_mapping pass
7. l2_mapping marker registered in pytest.ini
</success_criteria>

<output>
After completion, create `.planning/phases/20-l2-field-mapping-verification/20-01-SUMMARY.md`
</output>
