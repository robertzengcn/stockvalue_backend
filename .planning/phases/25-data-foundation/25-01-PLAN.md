---
phase: 25-data-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - stockvaluefinder/stockvaluefinder/db/models/index_constituent.py
  - stockvaluefinder/stockvaluefinder/db/models/market_scan.py
  - stockvaluefinder/stockvaluefinder/db/models/__init__.py
  - stockvaluefinder/stockvaluefinder/models/market_scanner.py
  - stockvaluefinder/stockvaluefinder/models/enums.py
  - stockvaluefinder/stockvaluefinder/market_scanner/__init__.py
  - stockvaluefinder/stockvaluefinder/market_scanner/config.py
  - stockvaluefinder/stockvaluefinder/alembic/versions/020_market_scanner_tables.py
  - stockvaluefinder/tests/unit/test_market_scanner/__init__.py
  - stockvaluefinder/tests/unit/test_market_scanner/test_config.py
  - stockvaluefinder/tests/unit/test_market_scanner/test_models.py
autonomous: true
requirements: [SCR-04, EXE-04, IDX-01, IDX-02]

must_haves:
  truths:
    - "All screening thresholds are defined in a frozen dataclass with __post_init__ validation, rejecting invalid values at instantiation time"
    - "Each scan run tracks status through pending, running, completed, and partial_failed states with count fields and JSONB error summary"
    - "Index constituent records carry effective_date and removed_date, supporting historical change tracking"
    - "Pydantic models validate all scanner domain inputs (index codes, tickers, scan types) before any data layer access"
  artifacts:
    - path: "stockvaluefinder/stockvaluefinder/market_scanner/config.py"
      provides: "MarketScannerConfig frozen dataclass with all screening thresholds"
      contains: "@dataclass(frozen=True)"
    - path: "stockvaluefinder/stockvaluefinder/db/models/index_constituent.py"
      provides: "IndexConstituentDB ORM model with effective_date, removed_date, is_active"
      contains: "class IndexConstituentDB"
    - path: "stockvaluefinder/stockvaluefinder/db/models/market_scan.py"
      provides: "MarketScanRunDB, MarketScanCandidateDB, MarketScanRuleDB ORM models"
      contains: "class MarketScanRunDB"
    - path: "stockvaluefinder/stockvaluefinder/models/market_scanner.py"
      provides: "Pydantic request/response models for scanner domain"
      contains: "class MarketScanRunCreate"
    - path: "stockvaluefinder/stockvaluefinder/alembic/versions/020_market_scanner_tables.py"
      provides: "Alembic migration creating 4 scanner tables"
      contains: "revision"
  key_links:
    - from: "stockvaluefinder/stockvaluefinder/db/models/__init__.py"
      to: "stockvaluefinder/stockvaluefinder/db/models/index_constituent.py"
      via: "import and __all__ registration"
      pattern: "from stockvaluefinder.db.models.index_constituent import"
    - from: "stockvaluefinder/stockvaluefinder/models/enums.py"
      to: "market_scanner.py"
      via: "ScanStatus, ScanType enums used in Pydantic and ORM models"
      pattern: "class ScanStatus"
    - from: "tests/unit/test_market_scanner/test_config.py"
      to: "market_scanner/config.py"
      via: "TDD: config validation tests before implementation"
      pattern: "MarketScannerConfig"
---

<objective>
Create the complete data foundation layer for the Market Index Value Scanner: ORM models, Pydantic domain models, frozen config dataclass, and Alembic migration. All work follows TDD (tests first, then implementation).

Purpose: This plan delivers the schema and validation contracts that Plan 02 repositories and future phases consume. Models define the domain vocabulary; config centralizes all thresholds (SCR-04); migration creates the physical tables.

Output: 4 new ORM models, 1 Pydantic model file, 1 config dataclass, 1 Alembic migration, 2 test files, updated enums and __init__ exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-data-foundation/25-RESEARCH.md
@.planning/phases/25-data-foundation/PATTERNS.md

<interfaces>
<!-- Existing patterns the executor must follow. Extracted from codebase. -->

From stockvaluefinder/stockvaluefinder/db/models/alpha.py (ORM model template):
```python
from datetime import datetime, timezone
from typing import Any
from uuid import uuid4
from sqlalchemy import DateTime, Float, ForeignKey, Integer, String, UniqueConstraint
from sqlalchemy.dialects.postgresql import JSONB, UUID
from sqlalchemy.orm import Mapped, mapped_column
from stockvaluefinder.db.base import Base

class AlphaScoreDB(Base):
    __tablename__ = "alpha_scores"
    __table_args__ = (UniqueConstraint("ticker", "fiscal_year", name="uq_alpha_ticker_fiscal_year"),)
    analysis_id: Mapped[str] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid4, comment="Unique identifier")
```

From stockvaluefinder/stockvaluefinder/pipeline/config.py (frozen config template):
```python
from dataclasses import dataclass

@dataclass(frozen=True)
class PipelineConfig:
    max_retries: int = 3
    retry_delays: tuple[float, ...] = (2.0, 8.0, 30.0)
    def __post_init__(self) -> None:
        if self.max_retries < 0:
            raise ValueError(f"max_retries must be >= 0, got {self.max_retries}")
```

From stockvaluefinder/stockvaluefinder/models/enums.py (enum template):
```python
from enum import Enum
class RiskLevel(str, Enum):
    LOW = "LOW"
    MEDIUM = "MEDIUM"
```

From stockvaluefinder/stockvaluefinder/db/models/__init__.py (current exports):
- All 20 ORM models imported and listed in __all__

From stockvaluefinder/stockvaluefinder/alembic/versions/019_rate_limit_overrides_table.py:
- revision: "019", down_revision: "018"
- New migration must set revision="020", down_revision="019"

Key decision from research:
- Do NOT add FK from index_constituents.ticker to stocks.ticker (sync may run before stock records exist)
- Use datetime.now(timezone.utc), NOT datetime.utcnow()
- Use X | None pipe syntax, NOT Optional[X]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MarketScannerConfig, enums, and Pydantic models (TDD)</name>
  <files>
    stockvaluefinder/tests/unit/test_market_scanner/__init__.py,
    stockvaluefinder/tests/unit/test_market_scanner/test_config.py,
    stockvaluefinder/tests/unit/test_market_scanner/test_models.py,
    stockvaluefinder/stockvaluefinder/market_scanner/__init__.py,
    stockvaluefinder/stockvaluefinder/market_scanner/config.py,
    stockvaluefinder/stockvaluefinder/models/enums.py,
    stockvaluefinder/stockvaluefinder/models/market_scanner.py
  </files>
  <behavior>
    Config tests (test_config.py):
    - Test: MarketScannerConfig() creates with valid defaults (index_codes=("CSI300","CSI500"), daily_top_n=50, min_margin_of_safety=0.30, min_composite_score=60.0, etc.)
    - Test: Config rejects empty index_codes tuple with ValueError
    - Test: Config rejects daily_top_n <= 0 with ValueError
    - Test: Config rejects weekly_top_n < daily_top_n with ValueError
    - Test: Config rejects min_margin_of_safety outside [0, 1] with ValueError
    - Test: Config rejects min_composite_score outside [0, 100] with ValueError
    - Test: Config rejects deep_analysis_concurrency < 1 with ValueError
    - Test: Config accepts custom valid values without error
    - Test: Config is frozen -- assigning to any field raises FrozenInstanceError

    Model tests (test_models.py):
    - Test: ScanStatus enum has pending, running, completed, partial_failed values
    - Test: ScanType enum has daily, weekly values
    - Test: MarketScanRunCreate validates required fields (index_codes, scan_type, rules_version)
    - Test: MarketScanRunCreate rejects invalid index_code format
    - Test: IndexConstituentCreate validates ticker format (6 digits + .SH/.SZ)
    - Test: MarketScanRunUpdate allows partial updates (status only, counts only)
    - Test: MarketScanCandidateCreate validates required fields
  </behavior>
  <action>
    TDD RED-GREEN cycle:

    RED: Write test files first. Create test_market_scanner/__init__.py, test_config.py, test_models.py with all test cases above. Run tests -- they MUST fail (imports will fail since modules don't exist yet).

    GREEN: Implement the production code:

    1. Create stockvaluefinder/stockvaluefinder/market_scanner/__init__.py (empty package init).

    2. Create stockvaluefinder/stockvaluefinder/market_scanner/config.py with MarketScannerConfig frozen dataclass following the PipelineConfig pattern exactly:
       - Fields: index_codes (tuple[str, ...] default ("CSI300","CSI500")), rules_version (str default "v1"), daily_top_n (int default 50), weekly_top_n (int default 100), min_margin_of_safety (float default 0.30), min_composite_score (float default 60.0), deep_analysis_concurrency (int default 5), request_delay_seconds (float default 0.5), max_price_cache_age_minutes (int default 30), alpha_max_age_days (int default 30)
       - __post_init__ validation for each field per test cases
       - Use tuple not list (frozen dataclass rule)

    3. Add to stockvaluefinder/stockvaluefinder/models/enums.py:
       - ScanStatus(str, Enum): PENDING="pending", RUNNING="running", COMPLETED="completed", PARTIAL_FAILED="partial_failed"
       - ScanType(str, Enum): DAILY="daily", WEEKLY="weekly"

    4. Create stockvaluefinder/stockvaluefinder/models/market_scanner.py with Pydantic models:
       - MarketScanRunCreate: run_id (UUID), index_codes (tuple[str,...]), scan_type (ScanType), status (ScanStatus default PENDING), rules_version (str), total_count (int default 0), screened_count (int default 0), candidate_count (int default 0), error_summary (dict | None default None), started_at (datetime | None), completed_at (datetime | None)
       - MarketScanRunUpdate: status (ScanStatus | None), total_count (int | None), screened_count (int | None), candidate_count (int | None), error_summary (dict | None), started_at (datetime | None), completed_at (datetime | None) -- all optional
       - MarketScanRunResult: frozen model with all run fields
       - IndexConstituentCreate: constituent_id (UUID), index_code (str), ticker (str with pattern r"^\d{6}\.(SH|SZ)$"), name (str), effective_date (date), is_active (bool default True), removed_date (date | None default None)
       - IndexConstituentUpdate: is_active (bool | None), removed_date (date | None)
       - MarketScanCandidateCreate: candidate_id (UUID), run_id (UUID), ticker (str), index_code (str), passed (bool), composite_score (float), screening_snapshot (dict)
       - MarketScanCandidateUpdate: passed (bool | None), composite_score (float | None)
       - MarketScanRuleCreate: rule_id (UUID), rule_name (str), rule_type (str), is_active (bool default True), parameters (dict), priority (int default 0)
       - MarketScanRuleUpdate: is_active (bool | None), parameters (dict | None), priority (int | None)
       - Add Config.json_schema_extra examples for request models per existing pattern in alpha.py

    Use X | None syntax, NOT Optional[X]. Use model_config = {"frozen": True} on Result models. Follow Google-style docstrings.
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_market_scanner/test_config.py tests/unit/test_market_scanner/test_models.py -v</automated>
  </verify>
  <done>
    - MarketScannerConfig frozen dataclass validates all thresholds in __post_init__
    - ScanStatus and ScanType enums in enums.py
    - All Pydantic models (Create/Update/Result) for runs, constituents, candidates, rules
    - All tests pass with 80%+ coverage on config.py and market_scanner.py
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ORM models, migration, and __init__ registration (TDD)</name>
  <files>
    stockvaluefinder/stockvaluefinder/db/models/index_constituent.py,
    stockvaluefinder/stockvaluefinder/db/models/market_scan.py,
    stockvaluefinder/stockvaluefinder/db/models/__init__.py,
    stockvaluefinder/stockvaluefinder/alembic/versions/020_market_scanner_tables.py
  </files>
  <behavior>
    ORM model structure tests (add to test_models.py or create separate test_orm.py):
    - Test: IndexConstituentDB has __tablename__ = "index_constituents"
    - Test: IndexConstituentDB has constituent_id UUID PK, index_code, ticker, name, effective_date, removed_date, is_active, source, created_at, updated_at
    - Test: IndexConstituentDB has UniqueConstraint on (index_code, ticker, effective_date)
    - Test: MarketScanRunDB has __tablename__ = "market_scan_runs"
    - Test: MarketScanRunDB has run_id UUID PK, index_codes JSONB, scan_type, status with default "pending", rules_version, count fields, error_summary JSONB, timestamps
    - Test: MarketScanRunDB has index on (status)
    - Test: MarketScanCandidateDB has __tablename__ = "market_scan_candidates"
    - Test: MarketScanCandidateDB has candidate_id UUID PK, run_id FK to market_scan_runs, ticker, passed, composite_score, screening_snapshot JSONB
    - Test: MarketScanRuleDB has __tablename__ = "market_scan_rules"
    - Test: MarketScanRuleDB has rule_id UUID PK, rule_name (unique), rule_type, is_active, parameters JSONB
    - Test: __init__.py exports all 4 new models
  </behavior>
  <action>
    TDD RED-GREEN cycle:

    RED: Write ORM model structure tests verifying table names, column definitions, constraints, and __init__.py exports. Tests fail because models don't exist yet.

    GREEN: Implement production code:

    1. Create stockvaluefinder/stockvaluefinder/db/models/index_constituent.py:
       - IndexConstituentDB(Base) following AlphaScoreDB pattern
       - Fields: constituent_id (UUID PK), index_code (String(20), not nullable), ticker (String(20), not nullable -- NO FK to stocks.ticker per research decision), name (String(100), not nullable), effective_date (Date, not nullable), removed_date (Date, nullable), is_active (Boolean, not nullable, default True, server_default="true"), source (String(50), not nullable, default "akshare"), source_raw (JSONB, nullable), created_at (DateTime(timezone=True), not nullable, default datetime.now(timezone.utc)), updated_at (DateTime(timezone=True), not nullable, default datetime.now(timezone.utc), onupdate=datetime.now(timezone.utc))
       - __table_args__: UniqueConstraint("index_code", "ticker", "effective_date", name="uq_idx_ticker_date")
       - Index on (index_code, is_active) and (ticker, is_active) via Index() in __table_args__
       - __repr__ method

    2. Create stockvaluefinder/stockvaluefinder/db/models/market_scan.py containing 3 models:

       MarketScanRunDB(Base):
       - __tablename__ = "market_scan_runs"
       - Fields: run_id (UUID PK), index_codes (JSONB, not nullable, default dict -- stores tuple of index codes as JSON array), scan_type (String(20), not nullable, default "daily"), status (String(20), not nullable, default "pending", index=True), rules_version (String(20), not nullable, default "v1"), total_count (Integer, not nullable, default 0), screened_count (Integer, not nullable, default 0), candidate_count (Integer, not nullable, default 0), error_summary (JSONB, nullable), started_at (DateTime(timezone=True), nullable), completed_at (DateTime(timezone=True), nullable), created_at (DateTime(timezone=True), not nullable, default datetime.now(timezone.utc)), updated_at (DateTime(timezone=True), not nullable, default/onupdate)
       - __repr__ method

       MarketScanCandidateDB(Base):
       - __tablename__ = "market_scan_candidates"
       - Fields: candidate_id (UUID PK), run_id (UUID, ForeignKey("market_scan_runs.run_id"), not nullable, index=True), ticker (String(20), ForeignKey("stocks.ticker"), not nullable, index=True), index_code (String(20), not nullable), passed (Boolean, not nullable, default True), composite_score (Float, not nullable, default 0.0), screening_snapshot (JSONB, not nullable, default=dict), created_at (DateTime(timezone=True), not nullable)
       - UniqueConstraint("run_id", "ticker", name="uq_candidate_run_ticker")
       - __repr__ method

       MarketScanRuleDB(Base):
       - __tablename__ = "market_scan_rules"
       - Fields: rule_id (UUID PK), rule_name (String(100), not nullable, unique=True), rule_type (String(50), not nullable, index=True), description (String(500), nullable), is_active (Boolean, not nullable, default True), parameters (JSONB, not nullable, default=dict), priority (Integer, not nullable, default 0), created_at, updated_at
       - __repr__ method

    3. Update stockvaluefinder/stockvaluefinder/db/models/__init__.py:
       - Add imports for IndexConstituentDB, MarketScanRunDB, MarketScanCandidateDB, MarketScanRuleDB
       - Add all 4 to __all__ list

    4. Create stockvaluefinder/stockvaluefinder/alembic/versions/020_market_scanner_tables.py:
       - revision = "020", down_revision = "019"
       - upgrade() creates 4 tables in dependency order:
         a. index_constituents (no FK to scanner tables)
         b. market_scan_rules (no FK to scanner tables)
         c. market_scan_runs (standalone)
         d. market_scan_candidates (FK to market_scan_runs.run_id, FK to stocks.ticker)
       - Each table uses sa.dialects.postgresql.UUID(as_uuid=True) for UUID columns, sa.JSON() for JSONB, server_default values for booleans and defaults
       - Create indexes: ix_idx_const_code_active, ix_idx_const_ticker_active, ix_scan_runs_status, ix_scan_candidates_run_id, ix_scan_candidates_ticker, ix_scan_rules_type
       - downgrade() drops tables in reverse dependency order: candidates, runs, rules, constituents

    Follow exact patterns from 019_rate_limit_overrides_table.py and 014_alpha_scores_table.py. Use sa.func.now() for server_default on timestamps.
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_market_scanner/ -v</automated>
  </verify>
  <done>
    - 4 ORM models (IndexConstituentDB, MarketScanRunDB, MarketScanCandidateDB, MarketScanRuleDB) with correct table names, columns, constraints
    - __init__.py updated with 4 new exports
    - Alembic migration 020 creates 4 tables with correct FK order and indexes
    - All tests pass including ORM model structure tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config instantiation | Invalid config values caught by __post_init__ validation |
| Pydantic model validation | External input (ticker codes, scan parameters) validated before data layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | MarketScannerConfig | mitigate | Frozen dataclass prevents runtime mutation; __post_init__ rejects invalid thresholds |
| T-25-02 | Tampering | Pydantic ticker validation | mitigate | Pattern validation r"^\d{6}\.(SH|SZ)$" prevents injection via ticker field |
| T-25-03 | Information disclosure | error_summary JSONB | accept | Contains only stock tickers and error messages, no PII or secrets |
</threat_model>

<verification>
- All tests pass: `cd stockvaluefinder && uv run pytest tests/unit/test_market_scanner/ -v`
- Config frozen: `cd stockvaluefinder && uv run python -c "from stockvaluefinder.market_scanner.config import MarketScannerConfig; c = MarketScannerConfig(); c.daily_top_n = 99"` must raise FrozenInstanceError
- Migration syntax: `cd stockvaluefinder && uv run python -c "from stockvaluefinder.alembic.versions.020_market_scanner_tables import upgrade, downgrade; print('OK')"` must not error
- ORM models importable: `cd stockvaluefinder && uv run python -c "from stockvaluefinder.db.models import IndexConstituentDB, MarketScanRunDB, MarketScanCandidateDB, MarketScanRuleDB; print('OK')"`
</verification>

<success_criteria>
- MarketScannerConfig frozen dataclass validates all thresholds in __post_init__ (SCR-04)
- IndexConstituentDB ORM model has effective_date, removed_date, is_active fields (IDX-01, IDX-02)
- MarketScanRunDB ORM model tracks status through pending/running/completed/partial_failed (EXE-04)
- 4 Pydantic Create/Update/Result model sets for scanner domain
- ScanStatus and ScanType enums in enums.py
- Alembic migration 020 creates all 4 tables
- All test_market_scanner tests pass with 80%+ coverage on new modules
- No hardcoded thresholds anywhere in production code
</success_criteria>

<output>
After completion, create `.planning/phases/25-data-foundation/25-01-SUMMARY.md`
</output>
