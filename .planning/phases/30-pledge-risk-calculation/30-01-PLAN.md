---
phase: 30-pledge-risk-calculation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - stockvaluefinder/stockvaluefinder/models/equity_pledge.py
  - stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py
  - stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-03
  - RISK-07
  - RISK-08
  - RISK-09

must_haves:
  truths:
    - "Company pledge ratio is graded into LOW/MEDIUM/HIGH based on explicit thresholds"
    - "Controlling shareholder pledge ratio is graded into LOW/MEDIUM/HIGH based on explicit thresholds"
    - "Closeout safety margin is calculated and graded; None inputs produce None margin"
    - "Data freshness is classified as CURRENT/STALE/UNAVAILABLE based on calendar days"
    - "Controlling shareholder is identified as holder with highest pledged_to_holding_ratio"
    - "HK tickers return supported=false with no pledge calculations"
    - "Zero-pledge stocks return holder_risk_level=LOW with no controlling holder"
  artifacts:
    - path: "stockvaluefinder/stockvaluefinder/models/equity_pledge.py"
      provides: "Result models: PledgeRiskGrade, CompanyPledgeRisk, HolderPledgeRisk, CloseoutRisk, RiskLevelBreakdown, PledgeRiskResult"
      contains: "class PledgeRiskResult"
    - path: "stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py"
      provides: "Pure calculation functions for pledge risk grading"
      exports: ["determine_company_pledge_risk", "determine_holder_pledge_risk", "calculate_closeout_safety_margin", "determine_closeout_risk", "determine_data_freshness", "find_controlling_holder", "is_hk_ticker", "PledgeRiskAnalyzer"]
    - path: "stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py"
      provides: "Unit tests covering RISK-01, RISK-02, RISK-03, RISK-07, RISK-08, RISK-09"
      min_lines: 200
  key_links:
    - from: "stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py"
      to: "stockvaluefinder/stockvaluefinder/models/equity_pledge.py"
      via: "import EquityPledgeSnapshot, EquityPledgeDetail, PledgeRiskResult"
      pattern: "from stockvaluefinder.models.equity_pledge import"
    - from: "stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py"
      to: "stockvaluefinder/stockvaluefinder/models/enums.py"
      via: "import RiskLevel, DataFreshness"
      pattern: "from stockvaluefinder.models.enums import"
---

<objective>
Define result models and implement pure calculation functions for the three pledge risk grading dimensions (company ratio, holder ratio, closeout margin), plus supporting utilities (data freshness, holder identification, HK detection).

Purpose: Establish the complete data contract and all individual grading functions that the combination rules and merge logic in Plan 02 will consume. TDD ensures every threshold boundary is verified before integration.
Output: Result Pydantic models in equity_pledge.py, pure functions + PledgeRiskAnalyzer class in pledge_risk_service.py, comprehensive test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pledge-risk-calculation/30-CONTEXT.md
@.planning/phases/30-pledge-risk-calculation/30-RESEARCH.md
@stockvaluefinder/stockvaluefinder/models/equity_pledge.py
@stockvaluefinder/stockvaluefinder/models/enums.py
@stockvaluefinder/stockvaluefinder/models/risk.py
@stockvaluefinder/stockvaluefinder/services/risk_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define pledge risk result models in equity_pledge.py</name>
  <files>stockvaluefinder/stockvaluefinder/models/equity_pledge.py, stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py</files>
  <read_first>
    - stockvaluefinder/stockvaluefinder/models/equity_pledge.py (existing models to extend)
    - stockvaluefinder/stockvaluefinder/models/risk.py (pattern: frozen=True, Field descriptions)
    - stockvaluefinder/stockvaluefinder/models/enums.py (RiskLevel, DataFreshness enums)
    - stockvaluefinder/stockvaluefinder/services/risk_service.py (RiskAnalyzer class pattern)
  </read_first>
  <behavior>
    - Test 1: PledgeRiskGrade model instantiates with risk_level=RiskLevel.LOW, notes=[]
    - Test 2: CompanyPledgeRisk model instantiates with risk_level, company_pledge_ratio, and notes
    - Test 3: HolderPledgeRisk model instantiates with risk_level, pledged_to_holding_ratio, holder_name, notes, controlling_holder=True
    - Test 4: CloseoutRisk model instantiates with risk_level, safety_margin (float|None), latest_price, estimated_closeout_price, notes
    - Test 5: RiskLevelBreakdown model instantiates with financial_risk_level, pledge_risk_level (RiskLevel|None), final_risk_level, merge_reason (str|None)
    - Test 6: PledgeRiskResult model instantiates with supported=True, company_risk, holder_risk, closeout_risk, red_flags, data_quality, risk_level_breakdown; verify frozen=True enforcement
    - Test 7: PledgeRiskResult with supported=False has warning in data_quality
  </behavior>
  <action>
    Add the following frozen Pydantic models to the END of stockvaluefinder/stockvaluefinder/models/equity_pledge.py, after the existing EquityPledgeDetail class (per D-03):

    1. PledgeRiskGrade(BaseModel) with model_config frozen=True, fields: risk_level (RiskLevel), notes (list[str], default_factory=list). This is the base grading result shared across dimensions.

    2. CompanyPledgeRisk(BaseModel) with model_config frozen=True, fields: risk_level (RiskLevel), company_pledge_ratio (float | None), notes (list[str], default_factory=list).

    3. HolderPledgeRisk(BaseModel) with model_config frozen=True, fields: risk_level (RiskLevel), pledged_to_holding_ratio (float | None), holder_name (str | None, default None), controlling_holder (bool, default False), notes (list[str], default_factory=list).

    4. CloseoutRisk(BaseModel) with model_config frozen=True, fields: risk_level (RiskLevel), safety_margin (float | None, default None), latest_price (float | None, default None), estimated_closeout_price (float | None, default None), notes (list[str], default_factory=list).

    5. RiskLevelBreakdown(BaseModel) with model_config frozen=True, fields: financial_risk_level (RiskLevel), pledge_risk_level (RiskLevel | None), final_risk_level (RiskLevel), merge_reason (str | None, default None).

    6. PledgeRiskResult(BaseModel) with model_config frozen=True, fields: supported (bool, default True), company_risk (CompanyPledgeRisk | None, default None), holder_risk (HolderPledgeRisk | None, default None), closeout_risk (CloseoutRisk | None, default None), combination_upgrades (list[str], default_factory=list), red_flags (list[str], default_factory=list), data_quality (EquityPledgeDataQuality), risk_level_breakdown (RiskLevelBreakdown).

    Add import of RiskLevel from stockvaluefinder.models.enums at the top of equity_pledge.py (DataFreshness is already imported).

    Create the test file stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py with a test class TestPledgeRiskModels containing the 7 behavior tests above. Use pytest, no async needed. Follow the existing test file naming pattern from the codebase.

    Ensure test directory __init__.py files exist.
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_services/test_pledge_risk_service.py::TestPledgeRiskModels -x</automated>
  </verify>
  <done>
    - PledgeRiskGrade, CompanyPledgeRisk, HolderPledgeRisk, CloseoutRisk, RiskLevelBreakdown, PledgeRiskResult models defined in equity_pledge.py with frozen=True
    - All models have proper Field descriptions following risk.py pattern
    - 7 model instantiation tests pass
    - No new enum values added (per pitfall 1 in RESEARCH.md)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure grading functions and PledgeRiskAnalyzer class</name>
  <files>stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py, stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py</files>
  <read_first>
    - stockvaluefinder/stockvaluefinder/services/risk_service.py (pattern: module-level pure functions + thin class wrapper)
    - stockvaluefinder/stockvaluefinder/models/equity_pledge.py (input models: EquityPledgeSnapshot, EquityPledgeDetail; result models from Task 1)
    - stockvaluefinder/stockvaluefinder/models/enums.py (RiskLevel, DataFreshness)
    - doc/equity_pledge_risk_analysis_technical_design.md lines 457-553 (threshold tables in sections 9.1-9.5)
  </read_first>
  <behavior>
    Company pledge risk grading (RISK-01):
    - ratio=None returns (RiskLevel.LOW, ["质押比例数据不可得"])
    - ratio=5.0 (<10%) returns (RiskLevel.LOW, [])
    - ratio=15.0 (10-20%) returns (RiskLevel.LOW, [note containing "10%-20%"])
    - ratio=25.0 (20-30%) returns (RiskLevel.MEDIUM, [])
    - ratio=35.0 (>30%) returns (RiskLevel.HIGH, [])
    - ratio=10.0 (exact boundary 10%) returns (RiskLevel.LOW, [note])
    - ratio=20.0 (exact boundary 20%) returns (RiskLevel.MEDIUM, [])
    - ratio=30.0 (exact boundary 30%) returns (RiskLevel.MEDIUM, [])

    Holder pledge risk grading (RISK-02):
    - ratio=None returns (RiskLevel.LOW, ["控股股东质押比例数据不可得"])
    - ratio=20.0 (<30%) returns (RiskLevel.LOW, [])
    - ratio=40.0 (30-50%) returns (RiskLevel.LOW, [note containing "30%-50%"])
    - ratio=65.0 (50-80%) returns (RiskLevel.MEDIUM, [])
    - ratio=85.0 (>80%) returns (RiskLevel.HIGH, [])

    Closeout margin (RISK-03):
    - calculate_closeout_safety_margin(10.0, 8.0) returns 25.0
    - calculate_closeout_safety_margin(None, 8.0) returns None
    - calculate_closeout_safety_margin(10.0, None) returns None
    - calculate_closeout_safety_margin(10.0, 0.0) returns None
    - determine_closeout_risk(60.0) returns (RiskLevel.LOW, [])
    - determine_closeout_risk(40.0) returns (RiskLevel.LOW, [note containing "30%-50%"])
    - determine_closeout_risk(25.0) returns (RiskLevel.MEDIUM, [])
    - determine_closeout_risk(15.0) returns (RiskLevel.HIGH, [])
    - determine_closeout_risk(None) returns (RiskLevel.LOW, ["平仓线安全距离数据不可得"])

    Data freshness (RISK-07):
    - latest_date=None returns DataFreshness.UNAVAILABLE
    - latest_date=today returns DataFreshness.CURRENT
    - latest_date=5 days ago returns DataFreshness.CURRENT
    - latest_date=10 days ago returns DataFreshness.CURRENT
    - latest_date=11 days ago returns DataFreshness.STALE

    Controlling holder identification (RISK-08):
    - Empty list returns None (D-07 zero-pledge)
    - Single detail with pledged_to_holding_ratio=50.0 returns that detail
    - Multiple details returns one with highest pledged_to_holding_ratio
    - Tie in pledged_to_holding_ratio returns first in list (D-06)
    - All details with pledged_to_holding_ratio=None returns None

    HK ticker detection (RISK-09):
    - is_hk_ticker("00700.HK") returns True
    - is_hk_ticker("600519.SH") returns False
    - is_hk_ticker("000002.SZ") returns False
  </action>
  <action>
    Create stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py with the following module-level pure functions. Follow the exact pattern of risk_service.py: module docstring, imports at top, pure functions, then thin class wrapper at bottom.

    Functions to implement (all synchronous, no I/O):

    1. determine_company_pledge_risk(company_pledge_ratio: float | None) -> tuple[RiskLevel, list[str]]: Per D-01 and PRD section 9.1 thresholds. company_pledge_ratio is percentage (0-100) per Phase 29 convention (pitfall 4). None returns LOW with data-unavailable note (pitfall 2). 10-20% range returns LOW with note string (pitfall 1 -- no WATCH enum).

    2. determine_holder_pledge_risk(pledged_to_holding_ratio: float | None) -> tuple[RiskLevel, list[str]]: Per PRD section 9.2 thresholds. Same pattern as company grading. pledged_to_holding_ratio is also percentage (0-100).

    3. calculate_closeout_safety_margin(latest_price: float | None, estimated_closeout_price: float | None) -> float | None: Formula from tech design section 9.3: (latest_price - estimated_closeout_price) / estimated_closeout_price * 100. Returns None when either input is None or estimated_closeout_price <= 0.

    4. determine_closeout_risk(safety_margin: float | None) -> tuple[RiskLevel, list[str]]: Per PRD section 9.3 thresholds (>50% LOW, 30-50% LOW+note, 20-30% MEDIUM, <20% HIGH). None returns LOW with data-unavailable note.

    5. determine_data_freshness(latest_date: date | None, reference_date: date | None = None) -> DataFreshness: Per PRD section 11.3. Uses calendar days (not trading days). reference_date defaults to date.today().

    6. find_controlling_holder(details: list[EquityPledgeDetail]) -> EquityPledgeDetail | None: Per PRD section 9.2. Iterates details to find max pledged_to_holding_ratio. Tie-breaking: first in list order (D-06). Empty list or all-None ratios returns None (D-07).

    7. is_hk_ticker(ticker: str) -> bool: Simple .endswith(".HK") check. Per RISK-09.

    8. PledgeRiskAnalyzer class: Stateless class following RiskAnalyzer pattern (per D-01). Has empty __init__. Has analyze() method signature per D-02: analyze(self, ticker: str, snapshot: EquityPledgeSnapshot | None, details: list[EquityPledgeDetail], financial_risk_level: RiskLevel, financial_red_flags: list[str] | None = None) -> PledgeRiskResult. The analyze() implementation for this task should: handle HK tickers early (return unsupported result via is_hk_ticker), compute freshness, grade the three dimensions, identify controlling holder, but leave combination upgrade rules and merge as TODO stubs (returning RiskLevel.LOW as pledge_risk_level for merge). Full implementation of combination rules and merge is Plan 02.

    Import RiskLevel, DataFreshness from stockvaluefinder.models.enums. Import EquityPledgeSnapshot, EquityPledgeDetail, PledgeRiskResult, CompanyPledgeRisk, HolderPledgeRisk, CloseoutRisk, RiskLevelBreakdown from stockvaluefinder.models.equity_pledge.

    Add tests to the existing test file under separate test classes: TestCompanyPledgeRisk, TestHolderPledgeRisk, TestCloseoutMargin, TestDataFreshness, TestFindControllingHolder, TestHKTicker, TestPledgeRiskAnalyzerBasic. Use pytest.mark.parametrize for threshold boundary testing. All tests use synchronous functions (no pytest.mark.asyncio).
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_services/test_pledge_risk_service.py -x -v</automated>
  </verify>
  <done>
    - pledge_risk_service.py created with 7 module-level pure functions + PledgeRiskAnalyzer class
    - All threshold boundaries tested with parametrize for RISK-01, RISK-02, RISK-03, RISK-07, RISK-08, RISK-09
    - None handling tested for every grading function (pitfall 2)
    - Tie-breaking tested for find_controlling_holder (D-06)
    - Zero-pledge returns None holder with LOW risk (D-07)
    - HK ticker returns supported=false result (RISK-09)
    - All functions are synchronous with no I/O (anti-pattern from RESEARCH.md avoided)
    - All threshold comparisons use integer values 10/20/30/50/80 not 0.1/0.2 etc (pitfall 4 avoided)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal only | Pure calculation functions -- no external input crosses a boundary in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | Grading function inputs | mitigate | Pydantic frozen models + explicit None handling in every grading function |
| T-30-02 | Denial of Service | NaN/inf propagation | mitigate | Explicit None checks before arithmetic; closeout_price <= 0 guard |
| T-30-SC | Tampering | No package installs this phase | accept | Pure Python, no new dependencies |
</threat_model>

<verification>
uv run pytest tests/unit/test_services/test_pledge_risk_service.py -v --cov=stockvaluefinder.services.pledge_risk_service --cov-report=term-missing
</verification>

<success_criteria>
- All 6 pure functions + is_hk_ticker implemented as synchronous, no I/O
- PledgeRiskAnalyzer class follows RiskAnalyzer pattern (stateless, thin wrapper)
- Every threshold boundary from PRD sections 9.1-9.3 tested
- None handling verified for all grading functions
- Company pledge ratio thresholds use percentage values (10, 20, 30) not decimal (0.1, 0.2, 0.3)
- Test coverage >= 80% for pledge_risk_service.py
- HK ticker returns supported=false with no calculations attempted
</success_criteria>

<output>
Create `.planning/phases/30-pledge-risk-calculation/30-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New files
- `stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py` -- pledge risk calculation service
- `stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py` -- test suite

### New classes (in existing file stockvaluefinder/stockvaluefinder/models/equity_pledge.py)
- `PledgeRiskGrade` -- base grading result model
- `CompanyPledgeRisk` -- company pledge risk dimension result
- `HolderPledgeRisk` -- controlling shareholder risk dimension result
- `CloseoutRisk` -- closeout margin risk dimension result
- `RiskLevelBreakdown` -- merge breakdown for financial + pledge risk
- `PledgeRiskResult` -- complete pledge risk analysis result

### New functions (in pledge_risk_service.py)
- `determine_company_pledge_risk()` -- RISK-01 grading
- `determine_holder_pledge_risk()` -- RISK-02 grading
- `calculate_closeout_safety_margin()` -- RISK-03 formula
- `determine_closeout_risk()` -- RISK-03 grading
- `determine_data_freshness()` -- RISK-07 classification
- `find_controlling_holder()` -- RISK-08 identification
- `is_hk_ticker()` -- RISK-09 detection
- `PledgeRiskAnalyzer` class with `analyze()` method -- orchestrator (partial: combination rules + merge stubbed for Plan 02)
