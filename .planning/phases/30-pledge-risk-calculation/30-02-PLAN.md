---
phase: 30-pledge-risk-calculation
plan: 02
type: tdd
wave: 2
depends_on:
  - 30-01
files_modified:
  - stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py
  - stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py
autonomous: true
requirements:
  - RISK-04
  - RISK-05
  - RISK-06

must_haves:
  truths:
    - "All 5 combination upgrade rules evaluate independently with no short-circuit, producing a full audit trail"
    - "Each triggered rule adds a specific red flag with actual data values in Chinese"
    - "Risk merge takes the higher of financial and pledge risk levels, never downgrading financial risk"
    - "PledgeRiskAnalyzer.analyze() orchestrates grading + upgrades + merge into a complete PledgeRiskResult"
    - "Combination rules 4 and 5 detect financial red flag conditions by checking financial_red_flags input"
  artifacts:
    - path: "stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py"
      provides: "Combination upgrade rule functions, merge function, complete analyze() implementation"
      exports: ["check_high_pledge_with_price_drop", "check_holder_over_80", "check_closeout_margin_low", "check_high_pledge_with_financial_high", "check_high_pledge_with_存贷双高", "merge_risk_levels"]
    - path: "stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py"
      provides: "Tests for combination rules, merge, and full analyze() integration"
      min_lines: 350
  key_links:
    - from: "stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py"
      to: "stockvaluefinder/stockvaluefinder/models/enums.py"
      via: "_RISK_ORDER dict maps RiskLevel to int for comparison"
      pattern: "_RISK_ORDER"
    - from: "PledgeRiskAnalyzer.analyze()"
      to: "check_high_pledge_with_price_drop, check_holder_over_80, etc."
      via: "calls all 5 rules, collects results, applies max upgrade"
      pattern: "check_"
---

<objective>
Implement the 5 combination upgrade rules (RISK-04), risk level merge logic (RISK-05), red flag generation (RISK-06), and complete the PledgeRiskAnalyzer.analyze() orchestration method.

Purpose: Wire together the individual grading functions from Plan 01 into a complete risk analysis pipeline. The combination rules catch multi-factor risk patterns (high pledge + price drop, high pledge + financial distress). The merge ensures pledge risk can only upgrade financial risk. Red flags provide explainability.
Output: Fully functional PledgeRiskAnalyzer.analyze() producing complete PledgeRiskResult, tested at all boundary conditions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pledge-risk-calculation/30-CONTEXT.md
@.planning/phases/30-pledge-risk-calculation/30-RESEARCH.md
@.planning/phases/30-pledge-risk-calculation/30-01-SUMMARY.md
@stockvaluefinder/stockvaluefinder/services/risk_service.py
@stockvaluefinder/stockvaluefinder/models/equity_pledge.py
@stockvaluefinder/stockvaluefinder/models/enums.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement combination upgrade rules and merge logic</name>
  <files>stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py, stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py</files>
  <read_first>
    - stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py (functions from Plan 01 to extend)
    - stockvaluefinder/stockvaluefinder/services/risk_service.py (pattern: module-level pure functions, line 750 shows red flag string format for rule 5)
    - stockvaluefinder/stockvaluefinder/models/enums.py (RiskLevel enum for _RISK_ORDER mapping)
    - doc/equity_pledge_risk_analysis_technical_design.md lines 523-553 (combination rules section 9.4 and merge section 9.5)
    - stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py (existing tests to extend)
  </read_first>
  <behavior>
    Rule 1 - check_high_pledge_with_price_drop (company pledge >30% + 1yr drop >30%):
    - company_pledge_ratio=35.0, one_year_price_change=-35.0 -> (True, red_flag containing "质押比例" and "跌幅")
    - company_pledge_ratio=25.0 (not >30%) -> (False, None)
    - company_pledge_ratio=35.0, one_year_price_change=-25.0 (not <-30%) -> (False, None)
    - company_pledge_ratio=None -> (False, None)
    - company_pledge_ratio=35.0, one_year_price_change=None -> (False, None)

    Rule 2 - check_holder_over_80 (holder pledge >80%):
    - holder_pledge_ratio=85.0 -> (True, red_flag containing "控股股东质押比例" and "80%")
    - holder_pledge_ratio=80.0 (not >80%, exact boundary) -> (False, None)
    - holder_pledge_ratio=None -> (False, None)
    - holder_pledge_ratio=50.0 -> (False, None)

    Rule 3 - check_closeout_margin_low (margin <20%):
    - safety_margin=15.0 -> (True, red_flag containing "平仓线安全距离" and "20%")
    - safety_margin=20.0 (not <20%, exact boundary) -> (False, None)
    - safety_margin=None -> (False, None)
    - safety_margin=30.0 -> (False, None)

    Rule 4 - check_high_pledge_with_financial_high (company pledge >20% + financial HIGH):
    - company_pledge_ratio=25.0, financial_risk_level=RiskLevel.HIGH -> (True, red_flag)
    - company_pledge_ratio=15.0 (not >20%) -> (False, None)
    - company_pledge_ratio=25.0, financial_risk_level=RiskLevel.MEDIUM -> (False, None)

    Rule 5 - check_high_pledge_with_存贷双高 (company pledge >20% + 存贷双高 flag in financial_red_flags):
    - company_pledge_ratio=25.0, financial_red_flags=["存贷双高: High cash and high debt anomaly detected"] -> (True, red_flag containing "存贷双高")
    - company_pledge_ratio=25.0, financial_red_flags=[] -> (False, None)
    - company_pledge_ratio=25.0, financial_red_flags=["other flag"] -> (False, None)
    - company_pledge_ratio=15.0, financial_red_flags=["存贷双高: ..."] -> (False, None)

    Merge (RISK-05):
    - financial=LOW, pledge=HIGH -> final=HIGH, merge_reason contains "升级"
    - financial=HIGH, pledge=LOW -> final=HIGH, merge_reason=None (no upgrade)
    - financial=MEDIUM, pledge=MEDIUM -> final=MEDIUM, merge_reason=None
    - financial=LOW, pledge=None -> final=LOW, merge_reason=None
    - financial=CRITICAL, pledge=HIGH -> final=CRITICAL, merge_reason=None (pledge lower)
    - financial=LOW, pledge=CRITICAL -> final=CRITICAL, merge_reason contains "升级"

    Red flag format (RISK-06):
    - Each red_flag is a plain Chinese string with actual data values
    - Format follows D-08: e.g., "公司质押比例35.0%超过30%阈值"
  </action>
  <action>
    Add the following to stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py, after the existing pure functions and before the PledgeRiskAnalyzer class:

    A. Module-level constant _RISK_ORDER: dict[RiskLevel, int] mapping LOW->0, MEDIUM->1, HIGH->2, CRITICAL->3. Used by merge_risk_levels for comparison. Per tech design section 9.5.

    B. Five combination upgrade rule functions (per D-04). Each returns tuple[bool, str | None] where True means triggered and the string is the red flag. All 5 functions always return a result -- no short-circuit (D-05). Each function accepts None inputs and returns (False, None) when inputs are insufficient:

    1. check_high_pledge_with_price_drop(company_pledge_ratio: float | None, one_year_price_change: float | None) -> tuple[bool, str | None]: Triggered when company_pledge_ratio > 30 AND one_year_price_change < -30. Red flag format: "公司质押比例{ratio:.1f}%超30%且近一年跌幅{change:.1f}%超30%". Per PRD section 9.4 rule 1.

    2. check_holder_over_80(holder_pledge_ratio: float | None) -> tuple[bool, str | None]: Triggered when holder_pledge_ratio > 80. Red flag format: "控股股东质押比例{ratio:.1f}%超过80%阈值". Per PRD section 9.4 rule 2. Per D-08 format.

    3. check_closeout_margin_low(safety_margin: float | None) -> tuple[bool, str | None]: Triggered when safety_margin is not None AND safety_margin < 20. Red flag format: "平仓线安全距离{margin:.1f}%低于20%阈值". Per PRD section 9.4 rule 3.

    4. check_high_pledge_with_financial_high(company_pledge_ratio: float | None, financial_risk_level: RiskLevel) -> tuple[bool, str | None]: Triggered when company_pledge_ratio > 20 AND financial_risk_level is HIGH or CRITICAL. Red flag format: "公司质押比例{ratio:.1f}%超20%且财务风险为{level.value}". Per PRD section 9.4 rule 4.

    5. check_high_pledge_with_存贷双高(company_pledge_ratio: float | None, financial_red_flags: list[str]) -> tuple[bool, str | None]: Triggered when company_pledge_ratio > 20 AND any string in financial_red_flags contains substring "存贷双高". This matches the existing RiskAnalyzer red flag: "存贷双高: High cash and high debt anomaly detected" (verified in risk_service.py line 750). Red flag format: "公司质押比例{ratio:.1f}%超20%且存在存贷双高". Per PRD section 9.4 rule 5 and pitfall 3 in RESEARCH.md.

    C. merge_risk_levels(financial_risk_level: RiskLevel, pledge_risk_level: RiskLevel | None) -> tuple[RiskLevel, RiskLevelBreakdown]: Per D-02 and tech design section 9.5. pledge_risk_level=None returns financial unchanged with None breakdown. Otherwise takes max by _RISK_ORDER. Sets merge_reason to f"质押风险{pledge.value}升级了财务风险{financial.value}" when pledge is higher, else None.

    Add test classes to the existing test file: TestCombinationRules (parametrized tests for all 5 rules, each with triggered and not-triggered cases), TestMergeRiskLevels (parametrized for all merge combinations including CRITICAL handling), TestRedFlagFormat (verifies exact string content of red flags per D-08).
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_services/test_pledge_risk_service.py::TestCombinationRules tests/unit/test_services/test_pledge_risk_service.py::TestMergeRiskLevels -x -v</automated>
  </verify>
  <done>
    - All 5 combination upgrade rules implemented as separate pure functions (D-04)
    - No short-circuit evaluation -- all rules always return results (D-05)
    - merge_risk_levels correctly handles upgrade-only semantics (RISK-05)
    - CRITICAL level handled correctly in merge (assumption A3 from RESEARCH.md)
    - Red flags contain actual data values in Chinese format (D-08, RISK-06)
    - Rule 5 detects 存贷双高 via substring match on financial_red_flags (pitfall 3)
    - All boundary values tested (exact threshold values like 30.0, 80.0, 20.0)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete PledgeRiskAnalyzer.analyze() orchestration</name>
  <files>stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py, stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py</files>
  <read_first>
    - stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py (existing PledgeRiskAnalyzer class with stubbed analyze())
    - stockvaluefinder/stockvaluefinder/services/risk_service.py (pattern: RiskAnalyzer.analyze() at line 835)
    - stockvaluefinder/stockvaluefinder/models/equity_pledge.py (EquityPledgeSnapshot, EquityPledgeDetail, PledgeRiskResult models)
    - stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py (existing tests to extend)
  </read_first>
  <behavior>
    Full analyze() integration test scenarios:

    Scenario 1 - Normal A-share with moderate pledge:
    - ticker="600519.SH", snapshot with company_pledge_ratio=25.0, one_year_price_change=-10.0
    - details with one holder pledged_to_holding_ratio=60.0, estimated_closeout_price=80.0, latest_price=100.0
    - financial_risk_level=RiskLevel.MEDIUM, financial_red_flags=[]
    - Result: company_risk.risk_level=MEDIUM, holder_risk.risk_level=MEDIUM, closeout_risk.safety_margin=25.0, supported=True
    - Red flags contain note strings for borderline ranges

    Scenario 2 - High risk triggers combination rule 1:
    - ticker="000002.SZ", snapshot with company_pledge_ratio=35.0, one_year_price_change=-35.0
    - details with holder pledged_to_holding_ratio=50.0
    - financial_risk_level=RiskLevel.LOW
    - Result: rule 1 triggers, pledge risk upgraded to HIGH, final merged level=HIGH, merge_reason present

    Scenario 3 - Multiple combination rules trigger simultaneously:
    - ticker="600000.SH", company_pledge_ratio=35.0, one_year_price_change=-35.0, holder ratio=85.0, closeout margin=15.0
    - financial_risk_level=RiskLevel.HIGH, financial_red_flags=["存贷双高: ..."]
    - Result: all 5 rules trigger, 5 red flags in combination_upgrades, final level=CRITICAL (financial HIGH + pledge HIGH)

    Scenario 4 - Zero-pledge stock:
    - ticker="601318.SH", snapshot with company_pledge_ratio=0.0, details=[]
    - financial_risk_level=RiskLevel.LOW
    - Result: holder_risk.risk_level=LOW, controlling_holder=None (via holder_name being None), no combination rules trigger

    Scenario 5 - HK ticker:
    - ticker="00700.HK"
    - Result: supported=False, company_risk=None, holder_risk=None, closeout_risk=None, data_quality.freshness=UNAVAILABLE

    Scenario 6 - None snapshot (data unavailable):
    - ticker="600519.SH", snapshot=None, details=[]
    - Result: data_quality.freshness=UNAVAILABLE, pledge_risk_level=None in breakdown, final=financial_risk_level unchanged
  </action>
  <action>
    Complete the PledgeRiskAnalyzer.analyze() method in pledge_risk_service.py. The method signature is already defined in Plan 01 Task 2. Replace the stub with the full implementation:

    The analyze() method orchestrates in this order:
    1. HK check: if is_hk_ticker(ticker), return unsupported PledgeRiskResult with supported=False, data_quality.freshness=UNAVAILABLE, warning in data_quality.warnings, risk_level_breakdown with pledge_risk_level=None and final=financial_risk_level. Per RISK-09.

    2. Data availability check: if snapshot is None, return PledgeRiskResult with data_quality.freshness=UNAVAILABLE, all dimension risks as LOW with data-unavailable notes, pledge_risk_level=None, final=financial_risk_level. Per pitfall 5.

    3. Data freshness: call determine_data_freshness(snapshot.latest_date).

    4. Grade company risk: call determine_company_pledge_risk(snapshot.company_pledge_ratio), construct CompanyPledgeRisk.

    5. Find controlling holder: call find_controlling_holder(details). If None found (zero-pledge), set holder risk to LOW with None ratio.

    6. Grade holder risk: call determine_holder_pledge_risk with the controlling holder's pledged_to_holding_ratio. Construct HolderPledgeRisk with holder_name and controlling_holder=True.

    7. Calculate closeout margin: get latest_price and estimated_closeout_price from the controlling holder detail (if found). Call calculate_closeout_safety_margin then determine_closeout_risk. Construct CloseoutRisk.

    8. Collect base red flags from all three dimension gradings (notes from each).

    9. Evaluate all 5 combination upgrade rules (no short-circuit, per D-05). Collect triggered rules and their red flags. Determine the max upgrade level from triggered rules (minimum HIGH if any rule triggers, since all rules target at least HIGH).

    10. Determine pledge_risk_level: max of company_risk.risk_level, holder_risk.risk_level, closeout_risk.risk_level, and any upgrade from combination rules. Use _RISK_ORDER for comparison.

    11. Merge: call merge_risk_levels(financial_risk_level, pledge_risk_level).

    12. Collect all red flags: base dimension notes + combination rule red flags.

    13. Construct and return PledgeRiskResult with all sub-models populated.

    Add integration test class TestPledgeRiskAnalyzerIntegration to the test file with the 6 scenario tests described in the behavior block. Build EquityPledgeSnapshot and EquityPledgeDetail fixtures directly in test functions using the frozen Pydantic model constructors.

    Ensure the test for Scenario 3 verifies that all 5 rule names appear in combination_upgrades list and that red_flags count includes contributions from all triggered rules plus dimension notes.
  </action>
  <verify>
    <automated>cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_services/test_pledge_risk_service.py -x -v</automated>
  </verify>
  <done>
    - PledgeRiskAnalyzer.analyze() fully implements the orchestration pipeline
    - All 9 RISK requirements (RISK-01 through RISK-09) are exercised in integration tests
    - HK tickers return supported=false without calculations (RISK-09)
    - None snapshot returns UNAVAILABLE result (pitfall 5)
    - Multiple combination rules can trigger simultaneously (D-05)
    - Merge preserves financial risk when pledge is lower (RISK-05)
    - Red flags aggregate from dimension notes and combination rules (RISK-06)
    - Full test suite passes: models + pure functions + combination rules + merge + integration
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal only | Pure calculation orchestration -- combination rules and merge operate on already-computed values, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | Rule 5 substring match on financial_red_flags | accept | String matching is defensive: any flag containing "存贷双高" triggers, even with unexpected prefixes |
| T-30-04 | Information Disclosure | Red flags contain actual ratio values | accept | Red flags are internal analysis results, not exposed to untrusted parties in this phase |
| T-30-SC | Tampering | No package installs this phase | accept | Pure Python, no new dependencies |
</threat_model>

<verification>
cd /home/robertzeng/project/stockvalue/stockvalue_backend/stockvaluefinder && uv run pytest tests/unit/test_services/test_pledge_risk_service.py -v --cov=stockvaluefinder.services.pledge_risk_service --cov-report=term-missing
</verification>

<success_criteria>
- All 5 combination upgrade rules implemented as separate pure functions (D-04)
- All rules always evaluated (no short-circuit), producing full audit trail (D-05)
- merge_risk_levels implements upgrade-only semantics correctly
- PledgeRiskAnalyzer.analyze() produces complete PledgeRiskResult for all scenarios
- Integration tests cover: normal case, multiple rule triggers, zero-pledge, HK, data unavailable
- Red flags contain specific data values in Chinese format per D-08
- Full test suite passes with coverage >= 80%
</success_criteria>

<output>
Create `.planning/phases/30-pledge-risk-calculation/30-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Modified files
- `stockvaluefinder/stockvaluefinder/services/pledge_risk_service.py` -- adds 5 combination rule functions, merge_risk_levels, _RISK_ORDER constant, complete analyze() implementation
- `stockvaluefinder/stockvaluefinder/tests/unit/test_services/test_pledge_risk_service.py` -- adds test classes for combination rules, merge, and integration scenarios

### New functions (in pledge_risk_service.py)
- `_RISK_ORDER` -- module-level constant dict for RiskLevel ordering
- `check_high_pledge_with_price_drop()` -- RISK-04 rule 1
- `check_holder_over_80()` -- RISK-04 rule 2
- `check_closeout_margin_low()` -- RISK-04 rule 3
- `check_high_pledge_with_financial_high()` -- RISK-04 rule 4
- `check_high_pledge_with_存贷双高()` -- RISK-04 rule 5
- `merge_risk_levels()` -- RISK-05 merge logic
- `PledgeRiskAnalyzer.analyze()` -- complete orchestration (was stubbed in Plan 01, now fully wired)
